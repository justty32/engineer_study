# 驗收紀錄 — 電機機械（零基礎互動課）

← 課程首頁：[index.html](index.html)｜建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)｜立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜品質關卡：[QUALITY-GATES](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)

**本檔為獨立驗收員（未參與建置）的複驗紀錄，不採信建置者自報，全部重驗一次。**

- 測試日期：2026-09-18（初驗）／2026-09-18（複驗：Lead 修正 2 項失敗後重跑）
- 驗收方式：獨立 verifier（sonnet），未參與本課建置，僅讀 BUILD-SPEC、PROJECT-BRIEF 與範本驗收紀錄格式後重跑全部關卡；複驗時不採信 Lead「已修正」的自報，逐檔逐行重新掃描與重算。
- 環境：Linux 6.18.49-1-MANJARO；Node v24.14.1；Python 3.14.7；**無無頭瀏覽器（無 Playwright／headless Chrome）**，桌面／手機視覺、觸控、螢幕閱讀器朗讀一律列「待人工」。
- 檔案清單：13 個章節 HTML（00–12）＋`index.html`＋`名詞與概念字典.html`＋`自我檢核.html`＋`app.js`＋`styles.css`＋`BUILD-SPEC.md`＋`PROJECT-BRIEF.md`，共 18 個產品檔，缺檔 0。

---

## 1. 驗證矩陣（A–M，對應任務指示字母）

| # | 項目 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| A | QUALITY-GATES 可複製片段 1–6（id 契約／外部資源／LaTeX／編碼／相對連結／`node --check`＋行數） | **通過** | id 契約：app.js 用 `$`／`val`／`pick`（非字面 `byId`／`getElementById`，已改用等效抽取法）取得 129 個 kebab-case id 字串，全部在 HTML 有對應 `id="..."`，缺 0。外部資源：非 `justty32.github.io` 的 `src`／`href` 命中 0。LaTeX 殘留 `\(` `\)` `\[` `\]` `$$`：0 檔命中。編碼：`iconv -f UTF-8 -t UTF-8` 全部通過；BOM／U+FFFD／U+0080–009F／U+E000–F8FF 命中 0。相對連結（含跨課 `../電磁學-互動課程/…`、`../電路學-互動課程/…`、`../電子學-互動課程/…`、`../../04-電力特化/P1-電力系統-互動網站/index.html` 共 27 個相異目標）逐一 `[ -e ]` 檢查，broken 0。`node --check app.js` 通過。行數：HTML 最大 436 行（`名詞與概念字典.html`），`app.js` 436 行，皆在參考線內 |
| B | pager 鏈 index → 00 → … → 12 → 字典 → 自我檢核 → index | **通過** | 16 頁逐一讀 `<nav class="pager">` 內容，前後相連成一個環，孤兒頁 0 |
| C | 數值重算（13 個 `xxxCalc`，預設值＋每章至少 2 組邊界值，比對 BUILD-SPEC 粗體數字到 6 位小數） | **通過** | Node `require("app.js")` 對 13 個函式共執行 **307 項斷言**（預設值 + BUILD-SPEC 明列的邊界對照全部覆蓋），**0 個失敗**。另外用 Python **獨立重寫**（不呼叫 app.js）04（`xfreg`）、08（`imtorque`）、10（`vcurve`）三章公式，共 **35 項斷言**，**0 個失敗**——BUILD-SPEC 本身在這三章的算術**沒有發現錯誤** |
| D | 正文數字與規格一致（抽取 HTML 內 `\d+\.\d{6}` 字串，比對 BUILD-SPEC 對應章行號範圍） | **通過（有 11 個非逐字但已驗證正確的衍生值，見下方說明）** | 13 章共抽出 **609** 個六位小數字串；其中 598 個逐字出現在 BUILD-SPEC 對應章節行號範圍內。另 11 個未逐字出現，但逐一手算／程式重算後全部數值正確（02 章「100.000000 倍」＝ a²＝10²；06 章轉矩–轉速取樣表 5 個點與「24.000000 倍」＝480/20，皆與 `dcmotorCalc` 結果一致；09 章取樣表 δ=30° 之 P=49500.000000＝3×220×300×sin30°/2；11 章「0.550000」＝1.1×0.5 起動判準門檻）。這些是 BUILD-SPEC 只給概念、未逐字列出的衍生量，不是數值錯誤 |
| E | 字元級掃描（先 `html.unescape`） | **通過（複驗後）** | 簡體字表（电机压变损绕铁线转动极频励导图计数应术输负载联关开闭发现实际测单双环绝缘检论）命中 **0**。配額詞：「轉差率」「勵磁」「電動機」（排除「同步電動機」重複計算後）各 **恰好 1**，且三處都在 `名詞與概念字典.html` 對應卡的「不要混淆」段（第 38、78、298 行）；「同步電動機」「反電勢」「圈數」「鐵芯」「等值電路」「籠型」各 **0**。形近錯字「滑叉」「激滋」「電柩」「轉距」「氣隨」「同歩」「崩壞轉矩」「匝数」全部 **0**；對照的正確寫法「滑差」94、「激磁」145、「電樞」66、「轉矩」327、「鐵心」50、「氣隙」80、「崩潰」41 次以上，寫法一致。符號：ASCII `*` 當乘號 **0**（全課 `*` 只出現在 `x*`／`η*` 變數名，非乘號）；U+03BC μ **0**；U+2126 **0**；`<option>` 內無標籤；`$` 字面 **0**。**複驗**：Lead 已將 `03-變壓器等效電路與試驗.html` 第 52 行、`04-電壓調整率與效率.html` 第 51 行、`05-旋轉機共同語言.html` 第 51 行的字面「NaN」改為「非數值」，複驗員對全課 16 個 HTML 重新執行 `grep -noE 'NaN\|Infinity\|undefined'`，**命中 0**，確認修正屬實，Gate E 改判**通過** |
| F | 模式型 widget（00 `lm-mode`、02 `rf-mode`、11 `st-method`）output 開頭需有「這個模式只用到…」或同義句，app.js 對應 draw 也需有 | **通過（複驗後）** | 02 `referral`：自耦模式「這個模式只用到 V_1、N_1、N_2，其餘控制不影響結果。」、折算模式「這個模式只用到 V_1、N_1、N_2、R、X。」，HTML 與 app.js（第 288、294 行）皆符合。11 `starting`：「這個方式只用到 k、t 與負載轉矩；自耦分接頭只在自耦模式有效。」（HTML 第 46 行、app.js 第 393 行），符合。**00 `linmach`（複驗）**：Lead 裁決採用 BUILD-SPEC 第 4 節前言（第 125 行）的通用句式，已把 `00-線性直流機與能量轉換.html` 第 129 行與 `app.js` 第 259、265 行改為「這個模式只用到六個控制的同一組值，把 F 當成負載力（馬達）／外力（發電機）。」。複驗員重新讀取三處，確認 HTML 與 app.js 兩處皆含「這個模式只用到」字樣，句式與 02、11 一致，Gate F 改判**通過**；已記錄裁決：BUILD-SPEC 第 00 章專屬「正確性判準①」（約第 220 行）原文與第 125 行通用規則不一致一事，Lead 選擇讓產品文案服從通用規則，BUILD-SPEC 本身的措辭尚未回頭同步（見第 4 節已知限制） |
| G | 邊界 NaN 掃描（各滑桿 min／max、`R=X=0`、`n=0`、`Ef=40`、`s=1`、`P_sc=260` 等極端值） | **通過** | 13 個 Calc 函式對所有滑桿 min／max 組合＋題目指定的極端值（`referral R=X=0`、`imslip/imtorque/powerflow n=0`、`imtorque s=1`、`vcurve Ef=40`、`xftest Psc=260`）共執行數千組合，`JSON.stringify` 後掃描 **NaN／Infinity／undefined／裸 -Infinity 命中 0**。`null` 出現 320 處，全部對應 BUILD-SPEC 明文的「不適用」情境（如 `Pin=0` 時 `eta=null`、短路 `R=X=0` 時 `I2=null`、`B2≤0` 時 `Xm_lv=null` 等），符合規格 |
| H | 字數（去除 `section.workspace`、去標籤，漢字＋全形標點 1700–2500） | **通過** | 13 章實測：00＝1751、01＝1719、02＝1754、03＝1848、04＝1724、05＝1727、06＝1974、07＝2050、08＝1840、09＝1838、10＝1712、11＝2117、12＝2388，全部落在 1700–2500 區間內 |
| I | 結構（workspace／七段 h2／`p.prereq`＋`p.note` 來源／`aria-current`／index 15 張卡／字典 50 卡六組／自我檢核 39 題） | **通過** | 13 章每章恰好 1 個 `section.workspace`；七段 h2（痛點／白話直覺／推導／數值實例／不要混淆／可觀察證據／連到哪門課）全部齊全；每章都有 `p.prereq` 與帶「來源：12-電機機械 §」字樣的 `p.note`（各 1 個）。`aria-current="page"` 僅出現在 `index.html`（第 14 行）、`00-…html`（第 15 行）、`名詞與概念字典.html`（第 3 行）、`自我檢核.html`（第 3 行）各自的自我連結上，且每檔恰好 1 個。index 15 張 `chapter-card`，13 章標題（冒號前）與各章 `<h1>` 冒號前完全一致，其餘 2 張對應字典／自我檢核。字典 `term-card` 恰好 50 張，6 組（8/10/7/6/9/10＝50），逐卡皆含白話／位置／機制／不要混淆四段。自我檢核 39 題，`q00-1`…`q12-3` 全部齊全、無缺無重複；13 個 `select` 型題目首項 `value=""`，答案（`sel` 型的 `ans`）皆存在於對應 `<option>` 清單中 |
| J | 樣式與檔案（`styles.css` 正本閘門／無新增 class／無 inline style／無 `<style>`／不連 `.md`／不含禁用函式） | **通過** | `cmp` 與 `專題/機器人運動學/互動課程/styles.css` **位元組相同**（sha256 皆為 `80eb33ac…`）。全課 HTML 使用到的 class 集合為 `{card, chapter-card, chapter-list, controls, dictionary-tools, eyebrow, flow, lead, main-site-link, note, number, output, pager, prereq, site-footer, term-card, term-list, topbar, warning, workspace}`，皆在 BUILD-SPEC 第 13 行允許清單內，新增 class **0**。`style=` 屬性 **0**、`<style>` 區塊 **0**、連向 `.md` 的 `href` **0**。`app.js` 內 `Math.random`／`setTimeout`／`localStorage`／`fetch(` 命中 **0** |
| K | CRLF（`*.html`、`app.js`、`*.md` 行尾 `\r\n`，無孤 `\n`，無 BOM；`styles.css` 不檢查） | **通過** | 16 個 HTML＋`app.js`＋`BUILD-SPEC.md`＋`PROJECT-BRIEF.md` 共 19 個檔逐位元組掃描，CRLF 均與檔案行數相符、裸 LF **0**、BOM **0**。**複驗**：Lead 用 `sed` 修改 5 個檔（00、03、04、05 之 HTML＋`app.js`）後，逐位元組重掃該 5 檔，CRLF 依舊完整、裸 LF **0**、BOM **0**，`node --check app.js` 亦重跑通過 |
| L | 規模（HTML ≤ 1000 行、`app.js` ≤ 2000 行、`BUILD-SPEC.md` ≤ 1400 行） | **通過** | HTML 最大 436 行、`app.js` 436 行、`BUILD-SPEC.md` 1065 行，全部在參考線內 |
| M | 無無頭瀏覽器 → 桌面 1440×900／手機 390×844 視覺、觸控、螢幕閱讀器朗讀 | **待人工** | 本機無 Playwright／headless Chrome，且驗收指示禁止安裝任何東西，此關無法自動化執行，需人工用真實瀏覽器複驗版面、觸控互動與螢幕閱讀器朗讀順序 |

---

## 2. 數值重算細節（Gate C／D 補充）

- Node 307 項斷言涵蓋：`linmachCalc`（10 組）、`corefluxCalc`（6 組）、`referralCalc`（9 組）、`xftestCalc`（6 組）、`xfregCalc`（7 組）、`powerflowCalc`（5 組）、`dcmotorCalc`（7 組）、`imslipCalc`（6 組）、`imtorqueCalc`（含 8 點轉矩–滑差取樣表全比對＋6 組邊界）、`syncpowerCalc`（11 組，含 δ = 0/90/120/180 全邊界）、`vcurveCalc`（含 V 曲線 7 點取樣表全比對＋7 組邊界）、`startingCalc`（4 種起動方式＋3 組邊界）、`motorpickCalc`（6 個應用場景第一名與總分＋全權重 3 的總分表＋全零權重邊界）。**0 個失敗**。
- Python 獨立重算（不引用 `app.js`，重新用 BUILD-SPEC 公式手刻）：04 章電壓調整率／效率公式、08 章戴維寧等效＋轉矩–滑差公式、10 章 V 曲線公式，共 35 項斷言，**0 個失敗**——這三章的 BUILD-SPEC 本身**未發現算術錯誤**。
- Gate D 的 11 個「未逐字命中」數字：02 章 1 個（`a²` 衍生倍數）、06 章 5 個（轉矩–轉速取樣表 4 點＋「24.000000 倍」衍生比）、09 章 2 個（δ=0°／180° 之 `P=0.000000`、δ=30° 之 `P=49500.000000`）、11 章 1 個（`1.1×0.5=0.550000` 判準門檻）——全數手算或程式重算核實無誤，屬 BUILD-SPEC 描述性但未逐字列出的衍生值，非規格與正文不一致。

---

## 3. 失敗項清單（複驗後）

**初驗發現的 4 處失敗，Lead 已全數修正並經複驗確認，目前失敗項為 0。** 修正歷程留存如下，供追溯：

1. ~~`00-線性直流機與能量轉換.html` 第 129 行、`app.js` 第 259、265 行：`linmach` widget 文案未用「只用到」句式~~ → **已修正**。Lead 裁決採 BUILD-SPEC 第 4 節前言（第 125 行）通用句式，三處均已改為含「這個模式只用到」的文字，複驗確認一致。裁決記錄：BUILD-SPEC 第 00 章專屬「正確性判準①」（約第 220 行）原文與此裁決不同步，屬規格文件本身待更新，非產品缺陷（見第 4 節）。
2. ~~`03-變壓器等效電路與試驗.html` 第 52 行：字面「NaN」~~ → **已修正**為「非數值」，複驗確認全課 HTML 的 NaN／Infinity／undefined 掃描命中 0。
3. ~~`04-電壓調整率與效率.html` 第 51 行：字面「NaN」~~ → **已修正**為「非數值」，複驗確認。
4. ~~`05-旋轉機共同語言.html` 第 51 行：字面「NaN」~~ → **已修正**為「非數值」，複驗確認。

全部 13 關（A–L，M 待人工）**複驗後皆為通過**，證據齊全；D 附帶 11 個已核實正確但未逐字命中的衍生值說明，不影響通過判定。

---

## 4. 已知限制

- 本機無 Playwright／無頭瀏覽器，且驗收規則禁止安裝任何工具，因此 Gate M（桌面 1440×900／手機 390×844 視覺回歸、觸控互動、螢幕閱讀器朗讀順序）**無法自動化驗證**，列為待人工，尚未有證據可判定通過或失敗，仍是交付前唯一剩餘的待辦。
- BUILD-SPEC 第 00 章專屬「正確性判準①」（約第 220 行）原文寫的是「這個模式把 F 當成負載力（馬達）／外力（發電機）」，與第 4 節前言（第 125 行）的通用句式規則不同步。Lead 已裁決產品文案改採通用句式（見 Gate F），但 BUILD-SPEC 文件本身尚未回頭同步修訂第 00 章那一格契約文字，僅記錄供之後若有人重讀 BUILD-SPEC 時對照，不影響本次產品驗收結論。

---

## 5. 交付判定

**通過，允許交付**：核心關卡（內容正確性、數值計算、離線與節流、技術驗證、結構、樣式、CRLF、規模）與字元級掃描、模式型 widget 文案全數通過，13 個 `xxxCalc` 函式共 342 項獨立斷言（Node 307 ＋ Python 35）零失敗，未發現 BUILD-SPEC 算術錯誤；初驗發現的 4 處字面規則失敗（00 章 widget 文案、NaN 字樣 ×3）經 Lead 修正、複驗員重新逐檔重跑掃描與 `node --check`／CRLF 檢查後全數確認修正屬實。**唯一未完成項為 Gate M**（桌面 1440×900／手機 390×844 視覺、觸控、螢幕閱讀器朗讀），因本機無無頭瀏覽器仍列待人工，建議安排人工複驗後即可視為完全合格交付。
