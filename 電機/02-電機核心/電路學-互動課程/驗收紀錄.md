# 驗收紀錄 — 電路學（零基礎互動課）

- **驗收日期**：2026-08-27
- **驗收對象**：`電機/02-電機核心/電路學-互動課程/`（18 個 HTML ＋ `app.js` ＋ `styles.css`）
- **驗收依據**：本目錄 `BUILD-SPEC.md` 第 8 節、`wf/workflows/interactive-study-site/QUALITY-GATES.md`（含硬體原理子工作流加驗）、`wf/workflows/interactive-study-site/BUILD-WITH-CODEX.md` 流程第 3 步
- **驗收員**：獨立驗收 agent（未參與建置）。**唯一寫入檔案為本檔；產品檔全程唯讀，發現問題只記錄不修。**
- **結論**：**允許交付**（0 個阻斷級缺陷；6 項低嚴重度觀察，全部不影響正確性與可用性，交由 Lead 裁決是否處理）

---

## 1. 環境

| 項目 | 值 |
| --- | --- |
| 作業系統 | Windows 11 Pro 10.0.26200 |
| Shell | Git Bash（POSIX）／PowerShell 7 |
| Node.js | v24.11.1（`C:\Users\WG-Guanyu\AppData\Local\ms-playwright-go\1.57.0\node.exe`） |
| Python | 3.13.0 |
| Playwright | playwright-core 1.57.0（系統既有，未下載任何瀏覽器） |
| 瀏覽器 | Google Chrome 151.0.7922.174（`channel:'chrome'`，headless） |
| 本機伺服器 | `python -m http.server 8931 --bind 127.0.0.1`，document root＝本課目錄 |
| 假 DOM 驅動 | 自寫 node + `vm` 沙箱腳本（暫存於 scratchpad，未寫入 repo） |

不採信 builder 自報，全部項目獨立重跑。機械掃描下放 sonnet 子 agent（唯讀），裁決、內容抽查、數值重算、widget 邊界抽測與瀏覽器實測由驗收員親自執行。

---

## 2. 逐項結果總表

| # | 檢查項 | 結果 | 證據位置 |
| --- | --- | --- | --- |
| 1 | 機械掃描（檔案／編碼／LaTeX／外部資源／CRLF／pager／連結） | **通過** | §3 |
| 2 | id 契約逐一比對（222 個 id 雙向差集） | **通過** | §4 |
| 3 | `styles.css` 正本閘門（`cmp` ＋ `sync-styles.py --check`） | **通過** | §5 |
| 4 | 內容正確性抽查（02／05／07／10／11 共 5 章，親手重算） | **通過** | §6 |
| 5 | 規格數值獨立重算（§8.4 的 8 組 ＋ 3 組加驗 ＋ 其餘全章） | **通過**（規格 0 筆算術錯） | §7 |
| 6 | 互動行為邊界抽測（rlcdamp ζ=1／thevenin k→0／impedance X=0／transformer 匝比極值） | **通過**（無 NaN／Infinity／undefined 洩漏） | §8 |
| 7 | 自我檢核行為抽測（數值對／單選對／空白／答錯／reset） | **通過** | §9 |
| 8 | 教學品質（QUALITY-GATES 1、2 節＋硬體加驗） | **通過** | §10 |
| 9 | 字元級錯字掃描（簡體／形近／單位／負號／全形／佔位符） | **通過**（2 筆用詞不一致，低嚴重度） | §11 |
| 10 | 拆檔量測（行數與位元組） | **通過**（`BUILD-SPEC.md` 1369 行超參考線，Lead 已裁決維持單檔） | §12 |
| 11 | 瀏覽器實測（1440×900 ＋ 390×844，18 頁） | **通過** | §13 |

---

## 3. 機械掃描

### 3.1 檔案存在性與語法

- 20 個目標檔全部存在，檔名逐字相符（含半形大寫、無空格的 `02-KCL與KVL.html`、`07-一階暫態RC與RL.html`、`08-二階RLC暫態.html`、`13-RLC共振與品質因數.html`）。**通過**
- `node --check 電機/02-電機核心/電路學-互動課程/app.js` → exit code 0，無輸出。**通過**（本次環境有 node，不需要走 BUILD-SPEC §8.1 的瀏覽器替代條款）

### 3.2 編碼

| 項目 | 結果 |
| --- | --- |
| BOM（`head -c 3` = `EF BB BF`） | 20 檔全部無 BOM。**通過** |
| UTF-8 嚴格解碼（`errors='strict'`） | 20 檔全部成功。**通過** |
| U+FFFD 出現次數 | 0。**通過** |
| U+0080–U+009F（C1 控制碼） | 0。**通過** |
| U+E000–U+F8FF（PUA） | 0。**通過** |
| CRLF（18 HTML ＋ `app.js`） | 逐檔 `LF 數 == CRLF 數`、裸 `\r` = 0（例：`00-電路世界觀.html` LF=70 CRLF=70；`app.js` LF=858 CRLF=858）。**通過** |
| `styles.css` 換行 | 依規約不檢查、不轉換（靠位元組比對驗收）。 |

### 3.3 公式與外部資源

| 項目 | 結果 |
| --- | --- |
| 18 HTML 內 `\(` `\)` `\[` `\]` `$` `MathJax` `KaTeX` | 全部 **0 筆**。**通過** |
| `app.js` 的 `$` 用法 | 每次出現後一字元皆為 `(` 或 `{`；唯一例外為第 7 行別名定義 `const $=x=>document.getElementById(x)`。`MathJax`／`KaTeX` = 0。**通過** |
| 外部 URL | `https://justty32.github.io/engineer_study/` 在 18 個 HTML 中各恰好 1 次（共 18 次）；無其他 http／https、無 `//cdn`、無 `src="http`、無 `@import`。**通過** |
| `Math.random` / `localStorage` / `fetch(` / `XMLHttpRequest` / `document.write` / `setTimeout` / `<style` / `style="` | 全資料夾各為 **0**。**通過** |
| HTML 內未跳脫的 `<`（可能被解析成標籤） | 0 筆。`app.js` 輸出字串中的 `<` 全部後接空白（如 `n < 1`、`X < 0`），HTML 剖析器視為文字，實測渲染正確。**通過** |

### 3.4 連結、pager 與骨架

| 項目 | 結果 |
| --- | --- |
| 全部 30 個 unique `href` 相對路徑 | 目標檔案全部存在。**通過** |
| `../../../共通基礎/數學/互動課程/*.html` | 9 個目標（01–09 章 ＋ `index.html`）全部存在。**通過** |
| `../../../專題/IoT聯網裝置/知識群互動網站/06-板級介面與工業匯流排/index.html` | 存在。**通過** |
| `pager` 前後相連 | 18 頁逐頁比對 BUILD-SPEC §2.2 的表，全部相符（含 `index → 00`、`14 → 名詞與概念字典 → 自我檢核 → index` 的環）。**通過** |
| `index.html` 的 `chapter-card` | 恰好 17 個，href 涵蓋 00–14 ＋ 字典 ＋ 自我檢核，無孤兒頁。**通過** |
| `index.html` 對 BUILD-SPEC §2.4 | `aria-current="page"` 在「課程地圖」、`h1` 逐字相符、`div.flow` 12 個 `<span>` 順序相符、`p.lead`／`p.note`／`p.warning` 各 1、`number` 用「查」「檢」、`先修與去向`＋`來源` 兩個 h2 齊備、工數與 IoT 連結齊備。**通過** |
| `topbar` 的 `aria-current` 規則 | 只有 00 標在「00 起點」，01–14 皆不標；`index`／字典／自我檢核 各標自己那一項。與 §2.1 一致。**通過** |
| 七段教學骨架 | 00–14 各章依序恰有「痛點／白話直覺／推導／數值實例／不要混淆／可觀察證據／連到哪門課」7 個 `<h2>`，順序全部一致。**通過** |
| `section.workspace` 數量 | 每章恰好 1 個。**通過** |
| `<p class="note">來源：…` | 每章恰好 1 個且有具體章節指向（例：07 章「來源：06-電路學-下 §5.1–§5.2。」）。**通過** |
| 「不要混淆」`<li>` 數 | 每章 6–8 條，全部 ≥ 4。**通過** |
| index 章卡標題 vs 各章 `h1` vs `<title>` | 17 組逐一核對，全部一致。**通過** |

### 3.5 正文中文字數（BUILD-SPEC §8.3 第 17 項，門檻 2000–3000）

量測方式：移除 `section.workspace` 整段、`<head>`、`<script>`，去標籤、反轉義後統計 U+4E00–U+9FFF。

| 章 | CJK 字數 | 判定 |
| --- | ---: | --- |
| 00-電路世界觀 | 2220 | 通過 |
| 01-電阻與歐姆定律 | 2179 | 通過 |
| 02-KCL與KVL | 2133 | 通過 |
| 03-節點與網目分析 | 2165 | 通過 |
| 04-疊加原理與相依源 | 2246 | 通過 |
| 05-戴維寧與最大功率 | 2269 | 通過 |
| 06-電容與電感 | 2252 | 通過 |
| 07-一階暫態RC與RL | 2291 | 通過 |
| 08-二階RLC暫態 | 2396 | 通過 |
| 09-弦波與相量 | 2415 | 通過 |
| 10-阻抗與交流穩態 | 2462 | 通過 |
| 11-交流功率與功因 | 2183 | 通過 |
| 12-一階濾波器與頻率響應 | 2482 | 通過 |
| 13-RLC共振與品質因數 | 2379 | 通過 |
| 14-變壓器與三相 | 2508 | 通過 |

15 章全數落在 2000–3000，最低 2133、最高 2508，厚薄均勻（極差 375 字）。**通過**

---

## 4. id 契約逐一比對

比對方式：以 `sed -n` 抽出 BUILD-SPEC 第 4／5／6／7 節，程式化擷取規格側的 id、`min`／`max`／`step`／`value`、`option value` 序列、output id，與 HTML 與 `app.js` 實際值做集合與序列比對（非肉眼掃描）。

| 對象 | 結果 |
| --- | --- |
| 15 章 widget 控制 id（合計 68 個） | id 拼寫、`min`／`max`／`step`／`value`、`option value` 集合與**順序**、output id，**逐章零差異**。**通過** |
| 15 個 output id | 零差異。**通過** |
| id 唯一性（`grep -c 'id="<id>"'` 必須 = 1） | 無任何 id 出現 0 次或 ≥2 次。**通過** |
| `名詞與概念字典.html` | `term-search`、`term-count` 各 1 次；`term-card` **62 張**（分五組 12／12／11／13／14，其中末組含 op-amp、雙埠兩張「留給後續課」卡），與 §5.2 的 12+12+11+13+12+2=62 相符。每張卡四段標籤順序固定為「白話／位置／角色／機制／不要混淆」，全部具 `data-search`。**通過** |
| `自我檢核.html` | 45 題（每章 3 題）的「題目 id ＋ `-check` ＋ `-output`」三件套齊全且各 1 次，共 135 個；`quiz-reset`、`quiz-progress` 各 1 次。13 個單選題 `option value` 順序皆為 `""→a→b→c→d`。45 題的 `ans`／`tol` 與 §6.3 表格逐題相符。**通過** |
| `app.js` ↔ HTML 雙向差集 | app.js 引用但 HTML 不存在的 id：**空集合**；HTML 有但 app.js 未綁定的控制 id：**空集合**。兩側集合各 222 個，完全對等（與 §8.2 宣告的 222 一致）。**通過** |
| 守衛函式清單 vs §7.1／§7.2 | 15 個 widget 函式 ＋ `dictionary` ＋ `selfcheck`，定義順序、註冊 `forEach` 清單順序、`if(typeof document!=="undefined")` 包裹方式、每個函式的 guard id／output id／監聽 id 清單，**逐字相符**。**通過** |
| §7.5 程式規則 | 未見 `Math.random`、`setTimeout`、`localStorage`、`fetch(`、`document.write`。**通過** |

---

## 5. `styles.css` 正本閘門（固定項）

```
$ cmp 專題/機器人運動學/互動課程/styles.css 電機/02-電機核心/電路學-互動課程/styles.css
（無輸出）→ 位元組相同

$ md5sum 專題/機器人運動學/互動課程/styles.css 共通基礎/數學/互動課程/styles.css 電機/02-電機核心/電路學-互動課程/styles.css
cb9d27ba5eda812ae95a18d6aabd5c5d  專題/機器人運動學/互動課程/styles.css
cb9d27ba5eda812ae95a18d6aabd5c5d  共通基礎/數學/互動課程/styles.css
cb9d27ba5eda812ae95a18d6aabd5c5d  電機/02-電機核心/電路學-互動課程/styles.css
```

三份完全相同（4141 bytes）。注意 BUILD-SPEC §8.1 第 3 項寫的比對對象是 `共通基礎/數學/互動課程/styles.css`，而工作流的正本是 `專題/機器人運動學/互動課程/styles.css`——**兩者本身位元組相同，故此差異不影響結論**。**通過**

```
$ python wf/workflows/interactive-study-site/tools/sync-styles.py --check
正本　　：專題/機器人運動學/互動課程/styles.css
sha256　：894f24a10105d006f0cd6742aff11a7ac970e8c82a1411a1efbeb8d1f00f3c2a
位元組數：4141
[相同] 專題/機器人數學基礎/互動課程
[相同] 專題/作業系統/互動課程
[相同] 專題/Linux/互動課程
[相同] 共通基礎/數學/互動課程
共 4 門下游課程：4 門相同，0 門漂移，0 門缺檔
[通過] 全家族與正本位元組相同。
EXIT=0
```

**exit 0，通過。** 本課尚未登記進 `FAMILY` 清單屬正常——`sync-styles.py` 第 50–53 行已明文註記「尚未登記（建置中，交付後由 Lead 加入）：… 電機/02-電機核心/電路學-互動課程」。**登記由 Lead 收尾執行，此處僅記載。**

---

## 6. 內容正確性抽查（親自重算，5 章）

抽查方式：讀來源筆記 `電機/02-電機核心/06-電路學-上.md`（134 行）與 `06-電路學-下.md`（140 行）全文，再讀 BUILD-SPEC 該章契約與產品 HTML 全文，逐條核對公式、推導完整性、數值與筆記出處。

### 6.1 章 02 KCL 與 KVL（來源：上 §1.3）

- 公式無走樣：KCL＝節點電流代數和為 0（電荷守恆）、KVL＝閉迴路電壓代數和為 0（能量守恆），與筆記 §1.3 逐字對應。
- 推導（a）(b)(c)(d) 四步齊備：並聯化簡→分壓求 V<sub>A</sub>→KCL 殘差→KVL 殘差→功率交叉驗算。§2.1 要求的「失效條件」兩句（節點寄生電容囤電荷、回路穿過變化磁通的 −dΦ/dt 項）都寫在白話直覺段。
- **親手重算**：R<sub>2</sub>‖R<sub>3</sub> = 6×10⁶/5000 = 1200 Ω ✓；V<sub>A</sub> = 12×1200/2200 = 6.545455 V ✓；I₁ = 5.454545 mA ✓；I₂ = 3.272727 mA ✓；I₃ = 2.181818 mA ✓；KCL／KVL 殘差 0 ✓。功率：源 12×0.005454545 = 0.065455 W；R₁ = I₁²R₁ = 0.029752 W、R₂ = V<sub>A</sub>²/R₂ = 0.021421 W、R₃ = 0.014281 W，合計 0.065454 W ✓。
- 無杜撰。筆記沒有的內容（符號規約、KCL／KVL 失效條件）已在 BUILD-SPEC §10 第 4、5 條明列為刻意補寫。**通過**

### 6.2 章 05 戴維寧與最大功率（來源：上 §2.5、§2.7）

- 公式無走樣：V<sub>th</sub>＝開路電壓、I<sub>N</sub>＝短路電流、R<sub>th</sub>＝獨立源歸零後看入、P<sub>max</sub> = V<sub>th</sub>²/(4R<sub>th</sub>)，與筆記完全一致。
- 推導完整：(e) 步把 dP<sub>L</sub>/dR<sub>L</sub> = 0 的分子化到 V<sub>th</sub>²(R<sub>th</sub>+R<sub>L</sub>)(R<sub>th</sub>−R<sub>L</sub>) 並說明兩側較小故為最大值；(f) 步給 η = R<sub>L</sub>/(R<sub>th</sub>+R<sub>L</sub>) 且單調遞增，並補了 R<sub>L</sub>→0 與 R<sub>L</sub>→∞ 兩端功率皆為 0 的一致性檢查（比規格更完整，非杜撰）。
- **親手重算**：V<sub>th</sub> = 12×2000/3000 = 8 V ✓；R<sub>th</sub> = 666.666667 Ω ✓；I<sub>N</sub> = 12/1000 = 12 mA＝8/666.666667 ✓；P<sub>max</sub> = 64/2666.666667 = 24 mW ✓。負載掃描 k=0.3：R<sub>L</sub>=200、I<sub>L</sub>=8/866.666667=9.230769 mA、V<sub>L</sub>=1.846154 V、P<sub>L</sub>=17.041420 mW、η=23.077 % ✓；k=1：6 mA／4 V／24 mW／50 % ✓；k=3：3 mA／6 V／18 mW／75 % ✓。「效率 1.5 倍、功率少 25 %」：24→18 mW = −25 % ✓。**通過**

### 6.3 章 07 一階暫態 RC 與 RL（來源：下 §5.1–§5.2）

- 公式無走樣：v<sub>C</sub>(t) = v<sub>C</sub>(∞)+[v<sub>C</sub>(0⁺)−v<sub>C</sub>(∞)]e<sup>−t/τ</sup>、τ = R<sub>th</sub>C／L/R<sub>th</sub>、連續性條件，與筆記逐條對應。
- 推導完整：分離變數積分 → 指數化 → 代初值，全程寫出；量綱檢查 Ω×F = s、H/Ω = s 都寫了；(e) 步指出 RC 電流是純指數無常數項。
- **親手重算**：τ = 10⁴×10⁻⁵ = 0.1 s ✓。0.5τ：e<sup>−0.5</sup>=0.6065307 → v=1.967347 V、39.3469 %、i=0.303265 mA ✓；1τ：3.160603 V／63.2121 %／0.183940 mA ✓；2τ：4.323324／86.4665／0.067668 ✓；3τ：4.751065／95.0213／0.024894 ✓；5τ：4.966310／99.3262／0.003369 ✓。RL 對照：10 mH/10 kΩ = 1 µs ✓、10 mH/100 Ω = 100 µs ✓。上升時間 t<sub>r</sub> = ln9·τ = 2.197τ ✓。
- 跨課錨定「工程數學先修第 07 章 τ=1 s 時 t=τ 得 3.160603 V」——同一數字，因 e<sup>−t/τ</sup> 只認比值，說法正確。**通過**

### 6.4 章 10 阻抗與交流穩態（來源：上 §4.2–§4.3、§4.5）

- 公式無走樣：Z<sub>R</sub>=R、Z<sub>L</sub>=jωL、Z<sub>C</sub>=1/(jωC)=−j/(ωC)、Y=1/Z、共軛匹配 Z<sub>L</sub>=Z<sub>th</sub>*，與筆記一致。
- 推導完整：(a) 由 v = L·di/dt 代入 Re{**I**e<sup>jωt</sup>} 得 **V** = jωL**I**；(b) 電容同理並有理化出負電抗；(c) 乘 j 等於轉 90°；(d) 串聯總 Z 與虛部歸零；(e) 由 |Z|、∠Z 求電流；(f) 額外補「為何阻抗必須是複數」。符號、單位、假設（理想元件、無繞線電阻／磁芯損耗／漏電）都標了。
- **親手重算**：ω = 6283.185307 rad/s ✓；X<sub>L</sub> = 62.831853 Ω ✓；X<sub>C</sub> = 159.154943 Ω ✓；X = −96.323090 Ω ✓；|Z| = √(2500+9278.137) = 108.527129 Ω ✓；∠Z：cos θ = 50/108.527129 = 0.460712 → θ = −62.566791° ✓；I = 10/108.527129 = 92.1431 mA ✓；f₀ = 1/(2π√(10⁻²×10⁻⁶)) = 1591.549431 Hz ✓。1000 Hz < f₀ → 容性 ✓。**通過**

### 6.5 章 11 交流功率與功因（來源：上 §4.4）

- 公式無走樣：P = V<sub>rms</sub>I<sub>rms</sub>cos θ、Q = V<sub>rms</sub>I<sub>rms</sub>sin θ、S = √(P²+Q²)、**S** = P+jQ = **V**<sub>rms</sub>**I**<sub>rms</sub>*，與筆記一致。
- 推導完整：積化和差 cos A cos B = [cos(A−B)+cos(A+B)]/2 完整展開得 p(t) = (V<sub>m</sub>I<sub>m</sub>/2)[cos θ + cos(2ωt−θ)]，再代 rms；功率三角形正交性；並聯電容不改負載電壓；C = Q<sub>C</sub>/(ωV<sub>rms</sub>²) 完整推導（筆記缺此式，BUILD-SPEC §10 第 7 條已列為刻意補寫）；線損比 = (pf₁/pf₂)²。
- **親手重算**：|Z| = √156.25 = 12.5 Ω ✓；θ = atan(0.75) = 36.869898° ✓；pf = 0.8 ✓；I = 17.6 A ✓；S = 3872 VA ✓；P = 3097.6 W（= I²R = 309.76×10）✓；Q = 2323.2 var（= I²X = 309.76×7.5）✓。校正：θ₂ = arccos(0.95) = 18.194872° ✓；tan θ₂ = √0.0975/0.95 = 0.328684 ✓；Q₂ = 1018.131884 var ✓；Q<sub>C</sub> = 1305.068116 var ✓；C = 1305.068116/(376.9911184×48400) = 7.15248×10⁻⁵ F = 71.5248 µF ✓；S₂ = 3260.631579 VA ✓；I₂ = 14.821053 A ✓；線損比 = 0.842105² = 0.709141 ✓。**通過**

### 6.6 抽查小結

5 章的公式、推導、數值、來源標註全部正確，**未發現任何走樣、遺漏推導或杜撰內容**。筆記本身沒有的內容（無任何數值範例、集總量化判準 d<λ/20、被動符號規約、KCL/KVL 失效條件、超越量公式、半功率閉合式、Q=1/(2ζ)、Δ 接 I<sub>L</sub>=√3I<sub>p</sub>、功因校正電容式）都已在 BUILD-SPEC §10 逐條列為刻意補寫，屬標準大學課綱內容，非杜撰。

---

## 7. 規格數值獨立重算（**驗收員親手算，不抄 spec**）

BUILD-SPEC §8.4 指定的 8 組必驗 ＋ 3 組加驗，另補算其餘章節的數值實例表：

| 組 | widget／章 | 重算結果 | 判定 |
| --- | --- | --- | --- |
| 21 | `ohm` | 串聯 R<sub>eq</sub>=3000 Ω、I=4 mA、V₂=8 V、P=48 mW；並聯 R<sub>eq</sub>=666.666667 Ω、I=18 mA | 與規格一致 |
| 22 | `kirchhoff` | V<sub>A</sub>=6.545455 V、I₁=5.454545、I₂=3.272727、I₃=2.181818 mA、殘差 0 | 一致 |
| 23 | `nodal2` | G=[[1.5,−0.5],[−0.5,0.833333]]、det=1.25−0.25=1.0 (mS)²、v₁=10.833333 V、v₂=12.5 V；代回得 10.000000／5.000000 mA | 一致 |
| 24 | `thevenin` | V<sub>th</sub>=8 V、R<sub>th</sub>=666.666667 Ω、I<sub>N</sub>=12 mA、P<sub>L</sub>(k=1)=24 mW、η=50 %；k=3 → 18 mW、75 % | 一致 |
| 25 | `rctau` | τ=0.1 s、v(1τ)=3.160603 V、63.2121 %；3τ→95.0213 %；5τ→99.3262 % | 一致 |
| 26 | `rlcdamp` | ω₀=10000 rad/s、α=5000 1/s、ζ=0.5、ω<sub>d</sub>=8660.254038 rad/s、OS=e<sup>−1.813799</sup>=16.3034 %、R<sub>臨界</sub>=200 Ω | 一致 |
| 27 | `impedance` | X<sub>L</sub>=62.831853、X<sub>C</sub>=159.154943、\|Z\|=108.527129 Ω、∠Z=−62.566791°、I=92.143 mA | 一致 |
| 28 | `acpower` | I=17.6 A、S=3872 VA、P=3097.6 W、Q=2323.2 var、pf=0.8、C=71.524808 µF、線損比 0.709141 | 一致 |
| 加驗 | `rcfilter` | f<sub>c</sub>=1591.549431 Hz；d=0 → \|H\|=0.707107、20log₁₀=−3.010300 dB、低通 −45°／高通 +45°；d=−2 → −0.000434／−40.000434 dB；d=−1 → −0.043214／−20.043214 dB | 一致 |
| 加驗 | `resonance` | Q=ω₀L/R=10、B=159.154943 Hz、半功率 1591.549431×(1.00124922∓0.05) = 1513.960154／1673.115097 Hz、差=159.154943 Hz；\|V<sub>L</sub>\|=Q·V<sub>s</sub>=10 V；掃描列 0.5／0.9／1.0／1.1／1.5 的 X、\|Z\|、I/I<sub>max</sub>、∠Z 全部重算一致 | 一致 |
| 加驗 | `transformer` | n=10 → V₂=22 V、Z<sub>ref</sub>=800 Ω；Y 接 V<sub>p</sub>=127.017059 V、P=6050 W；Δ 接 I<sub>L</sub>=47.631397 A、P=18150 W（恰 3 倍） | 一致 |
| 補算 | 章 00 | Q=1.2 C、電子數 1.2/1.602176634×10⁻¹⁹=7.489811×10¹⁸、P=0.1 W、W=6 J、λ=3 m、d/λ=0.033333；2400 MHz → λ=0.125 m、d/λ=0.8 | 一致 |
| 補算 | 章 01 | 串聯 3000 Ω／4 mA／4 V／8 V／16 mW／32 mW／48 mW；邊界 R₁=100、R₂=10000 → 分壓比 10000/10100=99.01 % | 一致 |
| 補算 | 章 04 | V<sub>s</sub> 分項 8 V、I<sub>s</sub> 分項 0.005×666.666667=3.333333 V、和 11.333333 V；節點法 (0.017)/(0.0015)=11.333333 V；P 總=64.2222 mW、分項和 37.5556 mW、交叉項 26.6667 mW | 一致（規格已自行標註第 4 位捨入差 1e−4 mW，屬顯示捨入，說明正確） |
| 補算 | 章 06 | i=100×10⁻⁶×1000=0.1 A、W<sub>C</sub>=5 mJ、v=10 mH×1000=10 V、W<sub>L</sub>=0.5 J；跳變代價 1000 A／10000 V | 一致 |
| 補算 | 章 09 | 10∠30°=8.660254+j5、6∠−45°=4.242641−j4.242641、和=12.902895+j0.757359、大小 12.925103、角度 3.359228°；16/12.925103−1=23.8 % | 一致 |
| 補算 | 章 13 邊界 | Δ 接 `tr-zl=1` → I<sub>p</sub>=220 A、I<sub>L</sub>=√3×220=381.05 A（規格寫 381 A） | 一致 |

**規格與產品的數值交叉檢查**：以程式抽出 BUILD-SPEC 各章「數值實例」段落中所有 6 位以上小數的數字，逐章檢查是否原樣出現在對應 HTML 中——15 章合計 198 個數值，**HTML 缺少 0 個**。

> **結論：BUILD-SPEC 本身 0 筆算術錯誤，產品也 0 筆轉抄錯誤。**（試點課曾出現規格兩處算術筆誤被忠實照抄，本課未重演。）

---

## 8. 互動行為邊界抽測（node 假 DOM）

方法：自寫 `vm` 沙箱假 DOM，從章節 HTML 解析全部控制項與初值，載入 `app.js` 觸發註冊，再逐一把每個 `range` 推到 `min`／`max`、每個 `select` 切過每個選項、以及全部同時推到 `min`／`max`，掃描 output 是否出現 `NaN`／`Infinity`／`undefined`／`null`／空白。

### 8.1 指定的 4 個邊界

| 邊界 | 設定 | 輸出摘要 | 判定 |
| --- | --- | --- | --- |
| `rlcdamp` ζ = 1 | R=200、L=10 mH、C=1 µF | 「ω₀ = 10000.000000 rad/s、α = 10000.000000 1/s、ζ = 1.000000、R<sub>臨界</sub> = 200.000000 Ω、Q = 0.500000、1/(2ζ) = 0.500000」→ 走**臨界阻尼**分支：「重根 s = −10000.000000 1/s，時間常數 1/α = 100.000000 µs。這是**不產生振盪的前提下最快**安定的設定，不是絕對最快。」 | **通過**：判準因果句齊備、0 洩漏。手算驗證：α=200/0.02=10000、ζ=1、R<sub>crit</sub>=2√(0.01/10⁻⁶)=200 ✓ |
| `thevenin` k → 0 | k 推到 0.1（滑桿下限）與強制 0 | k=0.1：R<sub>L</sub>=66.666667 Ω、I<sub>L</sub>=10.909091 mA、P<sub>L</sub>=7.933884 mW、P<sub>L</sub>/P<sub>max</sub>=33.058 %、η=9.091 %，附「接近短路」邊界句。k=0（超出滑桿範圍的強制值）：R<sub>L</sub>=0、I<sub>L</sub>=12 mA、V<sub>L</sub>=0、P<sub>L</sub>=0、η=0 %，**無除以零、無 NaN** | **通過** |
| `impedance` X = 0 | R=50、L=10 mH、C=1 µF、f=f₀=1591.5494309 Hz | X<sub>L</sub>=X<sub>C</sub>=100.000000 Ω、X=0.000000、\|Z\|=50.000000 Ω∠0.000000°、I=200 mA、f/f₀=1.000000 → 走**純阻**分支：「兩個電抗剛好抵消，Z = R，電流與電壓同相，而且此時 \|Z\| 最小、電流最大——這就是共振（第 13 章）。」另測 R=1 時額外觸發過流邊界提醒。`imp-f=0` 走保護分支「頻率或電容為 0 時容抗會發散，請把滑桿拉回一般值」，**不輸出 Infinity** | **通過** |
| `transformer` 匝比極值 | n=2000/10=200（極端降壓）、n=100/1000=0.1（極端升壓）、以及 wye／delta | n=200：V₂=3 V、I₁=0.015 A、Z<sub>ref</sub>=40000 Ω＝V₁/I₁、功率差 0 W。n=0.1：V₂=100 V、I₁=10 A、Z<sub>ref</sub>=1 Ω、走「升壓」分支＋「n ≤ 0.2」邊界提醒。三相：Y 接 127.017059 V／15.877132 A／6050 W；Δ 接 220 V／27.5 A／47.631397 A／18150 W，含「√3 在電壓 vs √3 在電流」的拆解與 Y–Δ 啟動說明 | **通過**（手算全部一致；`tr-zl=0` 走保護分支） |

### 8.2 全滑桿全選項掃描

`05`／`08`／`10`／`14` 四章的所有 range 推到 min 與 max、所有 select 切過每個選項、以及 ALL_MIN／ALL_MAX 組合，合計數十組——**`NaN`／`Infinity`／`undefined`／`null`／空白輸出 洩漏數 = 0**。

### 8.3 模式相依控制的行為確認（非缺陷）

瀏覽器實測發現三章有「改滑桿但 output 不變」的情形，逐一追查後確認**全部是規格設計的模式相依控制**，不是失效：

| 章 | 控制 | 行為 | 判定 |
| --- | --- | --- | --- |
| 06 | `sto-c` | `cap` 模式會變、`ind` 模式不變 | 符合規格（C 只在電容模式使用） |
| 06 | `sto-l` | `ind` 模式會變、`cap` 模式不變 | 符合規格 |
| 07 | `rc-c` / `rc-l` | 各自只在 RC／RL 模式生效 | 符合規格（標籤已寫明「只在 RC 模式使用」） |
| 14 | `tr-n1` / `tr-n2` | `xfmr` 模式會變、`wye`／`delta` 模式不變 | 符合規格（三相模式不用匝比） |

---

## 9. 自我檢核行為抽測

| 情境 | 操作 | 結果 | 判定 |
| --- | --- | --- | --- |
| 數值題答對 | `q11-1` = 3097.6 → 對答案 | 「**答對**：\|Z\| = 12.5 Ω、I = 220/12.5 = 17.6 A、S = 3872 VA、pf = 0.8，所以 P = 3872 × 0.8 = 3097.6 W，也等於 I²R。　回頭複習：**11 交流功率與功因**（連結）」；進度 1/45 | 通過 |
| 單選題答對 | `q02-1` = b → 對答案 | 「**答對**：KCL 說節點流入電流的代數和為零，根據是電荷守恆——理想節點不囤積電荷。　回頭複習：02 KCL與KVL」；進度 2/45 | 通過 |
| 空白作答 | `q05-1` 留空 → 對答案 | 「先填一個數字再對答案。」，進度不變、不阻擋 | 通過 |
| 數值題答錯 | `q07-2` = 100 → 對答案 | 「**再看一次**：正確答案是 63.2121。…　常見卡點：填 100，把時間常數當成『充飽的時間』…　建議回去看：07 一階暫態RC與RL」 | 通過 |
| 單選題答錯 | `q10-3` = a → 對答案 | 顯示正確答案、解釋、常見卡點、回章連結 | 通過（見 §14 觀察 4） |
| 容差邊界 | `q12-2` = −3.05（`tol` 0.05） | 判為答對 | 通過 |
| 非數字輸入 | `q12-2` = `abc` | 判為答錯並顯示正確答案，**不輸出 NaN** | 通過 |
| 全部清除 | `quiz-reset` | 45 題輸入值與回饋全清、進度回 0/45 | 通過 |
| 洩漏掃描 | 全部 45 個 `-output` | `NaN`／`Infinity`／`undefined`／`null` = **0** | 通過 |

無計分、無解鎖、無倒數、無強制答對，可無限重做，符合 BUILD-SPEC §0 第 8 條。

---

## 10. 教學品質（QUALITY-GATES 1、2 節 ＋ 硬體原理子工作流加驗）

### 10.1 逐章機械勾稽（15 章）

| 項目 | 結果 |
| --- | --- |
| 「先教後操作」：`section.workspace` 位於「數值實例」之後、「不要混淆」之前 | 15 章全部符合，順序無例外。**通過** |
| 比喻只出現一次且立刻收回（「比喻到此為止」） | 15 章全部有。**通過** |
| 「可觀察證據」段同時有「用什麼量」「能證明」「不能證明」 | 15 章全部齊備（`能證明` 與 `不能證明` 成對出現，最少各 1、最多 4）。**通過** |
| 量測儀器具名 | 三用電表／示波器／電流勾表／LCR 表／功率計／瓦特表／電流探棒／函數產生器／電能品質分析儀，逐章至少 1 種。**通過** |
| `p.prereq` 先修宣告 | 15 章全部有。**通過** |
| 工程數學先修相對連結 | 14 章有（章 02 無——該章確實沒有對應的數學先修章，§2.1 為條件性要求）。**通過** |
| 術語首現「繁中（English, 縮寫）」 | 逐章抽出約 60 條，格式一致（例：`克希荷夫電流定律（Kirchhoff's Current Law, KCL）`、`集總元件假設（lumped element assumption）`、`有效值（root-mean-square, rms）`、`功率因數（power factor, pf）`）。無未解釋的英文縮寫債。**通過** |

### 10.2 易混拆解（親自閱讀 5 章全文 ＋ 全課關鍵字勾稽）

| 易混對 | 覆蓋 | 樣本 |
| --- | --- | --- |
| 電壓 ≠ 電流 | 章 00、字典 | 「電壓是兩點間的位能差，不是某點持有的絕對量」「電流不會被負載消耗，理想穩態下流入與流出相等」 |
| 有效值 ≠ 峰值 ≠ 峰對峰值 | 章 09 及後續 8 檔 | 「10 V 峰值等於 20 V 峰對峰值，對弦波才等於 7.071068 V 有效值」「V<sub>rms</sub>=V<sub>m</sub>/√2 只對弦波成立；方波為 V<sub>m</sub>，三角波為 V<sub>m</sub>/√3」（重算 ✓） |
| 實功 ≠ 無效功率 ≠ 視在功率 | 章 11 | 「無效功率平均為零，並非已浪費掉的功率；它佔用電流並間接增加 I²R 線損」「W、var、VA 量綱相同但角色不同…不能直接相加」 |
| 相量 ≠ 向量 | 章 09、10、字典 | 「相量是複數編碼，不是空間中的幾何量」「阻抗 Z 是兩個相量的複數比值，不是相量本身」 |
| 直流電阻 ≠ 阻抗 | 章 10、字典 | 「LCR 表在 1 kHz 讀到的值與三用電表直流檔讀值邊界不同」 |
| 最大功率 ≠ 最高效率 | 章 05 | 「匹配點效率只有 50 %，而接近開路時效率趨近 100 %、功率卻趨近 0 W」 |
| RC 的 τ 正比 R ≠ RL 的 τ 反比 R | 章 07 | 明列為「最容易記反的一組」 |
| 電容連續的是電壓、電感連續的是電流 | 章 06、07 | 「連續的是電容**電壓**與電感**電流**，另外那兩個量可以跳」 |
| KCL（電荷守恆）≠ KVL（能量守恆） | 章 02 | 「兩者檢查的物理帳不同，不是把同一句定律換成電流版與電壓版」 |
| 集總 ≠ 元件很小 | 章 00、字典 | 「集總不是只看元件實體小；同一塊板在 1 kHz 可成立，在 2.4 GHz 可能失效」 |
| Y 接的 √3 在電壓 ≠ Δ 接的 √3 在電流 | 章 14、widget | 兩種模式各自寫出並互相對照 |

### 10.3 硬體加驗逐條

| 加驗條款 | 結果 |
| --- | --- |
| 不可先考後教（診斷／預測所需概念已在前文教過） | 抽查 02／05／07／10／11：workspace 用到的每個量都在前面推導段定義過。**通過** |
| 每節先答「為什麼」，指出狀態、回路、電流／電荷／能量去向與結果 | 「白話直覺」段皆走完一次 `電源與參考點 → 導通路徑／回路閉合 → 電荷與能量去向 → 節點電壓變化 → 結果` 因果鏈。**通過** |
| 術語首現可原地讀懂（白話動作、真實機制、可觀察現象、易混概念） | 字典 62 張卡每張固定四段「白話／位置角色／機制／不要混淆」；正文首現亦有白話與機制。**通過** |
| 類比後回到真實元件與適用限制 | 15 章「比喻到此為止」後接精確定義。**通過** |
| 公式附符號、單位、假設、量綱與極端案例 | 抽查 5 章全部齊備（例：章 07 (c) 步做 Ω×F=s、H/Ω=s 量綱檢查；章 05 (f) 步做 R<sub>L</sub>→0／→∞ 極端檢查）。**通過** |
| 自由實驗無計分、無強制答對；控制值改變時數值、現象與 why 同步改變 | 瀏覽器實測 15 章：初次載入 output 皆非空；改任一（該模式有效的）控制後 output 立即改變。每個 widget 都含「為什麼／因果句」與「邊界提醒」（`ohm` 依規格 §4 章 01 判準以「因為電壓一樣，i = v/R」「因為電流一樣，v = iR」等具名因果句呈現，未用「為什麼：」字樣，符合該章契約）。**通過** |
| 每個核心概念至少一種量測工具、能證明／不能證明 | 見 §10.1。**通過** |
| 三相安全警語 | 章 14 有明確警語：「市電三相是危險電壓…必須由合格人員使用額定量測設備、個人防護與正確隔離程序執行，學習者不可自行操作帶電配電盤」，並補上「斷電、上鎖掛牌、驗電、鉤表只夾單一導線」等具體邊界。**通過** |

### 10.4 學習有效性（QUALITY-GATES §2）

- 取捨判斷：章 05「要功率還是要效率」、章 13「選頻銳利 vs 振鈴久」、章 11「補到 1.00 反而容易補過頭」。**有**
- 參數實驗：15 個自由實驗，每章一個。**有**
- 故障診斷：章 02「殘差不為零代表哪一本帳不平」、章 07「尾巴出現第二個時間常數代表有寄生元件」、章 10「量到相位不符先查 µF/F、mH/H 換算」。**有**
- 跨層整合：章 08 ↔ 13 的 Q = 1/(2ζ) 雙向縫合、章 05 的 R<sub>th</sub> 直接餵給章 07 的 τ、章 10 的相位餵給章 11 的功因。**有**
- 可重設、進度不阻擋：`quiz-reset` 可全清，進度僅顯示「僅供參考，不影響瀏覽」。**有**

---

## 11. 字元級錯字掃描

| 項目 | 結果 |
| --- | --- |
| 簡體字（OpenCC 3980 組簡繁字表全字集掃描；另以「本課出現但既有已驗收繁體語料 1567 字集未見」的新字反查交叉驗證） | **18 HTML ＋ app.js ＝ 0 筆。** 反查得到的 36 個新字（岔耐囤怕盡削贏扼貨鉤鉗壤彷彿巡草稀桶犯宰餵抹攜迅宅款味銘市年暗灰踩敢灌枚）逐一確認全為合法繁體。`BUILD-SPEC.md` L1258–1259 有 12 個簡體字，是規格自己的 QC 清單在列舉「要掃描什麼」，屬 meta 非內容。**通過** |
| 形近錯字 | 戴維寧 50／克希荷夫 14／諾頓 11／阻尼 110／暫態 77／穩態 88／共振 112／耦合 30／變壓器 94／功因 71／匝比 37／阻抗 181／收斂 2／導納 11／電導 26／頻寬 40 —— 對應誤詞（戴維南、載維寧、克希何夫、克希霍夫、諾敦、阻泥、暫熊、穩熊、偕振、共震、諧震、偶合、藕合、變壓氣、功茵、幣比、組抗、阻杭、收旂、導鈉、電道、頻寛）**全部 0 筆**。**通過** |
| 「相量」誤植「向量」 | 全課 13 筆「向量」逐筆核對：11 筆合法（`‖` 不是向量範數、跨課連結「向量與矩陣」、規則說明句本身）；**2 筆為用詞不一致**（見 §14 觀察 2）。 |
| µ 符號 | U+00B5（正確）109 處；U+03BC（錯誤）在 18 HTML ＋ app.js 中 **0 處**（`BUILD-SPEC.md` 的 3 處是規則說明時舉例）。**通過** |
| Ω 符號 | U+03A9（正確）258 處；U+2126（OHM SIGN，錯誤）**0 處**。**通過** |
| 負號 | U+2212 共 473 處；正文公式中的 ASCII 減號 **0 筆**（用標籤內／外狀態機掃描，先前抓到的候選全為 `<input min="-5">` 一類屬性值與 `q07-2`、`06-電路學-上.md` 一類識別碼，非公式）。`app.js` 字面輸出字串中 ASCII 減號 0 筆（`fmt()` 統一 `.replace("-","−")`）。**通過** |
| 全形英數字（U+FF10–FF19／FF21–FF3A／FF41–FF5A） | **0 筆**。全形字元只有中文標點（，。；：（）｜／＝？）。**通過** |
| 佔位符（TODO／TBD／FIXME／XXX／待補／Lorem／連續三個以上 ASCII 句點） | 18 HTML ＋ app.js **0 筆**。**通過** |

---

## 12. 拆檔量測（固定項）

參考線：HTML 1000 行、`app.js` 2000 行、spec 類 md 1200 行。

| 檔案 | 行數 | 位元組 | 對參考線 |
| --- | ---: | ---: | --- |
| `index.html` | 37 | 6 034 | 遠低於 |
| `00-電路世界觀.html` | 70 | 11 704 | 遠低於 |
| `01-電阻與歐姆定律.html` | 81 | 12 867 | 遠低於 |
| `02-KCL與KVL.html` | 72 | 12 299 | 遠低於 |
| `03-節點與網目分析.html` | 75 | 13 285 | 遠低於 |
| `04-疊加原理與相依源.html` | 82 | 13 115 | 遠低於 |
| `05-戴維寧與最大功率.html` | 86 | 14 070 | 遠低於 |
| `06-電容與電感.html` | 78 | 12 446 | 遠低於 |
| `07-一階暫態RC與RL.html` | 85 | 13 911 | 遠低於 |
| `08-二階RLC暫態.html` | 79 | 13 746 | 遠低於 |
| `09-弦波與相量.html` | 81 | 13 258 | 遠低於 |
| `10-阻抗與交流穩態.html` | 86 | 13 684 | 遠低於 |
| `11-交流功率與功因.html` | 93 | 13 239 | 遠低於 |
| `12-一階濾波器與頻率響應.html` | 80 | 13 882 | 遠低於 |
| `13-RLC共振與品質因數.html` | 92 | 14 264 | 遠低於 |
| `14-變壓器與三相.html` | 85 | 14 616 | 遠低於 |
| `名詞與概念字典.html` | 95 | 37 457 | 遠低於 |
| `自我檢核.html` | 104 | 20 585 | 遠低於 |
| `app.js` | 858 | 69 762 | 低於 2000 行參考線 |
| `styles.css` | 15 | 4 141 | 正本複本，不適用 |
| `BUILD-SPEC.md` | **1369** | 173 484 | **超過 1200 行參考線 14 %** |
| `PROJECT-BRIEF.md` | 300 | 28 445 | 低於 |
| 產品檔合計（18 HTML＋js＋css） | 1 519 | 344 365 | — |

**說明**：HTML 每行平均約 165 bytes（段落整段一行的寫法），行數指標對本課偏低估，改看位元組亦全部遠低於同家族課程上限。`BUILD-SPEC.md` 1369 行為已知超線項，Lead 已裁決本課維持單檔，**此處僅記載，不建議拆檔**。

---

## 13. 瀏覽器實測（Playwright ＋ 系統既有 Chrome）

執行方式：`python -m http.server 8931` 起本機站，Playwright `chromium.launch({channel:'chrome', headless:true})`，桌面 `1440×900` 與手機 `390×844` 各載入全部 18 頁。

| 指標 | 桌面 1440×900 | 手機 390×844 |
| --- | --- | --- |
| HTTP 狀態 | 18 頁全部 200 | 18 頁全部 200 |
| `pageerror`（未捕捉例外） | 0 | 0 |
| `console.error` | **1**（僅 `index.html`，見下說明） | 0 |
| `console.warn` | 0 | 0 |
| 外部網域請求 | **0** | **0** |
| 失敗請求（requestfailed） | 0 | 0 |
| 橫向溢位（`scrollWidth > clientWidth`） | **0 頁**（18 頁皆 scrollW = clientW = 1440） | **0 頁**（皆 390） |
| 超出視窗寬度的元素 | 0 | 0 |
| widget output 初次載入即有內容 | 15/15 章 | 15/15 章 |
| output 中的 `NaN`／`Infinity`／`undefined` | 0 | 0 |

**唯一的 console.error 說明**：`index.html` 出現一筆 `Failed to load resource: 404`。查本機伺服器 log 確認為 `GET /favicon.ico 404`——**瀏覽器自動發出的同源請求，不是課程頁面撰寫的資源**，且同家族既有課程亦無 favicon。**非產品缺陷**；後續 18 頁因瀏覽器快取 404 而不再出現。

補充互動與可及性實測（桌面）：

| 項目 | 結果 |
| --- | --- |
| 改控制值後 output 立即改變 | 15 章、63 個控制逐一驅動；除 §8.3 列出的 4 個模式相依控制外，全部即時改變。**通過** |
| 每章 `section.workspace` 數量 | 15 章各 1。**通過** |
| 所有 workspace 控制都有 `<label>` | 缺 label 的控制 = **0**。**通過** |
| `output` 的 `aria-live` | 15 章皆為 `polite`。**通過** |
| 可聚焦元素數（Tab 可達） | 每章 13–18 個（含 topbar、主站連結、控制項、pager、footer）。`styles.css` 已提供 `:focus-visible` 與 `prefers-reduced-motion`（正本檔驗證）。**通過** |
| 字典搜尋 | 初始「顯示 62 個條目」→ 搜「阻抗」顯示 12（可見卡實測 12 張）→ 搜不存在字串顯示 0 → 清空回復 62。**通過** |

`自我檢核.html` 的 45 個 `-output` 在初次載入時為空白——**這是設計如此**（按下「對答案」才顯示回饋），BUILD-SPEC §8.5 第 31 項的「初次載入 output 不得留白」針對的是互動元件（widget），不含題目回饋區。**非缺陷。**

---

## 14. 問題清單

**阻斷級（必須修才能交付）：0 項。**

以下 6 項為低嚴重度觀察，不影響正確性、可用性或任何硬性關卡，**列出供 Lead 裁決**：

### 觀察 1｜章 12 出現一次「瞬態」，與全課統一用語「暫態」不一致

- **檔名行號**：`12-一階濾波器與頻率響應.html:72`
- **原文**：「…低通會呈充放電曲線，高通則在邊緣後回到基準。這能支持同一時間常數控制**瞬態**，卻不能用單次方波證明整段頻率響應…」
- **重現**：`grep -n "瞬態" *.html app.js` → 僅此 1 筆；同資料夾「暫態」共 77 筆。
- **影響**：用語不一致（「瞬態」為對岸慣用；本課與來源筆記統一用「暫態」）。純字串修正，Lead 可直接 sed（`瞬態` → `暫態`），不需回派 builder。

### 觀察 2｜`app.js` 相量工具兩處寫「零向量」，與章 09 自己的「相量 ≠ 向量」規則不一致（**規格源頭亦同**）

- **產品**：`app.js:428`「極式：大小 0.000000，角度 **未定義（零向量沒有方向）**」；`app.js:443`「邊界提醒：兩個振幅都是 0，和是**零向量**，角度沒有意義（不是 0°，是未定義）」
- **規格**：`BUILD-SPEC.md:699` 明文要求「角度必須顯示為『未定義（零向量沒有方向）』」；`BUILD-SPEC.md:681` 亦寫「合成**向量**幾乎躺在實軸上」
- **對照**：`09-弦波與相量.html:42` 的正文卻寫「合成**相量**幾乎落在實軸方向」，且同章「不要混淆」第 1 條明列「相量是複數編碼，**不是空間中的幾何量**」
- **重現**：`grep -n "向量" app.js BUILD-SPEC.md 09-弦波與相量.html`
- **判讀**：builder 在 HTML 已自行把規格的「向量」改成「相量」（正確判斷），但 `app.js` 忠實照抄了規格的字面要求。**問題源頭在規格。** 建議 Lead 一併把 `app.js:428/443` 與 `BUILD-SPEC.md:681/699` 的「向量」改為「相量」（純字串修正）。

### 觀察 3｜`transformer` widget 在三相模式仍附「變壓器靠共用磁通」的因果句（規格所要求）

- **檔名行號**：`app.js:721`（`rows.push` 在 `if/else` 之外，無條件附加）；規格來源 `BUILD-SPEC.md` 章 14 契約判準 ⑦「**每次都附因果句**：變壓器靠的是共用的磁通…」
- **重現**：切 `tr-mode` 到 `wye` 或 `delta`，輸出末段仍出現變壓器磁通說明。
- **影響**：三相模式下該因果句離題（三相的因果應是「120° 相差使瞬時功率為常數」之類）。**符合規格，非 builder 缺失**；若要改需先改規格。

### 觀察 4｜自我檢核單選題答錯時，顯示的正確答案是選項代號而非選項文字

- **檔名行號**：`app.js:839`　`const shownAns=q.t==='num'?trimZeros(fmt(q.ans,6)):q.ans;`
- **重現**：`q10-3` 選 a → 回饋顯示「正確答案是 **b**。」而非「正確答案是 **容性**」。
- **緩解**：緊接著的 `why` 文字已把正確概念完整說明（「X = X<sub>L</sub> − X<sub>C</sub> < 0 代表電容贏了，電路呈容性…」），讀者不會被卡住。
- **影響**：純體驗層，不影響學習正確性。13 個單選題皆同。

### 觀察 5｜課程目錄缺 `README.md`（頁面地圖）

- QUALITY-GATES「零基礎課程加驗」列有「`README.md` 記錄頁面地圖」。本課目錄有 `PROJECT-BRIEF.md` 與 `BUILD-SPEC.md`，無 `README.md`。
- **對照**：試點課 `共通基礎/數學/互動課程/` 同樣沒有 `README.md`（只有 `專題/機器人運動學/互動課程/` 有）；且 `BUILD-SPEC.md` §1 的檔案清單本就未列 `README.md`。
- **緩解**：`index.html` 已完整承擔頁面地圖責任（17 張章卡 ＋ flow ＋ 先修與去向 ＋ 來源）。
- **影響**：與試點課一致的已知落差，非本課退步。是否補寫由 Lead 裁決。

### 觀察 6｜掛站接線尚未執行（屬 Lead 收尾範圍，非驗收失敗）

驗收當下的狀態：

| 項目 | 狀態 |
| --- | --- |
| `.github/workflows/pages.yml` 的 `copy_course '…電路學-互動課程' circuit-analysis` | **未加**（`grep circuit-analysis` 無結果） |
| `pages.yml` 的 `replacements` 鍵 `circuit-analysis`（本課有 `../../../共通基礎/數學/…` 與 `../../../專題/IoT…` 兩種跨課相對連結，**需要**此規則） | **未加** |
| `互動學習網站/index.html` 主站入口 | **未加**（僅有工程數學先修那一列） |
| `電機/README.md`／`電機/02-電機核心` 入口連結 | **未加** |
| `tools/sync-styles.py` 的 `FAMILY` 登記 | **未加**（工具檔第 50–53 行已註明「交付後由 Lead 加入」） |
| `wf/SESSION-LOG.md` | 已有本輪修改（`git status` 顯示 M） |

以上全部落在 BUILD-SPEC §9「Lead 收尾工作」，**不計為 builder 或本次驗收的失敗項**，但**在完成前本課無法從主站瀏覽**，交付前請 Lead 補齊。

---

## 15. 未執行項目

**無。** BUILD-SPEC §8 的 34 個檢查點與本次任務指定的 8 大項全部實際執行並附證據：node 可用（不需走瀏覽器替代條款）、Playwright + 系統 Chrome 可用（桌面與手機兩尺寸皆完成）、`sync-styles.py` 可用。

---

## 16. 交付結論

> **允許交付。**

理由：

1. 硬性關卡（編碼、CRLF、零 LaTeX、零外部資源、id 契約、`styles.css` 正本位元組相同、`sync-styles.py --check` exit 0、pager 相連、連結完整、字數門檻、console error、外部請求、橫向溢位、NaN 洩漏）**全數通過，0 失敗**。
2. 內容正確性抽查 5 章、規格數值 8 組必驗 ＋ 3 組加驗 ＋ 其餘全章補算，**驗收員親手重算後 0 筆算術錯誤**——規格與產品皆無誤，198 個規格數值原樣進入產品且無遺漏。試點課的「規格算術筆誤被照抄」風險本次未重演。
3. 字元級錯字掃描（簡體、形近、單位符號、負號、全形、佔位符）**0 筆真問題**，僅 2 筆用詞不一致。
4. 教學品質關卡（含硬體原理加驗 7 條）全數滿足，15 章七段骨架、比喻收回、量測儀器「能證明／不能證明」、易混拆解、三相安全警語齊備。

交付前建議 Lead 處理：

- **必須**：完成觀察 6 的掛站接線（`pages.yml` 課程表一行 ＋ `replacements` 鍵 ＋ 主站入口 ＋ `電機` 來源入口 ＋ `sync-styles.py` `FAMILY` 登記），否則本課無法從主站瀏覽。
- **建議（純字串，sed 級）**：觀察 1 的「瞬態→暫態」；觀察 2 的「向量→相量」（`app.js:428/443` 與 `BUILD-SPEC.md:681/699` 一併改）。
- **可選**：觀察 3（需先改規格）、觀察 4、觀察 5。
