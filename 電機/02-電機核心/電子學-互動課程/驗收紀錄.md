# 驗收紀錄 — 電子學（零基礎互動課）

← [PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜[BUILD-SPEC.md](BUILD-SPEC.md)｜[品質關卡](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)｜[build-with-codex](../../../wf/workflows/interactive-study-site/BUILD-WITH-CODEX.md)

- **測試日期**：2026-08-28
- **驗收員**：獨立驗收 agent（未參與建置；不採信 builder 自報，全部重跑）
- **受驗品**：`電機/02-電機核心/電子學-互動課程/` 19 HTML ＋ `app.js` ＋ `styles.css`；規格 `BUILD-SPEC.md`（1399 行）＋ `PROJECT-BRIEF.md`
- **環境**：Windows 11 Pro 26200；Python 3.13；node v24.11.1（`ms-playwright-go/1.57.0/node.exe`）；Playwright（npx cache `e41f203b7505f1fb`）＋ `channel:'chrome'`；html-validate（npx cache `0056d06b4c1ac6f7`）；`python -m http.server 8971`
- **驗收員唯一寫入檔**：本檔。未修改任何產品檔或規格檔。
- **結論**：**允許交付**（無 P1；2 項 P2 建議、4 項 S 備查，均不影響可用性）

---

## 1. 驗證矩陣

| # | 項目 | 方法 | 證據量 | 結果 |
| --- | --- | --- | --- | --- |
| 1 | `node --check app.js` | node v24.11.1 | 1 檔 | **PASS** |
| 2 | 檔案清單完整性 | 目錄列舉 vs spec §1 | 19 HTML＋app.js＋styles.css | **PASS** |
| 3 | styles.css 正本閘門 | `sync-styles.py --check` | exit 0；家族 8 門全相同 | **PASS** |
| 4 | styles.css 位元組相同 | sha256 vs 正本 | 4141 B，`894f24a1…`，CR 數 0（LF） | **PASS** |
| 5 | id 契約總數 | 全 HTML `id=` 統計 | **240**（76 控制＋16 output＋2 字典＋2 quiz 全域＋144 題目相關），distinct 240，同檔零重複 | **PASS** |
| 6 | id 契約雙向比對 | spec §4／§7.2 ↔ HTML ↔ app.js | 0 缺、0 多、0 未被 app.js 參照 | **PASS** |
| 7 | `range` 屬性逐字比對 | min/max/step/value vs spec §4 | 76 控制中 65 個 range，0 差異 | **PASS** |
| 8 | `select` option value／順序／預設 | vs spec §4 | 13 章節 select＋16 題 select，0 差異 | **PASS** |
| 9 | app.js 監聽清單 vs spec §7.2 | 逐函式解析 | 17 列（16 widget＋dictionary），守衛 id／output id／監聽序列 0 差異 | **PASS** |
| 10 | LaTeX 分隔符 | 19 HTML＋app.js 子字串計數 | `$$ \( \) \[ \] \begin{ \frac` 全 0；app.js 55 個 `$` 全為 `$(` 別名呼叫 | **PASS** |
| 11 | 外部資源 | href/src/@import/fetch/XHR 掃描 | 唯一外部 URL＝主站連結，19 檔各 1 次；`fetch`／`XMLHttpRequest`／CDN／`@import` 全 0 | **PASS** |
| 12 | 相對連結存在性 | 逐一 resolve | 44 個相異目標全部存在（含三種跨課路徑） | **PASS** |
| 13 | UTF-8 無 BOM | 位元組級 | 23 檔全部無 BOM、全部可解碼 | **PASS** |
| 14 | 壞損字元掃描 | U+FFFD／U+0080–009F／U+E000–F8FF／U+03BC／U+2126 | 產品檔全 **0**（spec 3 處 U+03BC 為規則自述，見 S-3） | **PASS** |
| 15 | CRLF | 位元組級 | 19 HTML＋app.js 全 CRLF、0 lone LF；styles.css LF（正確，不轉換） | **PASS** |
| 16 | pager 相連 | 19 頁環狀比對 vs spec §2.2 | 0 錯（index→00→…→15→字典→自我檢核→index） | **PASS** |
| 17 | 無孤兒頁 | index chapter-card | 18 張，涵蓋全部 18 目標；number 序列 00–15＋查＋檢 | **PASS** |
| 18 | 教學骨架七段 | 每章 h2 比對 | 16 章全齊；`section.workspace` 各恰 1 個 | **PASS** |
| 19 | 安全警語 | 04／05／15 章 | 各 1 個 `p.warning` | **PASS** |
| 20 | 字數門檻 2000–3000 | 漢字＋全形標點，扣 workspace | 16 章全部落在區間（2021–2607） | **PASS** |
| 21 | 可觀察證據三件事 | 逐章勾稽 | 16 章皆有「儀器／能證明／不能證明」 | **PASS** |
| 22 | class 白名單 | 全檔 class 抽取 | 使用 20 個 class，全在白名單；`<style>` 0、inline `style=` 0 | **PASS** |
| 23 | 簡體字掃描 | **Big5 可編碼性**（1075 個相異漢字全測） | 非 Big5 字元 **0** ＝ 零簡體字 | **PASS** |
| 24 | 非台灣用語 | 22 詞，含 BUILD-SPEC §8.3-19 全表（反饋／噪聲／柵極／漏極／三極管／場效應管，及該表列出的簡體形基極區、集極、射極、耗盡區等） | **0 命中**；正確台灣用語計數見 §3 | **PASS** |
| 25 | 形近錯字 | spec §8.3-19 全表＋自訂 40 詞 | **0 真命中**（4 筆為「共源級／共射級／共閘級」誤報） | **PASS** |
| 26 | 「飽和」語義 | 06／08／11／13 章＋app.js＋字典＋自我檢核逐處 | 51 處全部正確，0 寫反；BJT↔MOSFET 對照句 6 處明寫「意思相反」 | **PASS** |
| 27 | 負號 U+2212 | 剝標籤後掃 ASCII `-數字` | 0 筆 | **PASS** |
| 28 | 無 `.md` 連結 | 全 HTML | 0 筆 | **PASS** |
| 29 | 來源標註 | 16 章 vs PROJECT-BRIEF §6 | 16/16 逐字相符 | **PASS** |
| 30 | **規格數值獨立重算** | 自寫 Python，第一原理 | **245 項**，實質全數相符（3 筆為捨入／spec 敘述省略，見 §4） | **PASS** |
| 31 | 規格→產品轉寫 | spec §4 粗體值 ↔ 章節 HTML | 250 值中 **241 命中**；9 筆為次要欄位省略（S-2） | **PASS** |
| 32 | widget 預設值 vs spec §8.4 | 假 DOM 實跑 16 widget | 要求值 **81/81 全數出現** | **PASS** |
| 33 | §8.4 邊界子案例 | 假 DOM 實跑 | 14/14 全部產生規定守衛文字、無洩漏 | **PASS** |
| 34 | 自我檢核 48 題契約 | HTML 結構＋app.js 答案表 | 48 題／48 check／48 output／48 label；答案與容差 **0 差異** | **PASS** |
| 35 | §6.3 選項措辭逐字 | 16 題 × 4 選項＝64 字串 | **0 差異**（builder 只抽查 6 題，本次補齊全部 16 題） | **PASS** |
| 36 | 字典契約 | 62 卡結構 | 62 卡；分組 10/12/14/12/14；四段齊全且順序正確；全部有 `data-search`；無重複標題 | **PASS** |
| 37 | widget NaN 邊界（可達範圍） | 假 DOM 全滑桿掃 min→max＋全選項＋極值組合 | **4008 案例，0 洩漏**，16/16 初載 output 非空 | **PASS** |
| 38 | 03 章牛頓法 | 對照自寫 bisection 解 | 11100 個 (V_s,R) 格點；最大 ΔV_D = **5.0×10⁻⁷ V**（＝顯示位數極限）；殘差 8.67×10⁻¹⁹ A ＜ 1e−15；重繪確定性一致 | **PASS** |
| 39 | Playwright 桌面 1440×900 | 19 頁 | console error 0、warning 0、外部請求 0、橫向溢位 0 | **PASS** |
| 40 | Playwright 手機 390×844 | 19 頁 | 同上，全部 0 | **PASS** |
| 41 | focus 可見性 | 真實 Tab 巡覽 | 21 個可聚焦元素，**0 個無 focus ring**（3px solid #E49A25，offset 3px），雙 viewport 一致 | **PASS** |
| 42 | 鍵盤可及 | 自我檢核頁 | 121 個可聚焦元素，tabindex<0 為 0，隱藏 0，label 覆蓋 48/48 | **PASS** |
| 43 | 即時反應 | 16 widget 各改一個控制 | 16/16 output 立即改變且無洩漏 | **PASS** |
| 44 | 自我檢核互動實測 | Playwright | 空白提示、答對、答錯（無「錯誤／失敗／不及格」字眼、附來源連結）、容差邊界（0.5797 過／0.5799 不過）、進度 0→1→3、reset 全清歸零 | **PASS** |
| 45 | 字典搜尋實測 | Playwright | 初始「顯示 62 個條目」；飽和→10、miller→1、CMRR→1、跨導→3、zzzz→0、清空→62，term-count 與可見卡數恆等 | **PASS** |
| 46 | HTML 合法性（加驗） | html-validate recommended | 結構性錯誤 0（143 筆全為 spec 自訂風格：小寫 doctype、`label for`＋巢狀 input） | **PASS** |
| 47 | 掛站接線（供 Lead 確認） | `pages.yml` | 課程表 `…電子學-互動課程\|electronics` 已加；`replacements` 三條齊全且 slug 相符 | **PASS** |

**合計 47 項全數通過，0 項 FAIL。**

---

## 2. builder 宣稱事項的獨立覆核

| 宣稱 | 覆核結果 |
| --- | --- |
| 1. `自我檢核.html` 18 個 `<option>` 攤平（去除 `<sub>`/`<sup>`），value 未動、48 題契約全過 | **部分不實**：18 個 `<option>` 的 `<sub>` 標籤**仍在檔案中**（見 P2-1）。但 `value` 屬性確實未動（`,a,b,c,d`），48 題契約確實全過，且瀏覽器實測渲染結果與攤平後**完全相同**（`option.textContent` 已是純文字「np = ni²」「因為 R1、R2 很大」），因此**外顯行為無缺陷**。 |
| 2. 字典補完「前 36 張卡位元組未動」，PREFIX_BYTES=25382 | **無法由驗收員驗證**（需前一版做位元組比對，該版不存在於工作區）。可佐證的是：第 36 卡結束於位元組 25549、第 37 卡起於 25635，兩者間 86 B 為第四組 `<h2>` 與 `term-list` 開標籤——25382 與此區間量級一致，未見矛盾。字典結構本身完全合規（見矩陣 #36）。 |
| 3. §6.3 選項文字只抽查 6 題 | **已補齊**：16 題 × 4 選項 = 64 個字串逐字比對，**0 差異**。 |
| 4. builder 自行修正 spec 四處（435、471、591、931 行） | **改後值全部正確**：L471（30 V 邊界）獨立算得 I_Z = 47.878723 mA、P_Z = 244.181489 mW、達額定 97.672596 %，全數吻合；L435、L591、L931 為判讀句／邊界敘述修正，經比對與 §4 計算式一致，且該三處涉及的數值（半波 60 Hz／全波 120 Hz、穩定度 1.224894 與 V_CE = 10.460352 V、L = 0.01 與 A_f = 0.999999 及 f_H = 10000010 Hz）皆通過獨立重算。 |
| 5. 「跨導與 CMRR 兩卡缺式子」的舊審查報告 | **確認作廢**：定稿字典的跨導卡含 `g_m = I_C/V_T`、`2I_D/V_OV`、`√(2kI_D)`；CMRR 卡含 `CMRR = 2g_m R_oc` 與 dB 換算。兩卡皆完整。 |

---

## 3. 內容正確性抽驗（獨立重算，未抄 spec）

以自寫 Python 腳本從第一原理重算，常數固定 V_T = 0.025851999786435535 V、n_i = 1.5×10¹⁰ cm⁻³、µ_n = 1350、µ_p = 480、q = 1.602176634×10⁻¹⁹ C。**共 245 項檢查**，涵蓋 spec §4 全部 16 章的數值實例、對照表與邊界值。

抽章重算摘要（超過要求的 5 章，實際全 16 章都算）：

| 章 | 重算項數 | 代表值（驗收員算得＝spec） | 結果 |
| --- | --- | --- | --- |
| 00 | 15 | I₀ = 0.574755 mA、g = 22.232499 mA/V、r_d = 44.979198 Ω、線性誤差 −1.645808 %、ΔV=200 mV 時 −99.618515 % | 全符 |
| 01 | 14 | σ = 2.162938456 S/cm、ρ = 0.462334 Ω⋅cm、本徵 ρ = 227377.379762 Ω⋅cm、p/n 型 ρ 比 2.812500 | 全符 |
| 02 | 8 | V₀ = 0.752879 V、I_D = 5.747546 mA、decade = 59.526429 mV、800 mV/1 pA → 27504.800183 mA | 全符 |
| 03 | 17 | 牛頓法四組 (V_s,R) 的 I_D／V_D／恆壓降誤差／r_d 全符（含 1 V/10 kΩ → 0.042675 mA、−29.701254 %） | 全符 |
| 04 | 14 | ΔV = 0.187943 V、V_dc = 10.506028 V、漣波 1.773050 %；半波 0.400709 V／3.546099 % | 全符 |
| 05 | 12 | I_Z = 15.963830 mA、P_Z = 81.415532 mW；30 V 邊界 244.181489 mW（97.672596 %） | 全符 |
| 06–09 | 43 | α = 0.990099、V_TH = 2.105263 V、R_TH = 8245.614035 Ω、β 漂移 4.418546 %、r_o = 181818.181818 Ω、V_M = 1.650000 V、P = 21.780000 µW | 全符 |
| 10–12 | 51 | g_m = 38.681727 mA/V、r_π = 2585.199979 Ω、A₀ = 3868.172707；CE A_v = −193.408635；C_in = 206 pF、f_H = 386298.405563 Hz、疊接提升 29.428571 倍 | 全符 |
| 13–15 | 71 | CMRR = 19340.863536（85.729517 dB）、tanh 四組線性誤差、A_f = 99.900100、GBW 兩者皆 1000000 Hz、f₀ = 1591549.430919 Hz、LSB = 805.664062 µV | 全符 |

**來源筆記勾稽**：`07-電子學-上.md`（113 行）、`-中.md`（139 行）、`-下.md`（190 行）逐條比對，蕭克萊方程、`I_C = βI_B`／`α = β/(β+1)`、Early 效應、MOSFET 三極／飽和式、`g_m = I_C/V_T`／`√(2µ_nC_ox(W/L)I_D)`、`r_o = 1/(λI_D)`、CMRR 定義、`A_f = A/(1+Aβ_f)` 全部有出處，無憑空新增。

**台灣用語計數（正確形）**：回授 111、雜訊 24、閘極 53、汲極 37、電晶體 59、空乏區 24、摻雜 61、載子 165、電洞 41、跨導 30、疊接 32、共模拒斥比 7、轉換速率 8、佔空比 3、退化電阻 3、蕭克萊 7、齊納 69、蕭特基 5、巴克豪森 4、遷移率 21。

---

## 4. 缺陷清單

### P1（必修）— **無**

### P2（建議修正，不阻擋交付）

| # | 檔案 | 位置 | 現況 | 應為 | 影響 |
| --- | --- | --- | --- | --- | --- |
| P2-1 | `自我檢核.html` | 18 個 `<option>`（q01-1 a、q02-1 b、q03-1 d、q05-1 b、q06-1 a/d、q07-1 b/d、q08-1 b、q09-1 a、q10-1 a/b/c/d、q11-1 c、q12-1 a、q13-1 b、q14-1 b 之屬 sub 者） | `<option>` 內仍含 `<sub>` 標籤（如 `np = n<sub>i</sub>²`） | `<option>` 內容模型只允許純文字；應寫成 `np = ni²` | **視覺無影響**（瀏覽器實測 `textContent` 已是攤平字串，`<sub>` 在 `<option>` 內本就不渲染），但 markup 不合內容模型，且與 builder 自報不符。修法為純字串替換，Lead 可自行 sed。 |
| P2-2 | `app.js` | 全部 16 widget 的讀值處 | 未對讀入值做非有限性前置檢查，完全倚賴 `range` 的 min/max | spec §7.5 與 §4（05、14 章邊界情形）明文要求「仍要在計算前檢查，不得讓 NaN／Infinity 進入畫面」 | **實務上不可達**：Playwright 實測 `input[type=range]` 會把 `''`／`'abc'`／`'NaN'` 一律回報為中點、`0`／`-999` 夾到 min、`1e99` 夾到 max，故畫面永不會出現 NaN（4008 案例佐證）。屬防禦深度差距，非行為缺陷。 |

### S（可接受，備查）

| # | 位置 | 說明 |
| --- | --- | --- |
| S-1 | `BUILD-SPEC.md` L831 | 邊界例「`fq-rs=50000`、`fq-cgd=20`、`fq-av=500` → f_H = 316.097206 Hz」漏寫 `fq-cgs = 50`；以預設 C_gs = 5 pF 重算應為 **317.516096 Hz**，以 C_gs = 50 pF 才得 316.097206 Hz。**該數字未進入任何產品檔**（12 章 HTML／app.js 皆 0 命中），僅為規格敘述省略。 |
| S-2 | 12／13／14 章 HTML | spec §4 對照表的**次要欄位**未全數轉寫：12 章漏 C_in 欄（16／56／506 pF，但 f_H 與 GBW 四組全在）；13 章漏 1 mV 與 52 mV 的線性預測值（0.019341／1.005725，但真值與誤差 % 全在）；14 章漏三組誤差 %（−0.990099／−0.009999／−0.001000，但 Aβ_f／A_f／f_H 全在）。核心數字與判讀句完整，不影響教學。 |
| S-3 | `BUILD-SPEC.md` L167／171／1289 | 3 處 U+03BC，皆為**規則自述**（該三行的內容正是「微符號一律用 U+00B5、不得混入 U+03BC」這條禁令本身，必須引用該字元才能寫得清楚）。產品檔 U+03BC 為 0。非缺陷。 |
| S-4 | `BUILD-SPEC.md` | 1399 行、208949 B，**超過工作流參考線 1200 行**（本規格 §8.3-20 自訂為 1400 行）。列報供 Lead 裁決是否拆檔。 |

**已確認「不是缺陷、不回派」**：`59.526429 mV`／`25.851999786 mV` 的高位數精度係 PROJECT-BRIEF §6 錨定值與 BUILD-SPEC §3.1 明文規定，已逐處確認一致。

---

## 5. 拆檔量測（固定項）

參考線：HTML 1000 行、`app.js` 2000 行、spec 類 md 1200 行（工作流）／1400 行（本規格 §8.3-20）。

| 檔 | 行數 | 位元組 | 超參考線 |
| --- | --- | --- | --- |
| `00-電子學世界觀.html` | 70 | 12539 | — |
| `01-半導體與載子.html` | 68 | 12817 | — |
| `02-PN接面與二極體方程.html` | 70 | 12202 | — |
| `03-二極體工作點與模型.html` | 72 | 12616 | — |
| `04-整流與濾波.html` | 70 | 11183 | — |
| `05-穩壓限幅與特殊二極體.html` | 77 | 12899 | — |
| `06-BJT原理與工作區.html` | 70 | 12052 | — |
| `07-BJT偏壓與Q點.html` | 81 | 14046 | — |
| `08-MOSFET原理與三個工作區.html` | 71 | 12393 | — |
| `09-CMOS反相器與數位功耗.html` | 74 | 12435 | — |
| `10-小訊號模型.html` | 72 | 12528 | — |
| `11-單級放大器三大組態.html` | 70 | 12849 | — |
| `12-米勒效應與頻寬.html` | 71 | 12876 | — |
| `13-差動對與電流鏡.html` | 70 | 11801 | — |
| `14-回授與穩定性.html` | 70 | 11864 | — |
| `15-運算放大器與電子系統.html` | 75 | 12845 | — |
| `index.html` | 38 | 6577 | — |
| `名詞與概念字典.html` | 94 | 46236 | — |
| `自我檢核.html` | 110 | 23931 | — |
| `app.js` | 1254 | 84304 | — |
| `styles.css` | 14 | 4141 | —（正本複本，LF） |
| `BUILD-SPEC.md` | **1399** | 208949 | **超 1200 行參考線**（見 S-4） |
| `PROJECT-BRIEF.md` | 331 | 35887 | — |

**注意**：HTML 行數偏低是因為本課採「一段一行」寫法（每行可達 1–2 KB），行數不是良好的體積代理；以位元組看，最大產品檔為 `名詞與概念字典.html` 46 KB，仍屬合理範圍。全部產品檔均未超參考線，**唯一超標者是規格檔 `BUILD-SPEC.md`**。

---

## 6. 已知限制

1. builder 宣稱的「字典前 36 卡位元組未動」無法由本次驗收獨立驗證（缺少前一版），僅能確認終稿結構完全合規。
2. Playwright 實測使用 `channel:'chrome'`（單一引擎）；未跨 Firefox／Safari 驗證。
3. 字數計法為「漢字＋全形標點」，與 spec 及工作流一致；半形英數與符號不計入。
4. 簡體字檢測以「Big5 可編碼性」為判準（1075 個相異漢字全數可編碼）；此法對極少數不在 Big5 的正體字會誤報，本次無誤報發生。
5. widget 邊界測試以假 DOM 驅動 4008 個案例＋Playwright 真實瀏覽器抽測；未做全 76 控制的完全笛卡兒積（組合數過大），改以「單控制全掃 ＋ 全控制極值組合」覆蓋。

---

## 7. 是否允許交付

**允許交付。**

- P1 缺陷 **0** 件。
- P2 兩件皆不影響使用者可見行為（P2-1 渲染結果與攤平相同；P2-2 的不安全路徑經瀏覽器實測不可達），屬純字串／防禦深度層級，Lead 可自行 sed 或列為後續改善。
- S 四件為規格側敘述與次要欄位省略，不涉及產品正確性。
- 掛站接線（`pages.yml` 課程表一行 ＋ 三條 `replacements`）已就位並經比對正確，可直接發布。
