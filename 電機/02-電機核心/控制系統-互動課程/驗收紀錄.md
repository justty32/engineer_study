# 驗收紀錄 — 控制系統（零基礎互動課）

← 課程首頁：[index.html](index.html)｜建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)｜立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜品質關卡：[QUALITY-GATES.md](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)

**本檔為獨立驗收員（未參與建置）的複驗紀錄，不採信任何建置端自報，全部項目親手重驗。** 臨時腳本存於
`/tmp/claude-1000/-home-lorkhan-repo-engineer-study/0c7d0627-5cec-4c9c-b89c-164f188f64ba/scratchpad/team-control-systems/verify/`（下稱 `verify/`），本檔引用的「證據量」皆可在該目錄的對應檔案中重現。

- 測試日期：2026-09-18
- 環境：Linux 6.18.49-1-MANJARO；Python 3.14.7（標準庫，無 numpy）；Node v24.14.1；Firefox 155.0.1（headless，無 Playwright／Chrome）
- 工作樹規則：本次驗收僅新增本檔，未修改 `app.js`、任何 HTML 或規格檔，未執行 `git add`／`commit`／`stash`／`checkout`。

---

## 1. 驗證矩陣

| # | 項目 | 結果 | 證據量 |
| --- | --- | --- | --- |
| 1 | 檔案齊全（15 HTML＋app.js＋styles.css） | **通過** | `ls` 確認 17 個檔名與 BUILD-SPEC §1 逐字相同，缺檔 0 |
| 2 | `styles.css` 與正本位元組相同 | **通過** | `cmp` 對 `專題/機器人運動學/互動課程/styles.css`：無差異輸出（IDENTICAL） |
| 3 | `node --check app.js` | **通過** | exit 0，`verify/gate6_syntax.txt` |
| 4 | QUALITY-GATES 片段 1：app.js 用到的 id 都在 HTML 內 | **通過** | 110 個真實 id 候選（濾除 `data-search`／`leading-zero`／`term-card` 三個非 id 字串後）全部命中，缺 0；`verify/gate1_ids_v2.txt` |
| 5 | QUALITY-GATES 片段 2：外部資源僅主站連結 | **通過** | 全課 15 次 `https://justty32.github.io/engineer_study/`，每頁恰 1 次；非主站外部連結 0；`verify/gate2_external.txt` |
| 6 | QUALITY-GATES 片段 3：零 LaTeX | **通過** | `\(`\)`\[`\]`\$\$` 命中檔案數 0；`verify/gate3_latex.txt` |
| 7 | QUALITY-GATES 片段 4：編碼／BOM／控制碼／私用區 | **通過** | 18 個檔 `iconv -f UTF-8 -t UTF-8` 全過；U+FFFD／U+0080–009F／U+E000–F8FF 命中檔案數 0；`verify/gate4_encoding.txt` |
| 8 | QUALITY-GATES 片段 5：相對連結存在 | **通過** | 課內連結與跨課連結（`../信號與系統-互動課程/*`、`../電路學-互動課程/*`，共 11 個相異目標）全部存在，broken 0；`verify/gate5_links.txt`、`verify/gate13_crosslinks.txt` |
| 9 | QUALITY-GATES 片段 6：`wc -l` | **通過（附規模）** | 12 章 69–86 行、`app.js` 673 行、字典 408 行、自我檢核 86 行、index 36 行；`verify/gate6_syntax.txt` |
| 10 | 182 個 id 逐一 `grep -c` 恰好 1 | **通過** | 58 控制＋12 output＋2 字典＋2 quiz 全域＋108 題目 id＝182，逐一比對，全部恰為 1，失敗 0；`verify/check_ids.py`、`verify/gate_b4_ids182.txt` |
| 11 | 8 個 `select` 的 option value／順序／文字 | **通過** | `bk-config`、`se-type`、`se-input`、`rh-order`、`bd-w`、`pd-preset`、`ld-type`、`ss-preset` 逐字比對 BUILD-SPEC §4，全部相符；`verify/gate5_selects.txt` |
| 12 | 自我檢核 12 個 `select`（q00-3…q11-3） | **通過** | 12 題 value=a/b/c/d 選項文字逐字比對 §6.3，全部相符；`verify/gate5_selects.txt` |
| 13 | 35 個 `range`／15 個 `number` 的 min／max／step／value | **通過** | 逐一比對 BUILD-SPEC §4 各章 widget 契約表，50 個控制全部相符，不符 0；`verify/gate6_ranges.txt` |
| 14 | `<option>` 內無標籤 | **通過** | `grep -n '<option[^>]*>[^<]*<'` 命中僅 `</option>` 自身，異常 0；`verify/gate8_option.txt` |
| 15 | 禁用詞配額（6 詞恰 1、14 詞恰 0、`&#` 實體 0、「信號」僅在信號與系統） | **通過** | `html.unescape` 後逐詞計數：反饋／傳遞函數／階躍／超調／伯德圖／工廠 各 1 次且全在 `名詞與概念字典.html` 的「不要混淆」欄；過衝等 14 詞全 0；`&#` 全課 0；「信號」50 次全部出現在「信號與系統」課名或連結內；`verify/gate9_quota.txt` |
| 16 | 高頻術語 grep（回授…配置 17 詞） | **通過（無異常樣本）** | 17 詞在全課均為非零且量級合理（如「極點」334 次），逐詞計數見 `verify/gate10_terms.txt`；未發現形近誤字 |
| 17 | 簡體字差集掃描 | **通過（無簡體字）** | 以「信號與系統」36 檔＋「微處理機與匯流排」語料做全字元差集，本課獨有漢字 67 個，逐一判讀皆為合法繁體字（如 森／泵／閥／輔／窺），非簡體形；另以常見簡體字清單直接掃描亦僅命中「只」（繁簡同形，非誤用）；`verify/gate10b_simplified.txt`、`verify/gate10c_simplified_direct.txt` |
| 18 | U+03BC／U+2126／ASCII `*` 當乘號 | **通過** | 三者在 HTML 全部 0；`app.js` 內的 `*` 為程式運算子，依規格不計 |
| 19 | 12 章 `p.prereq` 含「前置：」 | **通過** | 12/12 章各恰 1 個 `class="prereq"` 段落且含「前置：」；`verify/gate12_prereq.txt` |
| 20 | 09 章 `p.warning` | **通過** | 09 章恰 1 個，其餘 11 章 0 個；`verify/gate15_warning.txt` |
| 21 | 每章恰一個 `section.workspace` | **通過** | 12/12 章各 1 個；`verify/gate15_workspace.txt` |
| 22 | 七段 `h2` 前綴＋順序 | **通過** | 12 章逐一列出 h2 文字，痛點／白話直覺／推導／數值實例／（workspace 自有標題）／不要混淆／可觀察證據／連到哪門課，順序與前綴全部相符；`verify/gate15_h2.txt` |
| 23 | 章節正文字數（去 nav 與 workspace，漢字＋全形標點，1800–2600） | **12 通過**（10、11 章於 2026-09-18 修正後複驗通過） | 見下表；`verify/gate16_wordcount.txt` |
| 24 | 字典：54 卡／4 組／每卡四段／54 個 `data-search` | **通過** | `term-card` 54、`term-list` 4、`data-search` 54、逐卡驗證白話／位置或角色／機制／不要混淆四段順序，54/54 相符；`verify/gate17_dict.txt`、`verify/gate17b_dict_fourparts.txt` |
| 25 | 自我檢核：36 題 id／`-check`／`-output`／`quiz-reset`／`quiz-progress` | **通過** | 各 36、36、36、1、1；`verify/gate17c_selfcheck.txt` |
| 26 | index 14 張 chapter-card | **通過** | `verify/gate14_index.txt` |
| 27 | pager 環 index→00→…→11→字典→自我檢核→index | **通過** | 15 頁逐一列出 pager 連結，左右鏈與 BUILD-SPEC §2.2 完全相符，含三個例外（index、00、自我檢核）；無孤兒頁；`verify/gate14_pager.txt` |
| 28 | CRLF（styles.css 除外） | **通過** | 12 章＋index＋字典＋自我檢核＋`app.js` 逐行皆為 CRLF；`styles.css` 為純 LF（0 個 CR），符合例外規則；`verify/gate_crlf.txt` |
| 29 | 禁用 API：`Math.random`／`new Date`／`fetch(`／`setTimeout`／`setInterval`／`XMLHttpRequest`／`document.write`／`<style`／`style="` | **通過** | 全部命中 0；唯一的 `innerHTML` 用法在 `put()` helper 內（規格明文允許）；`verify/gate7b_forbidden.txt` |
| 30 | 表單 label 覆蓋率 | **通過** | 全課 95 個 `<input>`／`<select>` 皆有對應 `<label for>`，缺 0；`verify/gate33_labels.txt` |
| 31 | **數值交叉驗算（12 組，獨立 Python，未抄規格未呼叫 app.js）** | **通過** | 12 組全部與 BUILD-SPEC §8.4／§4 數值實例相符，差異 < 1e-5（模擬類 < 1e-3）；見第 3 節與 `verify/numbers_independent.py`、`verify/gateC_independent_results.txt` |
| 32 | app.js 實作與獨立值比對（`routhTable`／`cubicRoots`／`bisectGain`／`bisectPhase`／`pidSim`／`stateSpace`） | **通過** | node 呼叫 `module.exports` 六個函式，輸出與獨立值一致；`verify/gateC_appjs_outputs.txt` |
| 33 | 自我檢核 `QUIZ` 陣列（36 題 ans／tol） | **通過** | node dump `QUIZ`，36 題 ans／tol 與 BUILD-SPEC §6.3 逐字相符，`sel` 題答案全為 `a`；`verify/quiz_dump.json` |
| 34 | 假 DOM 邊界測試（12 widget，min／max／每個 option／number 清空與極端值） | **通過** | 143 組合，`NaN`/`Infinity`/`undefined`/`null`/空白輸出/缺「判讀」/缺「為什麼」 命中數皆為 **0**；`verify/fakedom_test.js`、`verify/gateD_fakedom_results.txt` |
| 35 | 本機靜態伺服器＋頁面身分斷言 | **通過** | `python3 -m http.server 8791`；`curl` 確認 index 與 04 章 `<title>` 含「控制系統」，非姊妹課；`verify/httpserver.log` |
| 36 | 桌面 1440×900／手機 390×844 截圖（index＋2 章） | **部分通過／橫向溢出待人工** | 6 張截圖（index、04、09 各兩種視埠）已存 `verify/shot_*.png`；截圖僅涵蓋首屏視窗尺寸，`firefox --headless --screenshot` 無法回報 `scrollWidth`／`clientWidth`，**橫向溢出本身記「待人工」**，不計入通過 |
| 37 | 人工抽驗 01／04 章不重推既有課公式 | **通過** | 01 章明文「既有課已證明，在零初始條件下，拉氏轉換會把 d/dt 換成 s」，只引用不重推；04 章明文「既有課已證明 Mp、tp、ts 與 tr 的二階結論…不重推公式」；見第 6 節 |
| 38 | 每章「能證明／不能證明」與「這件事在你的程式裡長什麼樣」 | **通過** | 12/12 章皆含「能證明」與「不能證明」（各至少 1 次）、皆含「這件事在你的程式裡長什麼樣」恰 1 次；`verify/gateF_evidence.txt` |
| 39 | 來源標註格式（筆記章號，非本課章號） | **通過** | 12 章 `<p class="note">來源：13-控制系統 §X.Y…</p>`，全部使用筆記章號 1–9，無使用本課 00–11 章號 |
| 40 | index.html 結構契約（§2.4） | **通過** | `h1`／`p.eyebrow`／`div.flow`（12 span）／`p.warning`／`aria-current` 逐一比對，文字與規格完全相符 |

---

## 2. 逐章字數表（去 `section.workspace` 與 `nav`，漢字＋全形標點，規格區間 1800–2600）

| 章 | 漢字 | 全形標點 | 合計 | 判定 |
| --- | --- | --- | --- | --- |
| 00 回授的價值 | 2120 | 326 | 2446 | 通過 |
| 01 建模 | 2088 | 317 | 2405 | 通過 |
| 02 方塊圖化簡 | 1755 | 269 | 2024 | 通過 |
| 03 穩態誤差與系統型式 | 1745 | 290 | 2035 | 通過 |
| 04 二階閉迴路 | 1635 | 278 | 1913 | 通過 |
| 05 Routh | 1818 | 207 | 2025 | 通過 |
| 06 根軌跡 | 1733 | 264 | 1997 | 通過 |
| 07 頻率響應與波德圖 | 1730 | 230 | 1960 | 通過 |
| 08 裕度與奈奎斯特 | 2221 | 348 | 2569 | 通過 |
| 09 PID | 2141 | 408 | 2549 | 通過 |
| 10 領先／落後補償器 | 2120 | 330 | 2450 | 通過（複驗 2026-09-18，原 2714） |
| 11 狀態空間入門 | 2079 | 353 | 2432 | 通過（複驗 2026-09-18，原 2908） |

（方法與腳本見 `verify/wordcount.py`。10、11 章於 2026-09-18 由建置端做「只精簡散文、不動 workspace／表格／公式／數字」的修正後，驗收員以同一腳本複驗，兩章皆已落回 1800–2600 區間，見第 7 節缺陷 D1／D2 之複驗紀錄。）

---

## 3. 數值驗算表（獨立值 vs. BUILD-SPEC vs. app.js）

驗算方法：`verify/numbers_independent.py` 純標準庫從物理／數學定義重新推導，未參考 app.js 原始碼、未抄 BUILD-SPEC 的公式化簡結果（僅用同一組物理假設與定義）。再用 `verify/gateC_appjs_outputs.txt` 記錄 `node -e "require('app.js')"` 呼叫 `module.exports` 的輸出比對。

| # | 項目 | 獨立值 | BUILD-SPEC | app.js | 判定 |
| --- | --- | --- | --- | --- | --- |
| 00 | y_cl（K=50, δ=20%, d=0.2） | 0.993388 | 0.993388 | （純算式，未呼叫模組） | 一致 |
| 01 | τ、y(0.1) | 0.099900、0.758228 | 同左 | — | 一致 |
| 02 | 閉迴路極點、T(0) | −42.000000、0.952381 | 同左 | — | 一致 |
| 03 | 型式1斜坡、型式0步階 | 0.2、0.166667 | 同左 | — | 一致 |
| 04 | ζ、M_p、y(0.5) | 0.6、9.478022 %、0.940686 | 同左 | — | 一致 |
| 05 | Routh 首行、ε 例 s¹ | [1,2,1,−6,5]、−2999998.0 | 同左（≈−2.999998×10⁶） | `routhTable([1,2,3,4,5])`＝{first:[1,2,1,−6,5],changes:2}；`routhTable([1,1,2,2,3])`＝s¹ −2999998.0000000005 | 一致 |
| 06 | K_crit、分離點 K_b | 48.000000、3.079201 | 同左 | `cubicRoots(6,8,10)`＝−4.760818、−0.619591±j1.310186（與獨立值相同） | 一致 |
| 07 | ω=10 精確 dB／相位 | −3.053514 dB、−140.710593° | 同左 | `lmag`/`lphase` 輸出同值 | 一致 |
| 08 | ω_gc、PM | 7.844079、47.403940° | 同左 | `bisectGain`＝7.844079147578313 | 一致 |
| 09 | `pidSim(20,10,5)` | peak 1.170189／M_p 17.018937／t_p 1.812／t_r 0.782／t_s 2.731／y_fin 1.000340／IAE 0.954804；K_u=60、T_u=1.894452 | 同左 | app.js `pidSim(20,10,5)` 全部欄位數值相同（至 6 位小數） | 一致 |
| 10 | 領先 PM_new、落後 PM_new | 62.864479°、44.445984° | 同左 | （lead() 函式邏輯手動核對，公式與 §7.3 bisect 一致） | 一致 |
| 11 | K=[48,7]、unobs det M_o | [48,7]、0 | 同左 | `stateSpace(...)` K=[48,7]、eigCl=−5±j5；unobs 版本 detMo=0，K=[41,−34]（與 §4 邊界例相符） | 一致 |

**未發現規格或實作的數值缺陷。** 所有差異均在浮點精度範圍內（< 1e-9），遠低於容差門檻（一般 1e-5、模擬類 1e-3）。

---

## 4. 假 DOM 邊界測試明細

- 方法：Node `vm` 建立最小 `document` stub（`getElementById` 回傳含 `value`/`type`/`addEventListener`/`innerHTML` 的物件），控制的預設值／min／max／option 清單由 `verify/extract_widgets.py` 直接從各章 HTML 解析取得（非手key）。
- 覆蓋：12 個 widget，每個含「初始載入」1 次＋各控制的 min/max（range）、每個 option（select）、清空與極端負值（number）。
- 總組合數：**143**（loop 7、model 13、block 11、sserr 11、second 9、routh 13、rlocus 7、bode 16、margin 7、pid 11、lead 11、state 27）。
- 命中：`NaN`＝0、`Infinity`＝0、`undefined`＝0、`null`＝0、空白輸出＝0、缺「判讀」＝0、缺「為什麼」＝0。
- 證據檔：`verify/fakedom_test.js`、`verify/gateD_fakedom_results.txt`、`verify/widgets.json`。

---

## 5. 截圖與伺服器證據

- 伺服器：`python3 -m http.server 8791 --bind 127.0.0.1`（測試結束已關閉）。
- 頁面身分斷言：`curl` 確認 `index.html`、`04-二階閉迴路與極點位置.html` 的 `<title>` 含「控制系統」字樣。
- 截圖檔（`verify/` 目錄）：
  - `shot_index_1440.png`（1440×900）、`shot_index_390.png`（390×844）
  - `shot_04_1440.png`（1440×900）、`shot_04_390.png`（390×844）
  - `shot_09_1440.png`（1440×900）、`shot_09_390.png`（390×844）
- 視覺檢視：三頁在兩種視埠下版面完整、控制項與文字未見錯位或截斷。
- **橫向溢出判定為「待人工」**：`firefox --headless --screenshot` 只能輸出與 `--window-size` 相同尺寸的首屏影像（已用 PNG header 量測寬高確認為 1440×900／390×844，未超出），無法讀取 `document.documentElement.scrollWidth` 與 `clientWidth` 做逐元素比對，也無法捲動到頁面下半部（Routh 表、九點波德讀值表等寬表格）觀察是否溢出。依 BUILD-SPEC §8.5 第 32 項與 QUALITY-GATES 明文規定，此關**不得記為通過**，僅以上述 6 張截圖作為部分證據。

---

## 6. 人工抽驗紀要

- **01 章**：「推導」段落對拉氏轉換僅寫「既有課已證明，在零初始條件下，拉氏轉換會把 d/dt 換成 s」，未重新定義或推導拉氏轉換本身，符合切分鐵律。
- **04 章**：「推導」段落對 M_p／t_p／t_s／t_r 僅寫「既有課已證明 Mp、tp、ts 與 tr 的二階結論，本章只把 K、a 算出的 ζ、ωn 代入，不重推公式」，符合 PROJECT-BRIEF §8.1 對 04 章的明文要求。
- 12 章「可觀察證據」段落均含「能證明」與「不能證明」兩個關鍵句（見 `verify/gateF_evidence.txt`），且每章都指出至少一種量測工具與量到的量。
- 12 章均含「這件事在你的程式裡長什麼樣」一句，落於白話直覺或推導段（依規格允許的兩個位置）。
- 字典 6 張「配額詞」卡（閉迴路與回授／轉移函數／暫態響應／超越量／波德圖／受控體）的「不要混淆」欄逐一讀過，確認上下文即為規格要求的對照句型，非誤植或散落全文。

---

## 7. 缺陷清單

| 編號 | 檔名 | 行號／位置 | 嚴重度 | 描述 | 建議處置 |
| --- | --- | --- | --- | --- | --- |
| D1 | `10-領先落後補償器.html` | 全篇正文（去 nav／workspace 後） | 中 → **已解決** | 原正文字數 2714（含 footer）／2703（不含 footer），超過 BUILD-SPEC §8.3 item 16／§0 鐵律 11 規定的 2600 上限，超標 103–114 字 | **處置**：建置端（gpt-sol）於 2026-09-18 執行「只精簡散文、不動 workspace／表格／公式／數字」修正 pass。**複驗日期 2026-09-18**：字數降至 2450（漢字 2120＋全形標點 330），落回 1800–2600 區間；同時重驗 h2 七段前綴、prereq「前置：」、p.note 來源、恰一個 workspace、widget id／屬性／option 逐字、禁用詞配額、簡體字差集、LaTeX／外部資源／`&#` 實體、相對連結、CRLF、「能證明／不能證明」與「這件事在你的程式裡長什麼樣」、規格數值實例 6 位小數是否仍在正文，以及 `lead` widget 假 DOM 邊界（11 組合）— 全部維持通過，無新缺陷。 |
| D2 | `11-狀態空間入門.html` | 全篇正文（去 nav／workspace 後） | 中 → **已解決** | 原正文字數 2908（含 footer）／2888（不含 footer），超過 2600 上限，超標 288–308 字，是全課唯一超標超過 10% 的章節 | **處置**：同 D1，建置端 2026-09-18 同一 pass 修正。**複驗日期 2026-09-18**：字數降至 2432（漢字 2079＋全形標點 353），落回 1800–2600 區間；Ackermann 推導步驟與矩陣邊界案例數字（如 −1.000000、−2.000000、48、7、−5.000000 ± j5.000000）仍完整出現在正文；同上各關（h2／prereq／note／workspace／widget 屬性／禁用詞／簡體字／LaTeX／連結／CRLF／能證明不能證明／程式裡長什麼樣）與 `state` widget 假 DOM 邊界（27 組合）全部維持通過，無新缺陷。 |
| D3 | 全課 | — | 低（觀察，非缺陷） | 橫向溢出測試受本機工具限制（無 Playwright／headless Chrome，只有純 firefox CLI screenshot），無法產生 `scrollWidth`／`clientWidth` 之類的量化證據，只能記「待人工」 | 待有 Playwright 或可執行 JS 讀 DOM 屬性的環境時補測；或人工用桌面／手機瀏覽器實測一次 |

**未發現的缺陷類別（皆已逐項重驗且通過，見第 1 節）**：id 契約、select/range/number 屬性、禁用詞配額、簡體字／形近字、CRLF／編碼、pager 環、字典與自我檢核結構、假 DOM 邊界輸出、12 組數值交叉驗算、app.js 與獨立值的一致性、label 覆蓋率、七段骨架與 09 章 warning。

---

## 8. 已知限制

1. 本機無 Playwright、headless Chrome、numpy；數值驗算與假 DOM 測試皆以 Python／Node 標準庫獨立實作完成，未使用任何第三方數值庫做交叉驗證（但已與 app.js 原始碼的獨立實作互相印證，一致）。
2. 橫向溢出（BUILD-SPEC §8.5 item 32）在本環境下技術上無法量化驗證，列為「待人工」，已在 [WAIT_USER](../../../wf/WAIT_USER.md) 慣例下應追加一行（本檔僅記錄此限制，實際登記由後續流程處理）。
3. 高頻術語與簡體字掃描為機械 grep／差集比對，不能完全排除語意層面的「形似但語意錯置」問題；已抽驗但未逐字通讀全部 12 章 2 萬餘字正文。
4. 截圖僅涵蓋 index、04、09 三頁的首屏，未涵蓋全部 12 章與字典／自我檢核頁面的視覺檢查。

---

## 9. 交付判定

**可交付（Pass），橫向溢出待人工。**

- 硬性關卡（id 契約、禁用詞配額、CRLF／編碼、pager 完整性、數值正確性、app.js 邊界安全性、label 覆蓋率等）全數通過，且皆有可重現的機械證據。
- 原兩項字數缺陷（D1：10 章、D2：11 章）已於 2026-09-18 由建置端修正並經本檔複驗，兩章字數皆落回 1800–2600 區間，其餘各關（h2、prereq、note、workspace、widget 屬性、禁用詞、簡體字、LaTeX、連結、CRLF、能證明／不能證明、程式裡長什麼樣、假 DOM 邊界）同步複驗維持通過，無新缺陷；詳見第 7 節 D1／D2。
- 橫向溢出一項依規定記「待人工」，不計入失敗，但也不得計為通過；六張截圖顯示的首屏視覺表現正常。

本課目前所有可機械驗證關卡皆為通過，僅橫向溢出因本機工具限制待人工複測。
