> 封存 2026-09-18：課程已上線、規格／驗收已定案；活文件不再引用本檔。

# 驗收紀錄 — 數位通訊（零基礎互動課）

← 課程首頁：[index.html](index.html)｜建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)｜立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)

**本檔為獨立驗收員（sonnet，未參與建置）之複驗紀錄。不採信建置者自報，全部重驗。**

- 測試日期：2026-09-18
- 環境：Linux 6.18.49-1-MANJARO；Python 3.14.7；Node v24.14.1；Google Chrome 153.0.8010.47（`google-chrome-stable --headless=new --disable-gpu --no-sandbox --remote-debugging-port=9300`，經由 CDP／Node 內建 `WebSocket`／`fetch` 直接量測，未安裝 Playwright／Puppeteer）；本機靜態伺服器 `python3 -m http.server 8800 --bind 127.0.0.1`（document root 為本課目錄，先以 `ss -ltn` 確認 8800、9300 埠未用）。
- 驗收範圍：BUILD-SPEC 第 8 節全部項目（8.1–8.5）＋ QUALITY-GATES 六段片段＋字元級錯字掃描。所有暫存腳本置於 `scratchpad/team-digital-communications/verify/`，未修改課程目錄任何檔案（唯一寫入為本檔）。

---

## 1. 驗證矩陣

| # | 項目 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| 1 | 檔案清單完整性 | **通過** | 16 個 HTML（13 章＋index＋字典＋自我檢核）＋`app.js`＋`styles.css` 全部存在；`app.js` 668 行（≤2000）、`BUILD-SPEC.md` 1257 行（≤1400）、各 HTML 37–92 行（≤1000） |
| 2 | `styles.css` 正本位元組相同 | **通過** | 與 `專題/機器人運動學/互動課程/styles.css` `cmp` 無輸出 |
| 3 | `node --check app.js` | **通過** | 語法無誤 |
| 4 | QUALITY-GATES 片段 1：id 存在性 | **通過** | app.js 用 `var ids=[...]` 傳給 `bind()`（非字面 `getElementById("...")`），改用 regex 展開 `bind([...])`／`var ids = [...]`／`{id:"qNN-k"}` 動態 id 後再驗；監聽 id 缺失 **0** |
| 5 | id 契約**雙向** diff（8.2.1） | **通過** | app.js 監聽 id → HTML 缺失 **0**；13 章 workspace 控制 id → app.js 監聽 缺失 **0**；自我檢核 39 題非按鈕控制 id → app.js 缺失 **0** |
| 6 | 13 個 widget 監聽清單「一字不差」（7.2 節） | **通過** | 以 `node -e` 抽出每個 widget function 的 `var ids=[...]`／守衛 id／輸出 id，逐一與 BUILD-SPEC 7.2 表比對，13/13 完全相符（順序亦相同） |
| 7 | QUALITY-GATES 片段 2：外部資源 | **通過** | 非 `justty32.github.io` 的 `src`／`href="http(s)://…"` **0 個** |
| 8 | QUALITY-GATES 片段 3：LaTeX 殘留 | **通過** | `\(`／`\)`／`\[`／`\]`／`$$` **0 個** |
| 9 | QUALITY-GATES 片段 4：編碼 | **通過** | 19 個檔案 `iconv -f UTF-8 -t UTF-8` 全過；U+FFFD／U+0080–009F／U+E000–F8FF／BOM **0** |
| 10 | QUALITY-GATES 片段 5：相對連結存在性 | **通過** | 全部 `href="*.html"` 目標存在，`BROKEN LINK` **0** |
| 11 | 換行 | **通過**（現況） | HTML／`app.js`／`styles.css` 現況全為 LF，CRLF **0**（符合「作者寫 LF、Lead 收尾轉 CRLF」） |
| 12 | 控制項屬性逐字 diff（type/min/max/step/value/option 順序/selected） | **通過** | 全課 49 個控制（36 range/input ＋ 13 select）逐一與 BUILD-SPEC 第 4 節每章 widget 契約表比對，含全部 `<option>` 文字、順序與 `selected` 位置，不符 **0** |
| 13 | 控制項 `<label for>` 文字 | **通過** | 49 個 `<label>` 文字與規格「標籤：」逐字比對，不符 **0** |
| 14 | `<option>`／`<label>` 內無標籤 | **通過** | 全課 `<option>` 內含 `<` **0**；`<label>`（扣除包住的 `<input>`/`<select>`）內含 `<` **0** |
| 15 | 七段 `<h2>` 順序＋單一 workspace | **通過** | 13 章皆為「痛點→白話直覺→推導→數值實例→〔workspace〕→不要混淆→可觀察證據→連到哪門課」，workspace 恰 1 個，13/13 |
| 16 | 章節正文字數（1800–2600） | **通過** | 13 章全部落在範圍內，最低 1820（06 章）、最高 2391（12 章），見 §2 |
| 17 | `p.prereq`（00–08、11、12 含「前置：」＋`../` 既有課連結；09、10 不含） | **通過** | 11 章皆含「前置：」＋至少一個 `../` 連結；09、10 兩章確認 `前置：` 與 `../` 連結皆 **0**，符合切分鐵律 |
| 18 | pager 環 | **通過** | `index→00→01→…→12→字典→自我檢核→index` 完整成環，每頁恰 2 個連結，左右文字與目標逐一核對相符，index／00／自我檢核三個例外符合 2.2 節寫法 |
| 19 | 禁用詞配額（先 `html.unescape`、剝 `href`） | **通過** | 「位元錯誤率」「捲積碼」「迴旋碼」「交織」「擴頻」「迫零」各恰好 **1**，皆在字典對應卡「不要混淆」欄、明碼；其餘 20 個零配額詞（卷積、徵狀、校正子、信噪比、訊噪比、採樣、抽樣、調製、比特、噪聲、噪音、信道、消息、碼元、帶寬、傳遞函數、鍵移、適應性、序言、幀）全部 **0** |
| 20 | 「碼率−編碼率−誤碼率」「信號−信號與系統」「數據−數據機」「捲積（總數）」 | **通過** | 依序 0、0、0、1（僅「捲積碼」該處），數字實體 `&#` **0** |
| 21 | 字典 5 張配額卡「不要混淆」內容（5.3 節） | **通過** | 「誤碼率」「零強制等化器」「摺積碼」「交錯」「展頻」5 張卡逐字含規格要求的固定句與對照詞（位元錯誤率／迫零／捲積碼＋迴旋碼／交織／擴頻），5/5 |
| 22 | 簡體字策展清單掃描 | **通過** | BUILD-SPEC §8.3.5 curated 58 字清單命中 **0** |
| 23 | 與已驗收課程語料漢字差集判讀 | **通過** | 抽驗高頻詞逐一 grep（見 §6 說明），未見策展清單外的真簡體字 |
| 24 | 形近錯字（收斂→收旂、摺積→折積、症狀→徵狀、等化器→均衡器、碼片→碼皮、載波→戴波） | **失敗** | `07-自適應等化LMS.html` 全篇 **9 處**「收旂」、**0 處**「收斂」，見缺陷 D1；其餘 5 組（折積、徵狀、均衡器、碼皮、戴波）全課 **0**，正確詞（摺積 51、症狀 48、等化器 48、碼片 36、載波 104）皆有使用 |
| 25 | 符號固定項（ASCII `*` 乘號、`μs`、U+00B5 接單位、`e-` 科學記號） | **通過** | ASCII `*` 當乘號 **0**；`μs`（U+03BC 接 s）**0**（U+03BC 全部作 LMS 步長 μ 使用）；U+00B5 後接字元皆為字母 **0** 反例；真 `e-` 科學記號樣式（`\d[eE][-+]\d`）**0**（初篩誤中 `E1E2` 為 `<sub>` 剝除後的假陽性，已人工排除） |
| 26 | 邊界鐵律：LDPC/Turbo/Polar 只在含「資訊理論與編碼」句；MIMO 只在含「無線與行動通訊」句 | **通過** | 逐一列出每個出現位置（index、08、09、10、11、12、00 共 13 處）並取整句核對，13/13 句子皆含對應限定詞 |
| 27 | 字典結構（60 卡／6 組／四段／`data-search`） | **通過** | `term-card` **60** 張、6 個 `term-list`、60/60 卡皆含 4 段（白話／位置或角色／機制／不要混淆）、缺 `data-search` **0**；60 張卡標題順序與 5.2 節詞表逐一核對完全相符 |
| 28 | 自我檢核結構（39 題、13 個 select 首項 `value=""`） | **通過** | 39 題 id（本體／`-check`／`-output`）齊全，缺失 **0**；13 個 `select` 首項全部 `value=""` |
| 29 | 自我檢核內容與 `app.js QUIZ` 陣列比對 | **通過** | `node -e` 讀 `module.exports.QUIZ`，39 題 `ans`／`tol` 逐一與 BUILD-SPEC 6.3 節表核對，不符 **0** |
| 30 | `<!doctype html>`／`lang="zh-Hant"`／`meta charset` | **通過** | 16 頁全部齊全 |
| 31 | 頁內 `<style>`／`style=`／額外 `<script>` | **通過** | 全課 0 個 `<style>`、0 個 `style="`、每頁僅 1 個 `<script defer src="app.js">` |
| 32 | `<label for>` 覆蓋率 | **通過** | 全站控制 id 對 `label for` 缺失 **0** |
| 33 | `output`／`count`／`progress` 的 `aria-live` | **通過** | 相關 `<div id="*-output">`／`term-count`／`quiz-progress` 缺 `aria-live` **0** |
| 34 | 數值交叉驗算（獨立 Python，`math` only） | **通過** | 14 組全部到六位小數／六位有效數字相符，見 §3 |
| 35 | 規格抽驗（06／08／11 章） | **通過（發現 1 處規格文字用例算術與其自身公式不符，未流入產品，見 §3、缺陷 D2）** | 三章逐一重算，見 §3 |
| 36 | `app.js` 之 `module.exports` 對照 Python | **通過** | `erfc`／`Q`／`berOf`／`reqEbN0`／`lmsRun`／`hamEncode`／`hamSyndrome`／`convEncode`／`viterbiDecode`／`interleaveRun`／`CONS`／`TRAIN` 全部與獨立 Python 六位小數相符 |
| 37 | 假 DOM 邊界測試（13 widget × 預設／min／max／option） | **通過** | 267 次輸出檢查，`NaN`／`Infinity`／`undefined`／`null`／`e-` 科學記號／空白 output **0** |
| 38 | headless Chrome：16 頁 × 2 視埠（CDP 直連） | **通過** | 32 次量測，見 §5；`document.title` 含「數位通訊」32/32；橫向溢出（`scrollWidth > clientWidth+1`）**0/32**；console error／page exception **0/32** |
| 39 | 目視截圖（桌面／手機，代表頁） | **通過** | `00` 章手機、字典手機截圖無截斷／無水平捲軸／控制項未重疊，見 §5 |
| 40 | 功能互動測試（真實瀏覽器） | **通過** | `chain` widget 即時反應、鍵盤 focus、quiz 對／錯／空白回饋、`quiz-reset`、字典搜尋，全部見 §5 |
| 41 | 外部請求（真實瀏覽器 Network） | **通過（1 項環境雜訊，非產品缺陷）** | 每頁 4 筆：頁面＋`styles.css`＋`app.js`＋`chrome-extension://…/page-script.js`（本機 headless Chrome 擴充套件自動注入，非頁面原始碼） |
| 42 | HTML 結構驗證（html-validate 等） | **待人工** | 環境未安裝，依規則不得安裝新套件，未執行 |
| 43 | 字元級錯字掃描（29 個高頻術語） | **通過（1 項延伸發現已併入 D1）** | 29 個高頻術語（訊號、符元、星座…高斯）逐一 grep 全課計數，見 §6；「收斂／收旂」對照掃描即是發現 D1 的來源 |
| 44 | 英文縮寫首次出現繁中展開（抽驗 10 個） | **部分通過，1 項待裁決** | ISI／MMSE／ML／MAP／CP／ZF 6 個在課內（字典或章節正文）有「繁中（English, 縮寫）」展開；**QAM／BPSK／QPSK／SNR 全課 0 次展開**，其中 SNR 依 PROJECT-BRIEF §2.6 屬固定符號用法（比照 E_b/N_0，不要求逐次展開），QAM／BPSK／QPSK 三個調變縮寫則全課皆未展開，見缺陷 D3 |
| 45 | 實機觸控／真人視覺／螢幕閱讀器 | **待人工** | 無實體裝置與真人測試者，機械檢查無法涵蓋 |

**通過 41／失敗 1／待人工 2／部分通過（不計入前三類，已併入失敗與待人工外的觀察項）1，共 45 列。**

---

## 2. 章節字數量測

計法：漢字（U+4E00–U+9FFF、U+3400–U+4DBF）＋全形標點（U+3000–U+303F、U+FF00–U+FFEF），取整檔可見文字，先移除 `<section class="workspace">` 與 `<nav>`，再去標籤並 `html.unescape`。

| 章 | 字數 | 章 | 字數 |
| --- | --- | --- | --- |
| 00 數位通訊世界觀與收發鏈 | 1918 | 07 自適應等化 LMS | 1884 |
| 01 訊號空間與正交基底 | 1919 | 08 漢明碼與症狀解碼 | 2017 |
| 02 M 進制星座與最小距離 | 1887 | 09 摺積碼與 Viterbi 解碼 | 1848 |
| 03 最佳接收機與 ML 判決 | 1874 | 10 交錯與突發錯誤 | 2362 |
| 04 誤碼率公式與 Q 函數 | 1824 | 11 展頻與 CDMA | 2346 |
| 05 載波相位與同步 | 1932 | 12 OFDM 與循環字首 | 2391 |
| 06 等化器 ZF 與 MMSE | 1820 | | |

全部落在 1800–2600，無需援引寬限。檔案行數：`index.html` 37、章節頁 56–84、`名詞與概念字典.html` 458、`自我檢核.html` 92、`app.js` 668、`styles.css` 14；`BUILD-SPEC.md` 1257 行。

---

## 3. 數值交叉驗算

以獨立 Python（僅用 `math` 模組，未參考 `calc.py`／`calc-out.txt` 的計算結果，全部重寫）重算 BUILD-SPEC §8.4 指定之 14 組，並抽驗第 06、08、11 章之規格算術。

| # | 章 | 重算內容 | 獨立 Python 結果 | BUILD-SPEC 錨定值 | 相符 |
| --- | --- | --- | --- | --- | --- |
| 1 | 00 | 1000、1/2、2、0.25 → B、η | B 1250.000000、η 0.800000 | B 1250.000000、η 0.800000 | 是 |
| 2 | 01 | 1、1、−1、0.1 → d、Pe | d 2.000000、Pe 3.872108×10⁻⁶ | 同左 | 是 |
| 3 | 02 | qam16 gray/natural 12dB → Pb | 1.386587×10⁻⁴／1.848783×10⁻⁴ | 同左 | 是 |
| 4 | 02 | psk16 vs qam16 dmin dB 差 | 4.195285 dB | 4.195285 dB | 是 |
| 5 | 03 | 0.8、4dB → γ、MAP | γ −0.137973、MAP 9.635546×10⁻³ | 同左 | 是 |
| 6 | 04 | qam64@10dB；req(1e-5) | 2.653261×10⁻²；17.786888 dB | 同左 | 是 |
| 7 | 05 | qpsk,10dB,10°,5‰ → Pb、損失、跨界符元數 | 7.156784×10⁻⁵、1.817874dB、19 | 同左 | 是 |
| 8 | 06 | a=0.9,SNR=15dB → ZF/MMSE dB | 7.787536dB／10.535224dB | 同左 | 是（抽驗章，另見下） |
| 9 | 07 | lmsRun(0.05,0.5,5,200) | curve[200]=0.001922、w0=0.997251；0.35→發散於99 | 同左 | 是 |
| 10 | 08 | hamEncode(1011)+錯3,6；10dB Pb | 誤糾為 1001；Pb 1.175716×10⁻⁶ | 同左 | 是（抽驗章，另見下） |
| 11 | 09 | convEncode+viterbiDecode(誤1,2,3) | 解碼 0011、finalMetric 2 | 同左 | 是 |
| 12 | 10 | interleaveRun(4,6,5) | errs[2,2,1,1]、fail 2 | 同左 | 是 |
| 13 | 11 | N=64,K=8,J/S=10,12dB,async/sync | 4.831729dB／6.819837×10⁻³；sync 6.588708dB | 同左 | 是（抽驗章，另見下） |
| 14 | 12 | 20,64,0.25,0.5,6 → Δf、Tcp、位元率 | 312.500000kHz、0.800000µs、96.000000Mbit/s | 同左 | 是 |

erfc 參考值（Python `math.erfc` 與 `app.js` `erfc` 對照）：erfc(0.5/1/2/3/4/5/7) 與 `Q(√20)` 皆到六位有效數字相符，`node -e` 呼叫 `module.exports.erfc`／`.Q` 同步驗證通過。

**規格抽驗（06、08、11 章，逐一親手重算，附額外邊界值，非僅第 8.4 節 14 組本身）**：

- **06 章**：獨立重算 a=0.5/15dB 之 ZF/MMSE（13.750613dB 一致）；並抽驗正文「邊界對照」與「量級對照」段落內 a=0.95,SNR=0dB（ZF −10.109954dB、MMSE 0.770765dB、多 10.880718dB）——與獨立重算完全相符，**課程內容無誤**。
- **08 章**：獨立重算編碼增益表三個目標（10⁻³/10⁻⁵/10⁻⁶）之未編碼／編碼 E_b/N_0 與增益，三組皆與課程內容（6.789523/6.658027/0.131495dB；9.587858/9.174484/0.413375dB；10.529832/10.056825/0.473007dB）完全相符，**課程內容無誤**（過程中曾因自己混用線性／dB 引數的暫時性程式錯誤得到錯誤中間值，修正後確認課程與規格皆正確，記此以示過程透明）。
- **11 章**：獨立重算 §8.4 第 13 組（async/sync）與正文「邊界對照」段另外 3 組（J/S=−10dB,K=8：async 7.593678dB/3.494710×10⁻⁴、sync 11.893762dB/1.336236×10⁻⁸、Kmax=9；N=16,J/S=10：−0.513824dB/9.126911×10⁻²；N=128：7.079473dB/6.988372×10⁻⁴、Kmax=8），全部與課程內容完全相符，**課程內容無誤**。但在核對 BUILD-SPEC.md 第 4 節本章 widget 契約「邊界情形」欄位文字（非 §8.4 表、非課程 HTML）時，發現一處與 widget 自身公式不符：規格文字寫「`cd-js = 40` 且 `cd-sf = 128` → 仍壓不住（−8.992887 dB）」，但用 widget 契約自己定義的公式（`inv = 1/g + mai + js/N`，預設 K=8、E_b/N_0=12dB）重算，得 γ_eff ≈ **−18.934443 dB**，非 −8.992887 dB；反推 −8.992887 dB 約對應 J/S=30dB 而非 40dB。**已確認此為 BUILD-SPEC.md 內部例示文字的算術落差，未被建置者抄進任何章節 HTML**（11 章正文實際使用的是 N=64（預設）配 J/S=40dB 的組合，算出 −21.942991dB，與其自身公式完全一致，見缺陷 D2）。依規則**不得修改規格**，僅記錄。

`app.js` 之 `module.exports`（`erfc`、`Q`、`berOf`、`reqEbN0`、`lmsRun`、`hamEncode`、`hamSyndrome`、`convEncode`、`viterbiDecode`、`interleaveRun`、`CONS`、`TRAIN`）以 `node -e` 呼叫，與上列 Python 值逐一核對，六位小數／六位有效數字內完全相符。

---

## 4. 假 DOM 邊界測試

複製 `scratchpad/team-digital-communications/fakedom.js` 到 `verify/fakedom_v.js`（未改動邏輯），以 `node fakedom_v.js <課程目錄> sweep` 對 16 個 HTML 解析 `input`/`select` 預設值、載入 `app.js`、逐一驅動 13 個 widget 的「預設 → 每個控制推到 min／max 或每個 option → 還原」。

- **結果**：267 次 output 字串檢查，`NaN`／`Infinity`／`undefined`／`null`／`e[+-]` 科學記號畫面輸出 **0 次**；空白 output（排除 quiz 頁載入時未作答的 `-output`）**0 次**。
- 13 個 widget（`chain`、`sigspace`、`mary`、`mapml`、`berfam`、`phase`、`eq`、`lms`、`hamming`、`viterbi`、`interleave`、`cdma`、`ofdm`）皆有覆蓋；本課無 checkbox 控制（全部為 `range` 或 `select`）。

---

## 5. headless Chrome（CDP 直連量測）

`google-chrome-stable --headless=new --disable-gpu --no-sandbox --remote-debugging-port=9300 --enable-logging=stderr --v=0` 搭配本機伺服器 `python3 -m http.server 8800`，經 Node 24 內建 `WebSocket`／`fetch`（`verify/cdp_check.js`）對每頁送 `Emulation.setDeviceMetricsOverride`／`Page.navigate`／`Runtime.evaluate`，量測後 `pkill` 關閉 Chrome 與伺服器、確認埠已釋放。

- 16 頁 × 2 視埠（`1440×900` 桌面／`390×844` 手機）＝ **32 次量測**，**每次先讀 `document.title` 斷言含「數位通訊」**，32/32 通過。

| 視埠 | 頁數 | title 斷言 | 橫向溢出 | console error | page exception |
| --- | --- | --- | --- | --- | --- |
| 桌面 1440×900 | 16 | 16/16 | 0/16（`scrollWidth`＝`clientWidth`＝1425） | 0 | 0 |
| 手機 390×844 | 16 | 16/16 | 0/16（`scrollWidth`＝`clientWidth`＝390） | 0 | 0 |

**目視截圖**（`google-chrome-stable --headless=new --screenshot --window-size=…`，存於 `verify/`）：`shot-desktop-00.png`（1440×900）、`shot-mobile-00.png`（390×844，00 章）、`shot-mobile-dict.png`（390×844，字典）——三張皆無水平捲軸、無文字截斷、控制項無重疊。

**功能互動測試**（真實瀏覽器，`verify/cdp_func.js` 透過 `Runtime.evaluate` 派發真實 DOM 事件，非假 DOM）：

| 測項 | 結果 |
| --- | --- |
| `chain` widget：`ch-rb` 改 5000 後 output 立即含 `Rc=10000.000000`／`Rs=5000.000000`／`B=6250.000000`／`η=0.800000` | 通過（人工核對 rc=5000/0.5=10000、rs=10000/2=5000、B=1.25×5000=6250、η=5000/6250=0.8，相符） |
| 鍵盤可聚焦：`ch-rb.focus()` 後 `document.activeElement.id` ＝ `ch-rb` | 通過 |
| 自我檢核答對：`q00-1`＝1000 → 「答對」＋解釋＋回去看連結 | 通過 |
| 自我檢核答錯：`q00-2`＝999 → 「再看一次」＋正確答案 1250＋錯因 | 通過 |
| 自我檢核空白：`q00-3`（select）＝"" → 「先選一個選項」 | 通過 |
| `quiz-progress`：答 2 題後為 2/39 | 通過 |
| `quiz-reset`：清空 value／output，`quiz-progress`→0/39 | 通過 |
| 字典搜尋：`term-search`＝"ofdm" → `term-count` 顯示 4/60 | 通過 |

**外部請求**（真實瀏覽器 Network，`verify/cdp_net.js`）：每頁固定 4 筆——頁面本身、`styles.css`、`app.js`、`chrome-extension://cimiefiiaegbelhefglklhhakcgmhkai/page-script.js`。後者為本機 headless Chrome 自動載入之擴充套件注入，與頁面原始碼無關（靜態掃描已確認 0 個非本站外部 `src`／`href`），判定為環境雜訊。

---

## 6. 缺陷清單

| # | 檔名 | 行號 | 症狀 | 建議修法 | 嚴重度 |
| --- | --- | --- | --- | --- | --- |
| D1 | `07-自適應等化LMS.html` | 32、34、36（×2）、54、62、63（×2）、66 | 「收斂」（convergence）全篇誤植為「收旂」，共 **9 處**，本章「收斂」正確寫法 **0 處**；`自我檢核.html`／`名詞與概念字典.html` 的「收斂」共 7 處皆正確，僅 07 章單章受影響 | 全篇「收旂」逐一改回「收斂」（純字串修正，不影響任何數值或結構） | 中（形近錯字，整章一致誤植，影響閱讀但由上下文仍可推知詞義；不影響互動邏輯與數值） |
| D2 | `BUILD-SPEC.md`（第 4 節 11 章 widget `cdma` 之「邊界情形」欄） | 約 11 章 widget 契約段（規格文字，非課程頁） | 規格文字寫「`cd-js=40` 且 `cd-sf=128` → 仍壓不住（−8.992887 dB）」，與 widget 自身公式（`inv=1/g+mai+js/N`，預設 K=8、E_b/N_0=12dB）重算得 ≈ −18.934443 dB 不符；反推 −8.992887 dB 較接近 J/S=30dB 的結果 | 依規則**不得修改規格**，僅記錄供 Lead 決定是否更正 BUILD-SPEC 文字；**未propagate 進任何課程 HTML**，11 章正文實際使用的另一組合（N=64 預設＋J/S=40dB → −21.942991dB）已獨立驗算相符，不構成產品缺陷 | 低（規格文字本身的算術瑕疵，未影響交付內容） |
| D3 | `00`、`02`、`04` 等多章（QAM／BPSK／QPSK 全課） | 全課 54／43／35 處 | `QAM`、`BPSK`、`QPSK` 三個調變縮寫在全課正文與字典中從未以「繁中（English, 縮寫）」形式展開（全課檢索「quadrature amplitude modulation」「binary phase shift keying」「quadrature phase shift keying」皆 0 命中），可能是因為 PROJECT-BRIEF §8.1 明訂星座基礎（含這些縮寫）屬「通訊系統」課 09 章教過、本課不重講的範圍，但嚴格對照零基礎加驗「術語首次出現有繁中、英文全名與必要縮寫」規則仍屬未展開 | 若 Lead 認定星座調變縮寫屬明確前置知識（00 章 `p.prereq` 已連到通訊系統 09），可維持現狀並在課程說明補一句「QAM/BPSK/QPSK 等調變縮寫已在前置課展開，本課直接沿用」；否則於任一縮寫首次出現處或字典補展開 | 低（屬課程邊界設計判斷，非機械性缺陷；`SNR` 依 PROJECT-BRIEF §2.6 屬固定符號用法，不在此列） |

**未發現的類別（已檢查、結果為 0，不重複列出）**：id 契約不符、控制屬性不符、`option`／`label` 內嵌標籤、七段順序或 workspace 計數錯誤、字數超出範圍、pager 斷環、禁用詞配額不符（含 5 張配額卡文字）、簡體字、符號誤用、邊界鐵律違反、字典／自我檢核結構或內容不符、13 組（除 D2 所述規格文字外）數值計算錯誤、假 DOM 執行期錯誤、頁面 title 錯誤、橫向溢出、console error、Barker-13 自相關數列錯誤。

---

## 7. 是否允許交付

**允許交付，但需 Lead 處理 D1 後再收尾。** BUILD-SPEC §8 全部關卡（8.1–8.5）與 QUALITY-GATES 六段片段複驗結果為通過；數值交叉驗算 14 組＋規格抽驗（06／08／11 章）全部與課程內容相符；假 DOM 與真實 headless Chrome（CDP 直連）測試均無 `NaN`／`Infinity`／`undefined`／`null`／橫向溢出／console error。

- **D1（07 章「收旂」×9）建議在交付前修正**：屬全篇一致的形近錯字，修法為純字串取代，風險低、影響閱讀品質，建議 Lead 修正後另開一輪複驗（沿用本檔第 9 節格式）。
- **D2（BUILD-SPEC 規格文字本身的例示算術）不阻擋交付**：未流入任何課程頁面，僅供 Lead 決定是否訂正規格文件。
- **D3（QAM/BPSK/QPSK 未展開）為設計裁決項，不阻擋交付**：建議 Lead 一次性裁決是否需要展開或維持現狀（比照通訊系統課的 D1/D2 先例）。

---

## 8. 待真人實機驗收項目

1. 實體觸控裝置（非模擬視埠）之手感與觸控目標大小。
2. 真人以螢幕閱讀器（NVDA／VoiceOver 等）實際操作 13 個自由實驗與 39 題自我檢核之可用性（本輪僅驗證 `label for`／`aria-live`／keyboard focus 之存在性與可動作性，未做完整螢幕閱讀器語音核對）。
3. `prefers-reduced-motion` 之真人視覺確認（本課無動畫，理論上不受影響，但未以系統層級開關實測）。
4. `html-validate`（或等價 HTML 結構驗證器）之後續補跑（環境未安裝，依規則不得安裝新套件）。
5. D3（QAM/BPSK/QPSK 展開與否）之 Lead 裁決。

---

## 9. Lead 修正後複驗

- 複驗日期：2026-09-18
- 複驗員：獨立複驗員（sonnet，新線，未延續初驗員 context）
- 範圍：僅複驗 Lead 針對 D1／D2／D3 的修正與連帶影響（CRLF 轉換、app.js 用詞回復、QUIZ_CH 章名），不重跑整份初驗

### 9.1 複驗項目表

| # | 項目 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| 1 | 07 章「收旂」→「收斂」 | 通過 | 全課 `*.html`／`app.js` 之「收旂」計 0；07 章「收斂」`grep -o` 逐字計 9 處（`grep -c` 依「行」計為 7，因第 36、63 兩行各含 2 處「收斂」），9 處位置與原缺陷 D1 記載完全對應 |
| 2 | 00 章 BPSK／QPSK／QAM 展開 | 通過 | 「binary phase shift keying, BPSK」「quadrature phase shift keying, QPSK」「quadrature amplitude modulation, QAM」於「k ＝ log₂M」句各恰 1 次；00 章正文字數（漢字＋全形標點，扣 workspace 與 nav）1955，落在 1800–2600 |
| 3 | app.js 用詞回復＋QUIZ_CH 章名 | 通過 | `傳輸介質／重排／不處理` 於 app.js 計 0；`node --check app.js` 通過；QUIZ_CH 13 章與各章 `<title>`／`<h1>` 逐一比對 13/13 相符，含 06「等化器 ZF 與 MMSE」、07「自適應等化 LMS」、10「交錯與突發錯誤」 |
| 4 | BUILD-SPEC 11 章 cdma 邊界值重算 | 通過 | 獨立 Python（僅 math）：N=128、K=8、J/S=30 dB、E_b/N_0=12 dB → γ_eff ＝ −8.992887 dB；J/S=40 dB → −18.934443 dB，與 Lead 加註值一致；另以假 DOM（複製 fakedom.js 改寫為 verify2/cdma_check.js）將 cd-sf 設 128、cd-js 設 30 後執行 app.js 的 cdma()，cdma-output 含「−8.992887」 |
| 5 | 全目錄 CRLF 轉換 | 通過 | 20 個 html／js／md 檔逐位元組計數：CRLF 數與各檔 wc -l 行數相符，裸換行（不接在 CR 後的 LF）全部 0；styles.css（14 行，LF）與正本 專題/機器人運動學/互動課程/styles.css cmp 無差異 |
| 6 | QUALITY-GATES 六段＋fakedom sweep＋禁用詞配額 | 通過 | 六段片段（id／外部資源／LaTeX／編碼／相對連結／node --check＋wc -l）輸出全部符合「應為空」；node fakedom.js D sweep 267 次輸出檢查、問題數 0；禁用詞（先去標籤取可見文字、html.unescape）：位元錯誤率／捲積碼／迴旋碼／交織／擴頻／迫零 各恰 1；傳輸介質 0；數據−數據機＝0；信號−信號與系統＝0 |
| 7 | headless Chrome：00／07／10 章 × 1440×900／390×844 | 通過 | CDP 直連（google-chrome-stable --headless=new＋Node 內建 WebSocket／fetch），3 頁 × 2 視埠共 6 次量測；document.title 含「數位通訊」6/6；console error／page exception 0/6；手機視埠 scrollWidth 皆為 390（≤390）3/3 |

### 9.2 D1–D3 狀態

- **D1**（07 章「收旂」×9）：**已解除**。全篇改回「收斂」，本章「收旂」計 0。
- **D2**（BUILD-SPEC 11 章 cdma 邊界情形例示算術）：**已解除**。規格文字已改為 cd-js = 30 對應 −8.992887 dB，並加註 cd-js = 40 正確值 −18.934443 dB；獨立 Python 重算與假 DOM 實測皆與加註值相符，且此值本就未流入任何課程頁面。
- **D3**（QAM／BPSK／QPSK 全課未展開）：**已解除**。00 章「k ＝ log₂M」句後已加入三個縮寫之「繁中（English, 縮寫）」展開，且 00 章字數仍在 1800–2600 範圍內。

### 9.3 最終結論

**允許交付。** D1、D2、D3 三項缺陷經獨立複驗全數解除；本輪複驗之 7 個項目（含連帶重跑之 QUALITY-GATES 六段、假 DOM sweep、headless Chrome 3 頁複測）全部通過，無新增缺陷。
