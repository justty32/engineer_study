> 封存 2026-09-18：課程已上線、規格／驗收已定案；活文件不再引用本檔。

# 驗收紀錄 — 通訊系統（零基礎互動課）

← 課程首頁：[index.html](index.html)｜建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)｜立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)

**本檔為獨立驗收員（sonnet，未參與建置）之複驗紀錄。不採信建置者自報，全部重驗。**

- 測試日期：2026-09-18
- 建置方式：章節 HTML、字典、自我檢核與 `app.js` 由 codex CLI（gpt-sol）撰寫（依 BUILD-SPEC 第 9 節派工）。
- 環境：Linux 6.18.49-1-MANJARO；Python 3.14.7；Node v24.14.1；Google Chrome 153.0.8010.47（`google-chrome-stable --headless=new --remote-debugging-port=9333`，經由 CDP／WebSocket 直接量測，未安裝 Playwright／Puppeteer，全用 Node 內建 `WebSocket`／`fetch`）；本機靜態伺服器 `python3 -m http.server 8800 --bind 127.0.0.1`（document root 為本課目錄）。
- 驗收範圍：BUILD-SPEC 第 8 節全部項目（8.1–8.5）＋ QUALITY-GATES 六段片段＋字元級錯字掃描。

---

## 1. 驗證矩陣

| # | 項目 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| 1 | 檔案清單完整性 | **通過** | 15 個 HTML ＋ `app.js` ＋ `styles.css` 全部存在，檔名與第 1 節逐字相同；共 2032 行（含 `styles.css` 14 行） |
| 2 | `styles.css` 正本位元組相同 | **通過** | 與 `專題/機器人運動學/互動課程/styles.css` `cmp` 無輸出；sha256 皆為 `80eb33ac…`，14 行 |
| 3 | `node --check app.js` | **通過** | 語法無誤 |
| 4 | QUALITY-GATES 片段 1：id 存在性 | **通過** | 原片段的 `getElementById`／`byId` regex 對本課不適用（本課用 `$("id")` helper，BUILD-SPEC §7.7 明文規定），改用 `\$\("id"\)`／`put(...)`／`var ids=[...]`／`QUIZ` 動態 id 展開後再驗；共 177 個 id（53 控制 ＋ 12 章節 output ＋ 108 個 quiz 相關 id ＋ `quiz-reset`／`quiz-progress`／`term-search`／`term-count`），缺失 **0** |
| 5 | id 契約**雙向** diff（BUILD-SPEC §8.2.1–2） | **通過** | app.js→HTML 缺失 **0**；HTML（`section.workspace`＋字典＋自我檢核）→app.js 缺失 **0**；與 §7.2 宣稱之 53 控制 ＋ 12 output 完全吻合 |
| 6 | QUALITY-GATES 片段 2：外部資源 | **通過** | 非 `justty32.github.io` 的 `src`／`href="http(s)://…"` **0 個** |
| 7 | QUALITY-GATES 片段 3：LaTeX 殘留 | **通過** | `\(`／`\)`／`\[`／`\]`／`$$` **0 個**（app.js 的 `$` 是 helper 名，非 LaTeX） |
| 8 | QUALITY-GATES 片段 4：編碼 | **通過** | 18 個檔案 `iconv -f UTF-8 -t UTF-8` 全過；U+FFFD／U+0080–009F／U+E000–F8FF／BOM **0** |
| 9 | QUALITY-GATES 片段 5：相對連結存在性 | **通過** | 全部 `href="*.html"` 目標存在，`BROKEN LINK` **0** |
| 10 | 換行 | **通過**（現況） | HTML／`app.js` 現況為 **LF**（CRLF 0 行），符合 BUILD-SPEC 鐵律 6「作者寫 LF 即可，CRLF 由 Lead 收尾」；`styles.css` 亦為 LF（正確，不得轉） |
| 11 | `range`／`select`／`checkbox` 型別分佈 | **通過** | range **44**、select **8**、checkbox **1** ＝ **53**，與 BUILD-SPEC §7.2 完全相符 |
| 12 | 控制項屬性逐字 diff（type/min/max/step/value） | **通過** | 53 個控制對照 BUILD-SPEC 第 4 節逐字比對（手動列表核對，見下方說明），不符 **0** |
| 13 | `select` 的 `option` value 與順序逐字 diff | **通過** | 8 個 select 全部核對，選項順序、`value`、`selected` 預設位置與規格逐字相符，不符 **0** |
| 14 | `<option>`／`<label>` 內無標籤 | **通過** | 全課 `<option>` 內文含 `<` 之情形 **0**；`<label>` 內除 `<input>`／`<select>` 控制本身外含標籤之情形 **0** |
| 15 | checkbox label 順序 | **通過** | `nf-swap` 的 `<input>` 位於文字之前（唯一 1 個 checkbox），其餘控制文字皆在 `<input>`／`<select>` 之前，符合 §2.3 |
| 16 | 七段 `<h2>` 順序＋單一 workspace | **通過** | 12 章皆為「痛點→白話直覺→推導→數值實例→〔workspace：自由實驗〕→不要混淆→可觀察證據→連到哪門課」，workspace 恰 1 個且位置正確，12/12 |
| 17 | 章節正文字數（1800–2600） | **通過** | 12 章全部落在範圍內，最低 1813（02 章），見 §2 |
| 18 | `p.prereq`「前置：見〈…〉」＋跨課連結（00–09、11） | **通過** | 11 章全部含「前置：」字樣＋至少一個 `../../` 既有課連結；10 章確認 **無**既有課連結、無「前置：」字樣，符合切分鐵律 |
| 19 | pager 環 | **通過** | `index→00→01→…→11→字典→自我檢核→index` 完整成環，每頁恰 2 個連結，左右文字與目標逐一核對相符，特例（index／00／自我檢核）符合 §2.2 |
| 20 | 外部資源／LaTeX／`.md` 連結 | **通過** | 外部資源 0（僅 `main-site-link`）；LaTeX 0；`href="*.md"` **0**（index.html 的 `.md` 檔名為純文字條列，未加連結，符合規格） |
| 21 | 禁用詞配額（先 `html.unescape`） | **通過** | 「誤碼率」「摺疊」各恰好 **1** 處，皆在 `名詞與概念字典.html` 對應卡「不要混淆」欄、明碼；其餘 18 個零配額詞（信噪比、訊噪比、採樣、抽樣、調製、解調製、比特、噪聲、噪音、信道、消息、碼元、帶寬、震盪器、數據、卷積、捲積、傳遞函數、鍵移）與簡體形（滤波器、载波、频谱、锁相环）全部 **0**；「碼率」扣除「誤碼率」子字串後 **0**；數字實體 `&#NNN;` **0** |
| 22 | `信號` − `信號與系統` | **通過** | 全課「信號」40 處、「信號與系統」40 處，差 **0** |
| 23 | 簡體字策展清單掃描 | **通過** | BUILD-SPEC §8.3.9 curated 58 字清單命中 **0** |
| 24 | 與已驗收課程語料漢字差集判讀 | **通過** | 與 `電機/02-電機核心/信號與系統-互動課程/*.html`（19 檔）比較，本課獨有漢字 **125** 個，逐一目視判讀，全為正常繁中用字（如 偵測、儲存、瑕疵、閃爍、遙遠、軌道、輻射、國、衛、靈敏 等），真簡體字 **0** |
| 25 | 符號固定項 | **通過** | ASCII `*` 當乘號：HTML 內 **0**（`app.js` 內 108 處皆為 JS 乘法運算子，非畫面文字）；U+2126 **0**；`μs`（U+03BC 接 s）**0**；U+00B5 後接字元皆為 `s`（23 處，全部合規）；U+03A9（Ω）2 處皆為「50 Ω 終端」正確用法 |
| 26 | 邊界鐵律（LDPC/Turbo/Viterbi/FFT／等化器等） | **通過** | LDPC／Turbo／Viterbi **0** 處；FFT **1** 處，在「OFDM 的 FFT 實作留給數位通訊課」句中；等化器／漢明碼／捲積碼／展頻 **0**；MIMO 1 處在「留給無線與行動通訊課」句中 |
| 27 | 第 11 章 `p.warning` | **通過** | 含法規管制與「本課的鏈路預算只是計算，不是發射授權」字樣 |
| 28 | 字典結構（60 卡／6 組／四段／`data-search`） | **通過** | `term-card` **60** 張，6 個組標題＋6 個獨立 `term-list`（組標題不在 `.term-list` 內）；缺 `data-search` **0**；四段（白話／位置或角色／機制／不要混淆）順序不全 **0**；60 張卡標題與 BUILD-SPEC §5.2 詞表順序逐一核對相符（`E_b/N_0`、`d_min` 依規格允許以 `<sub>` 呈現） |
| 29 | 自我檢核結構 | **通過** | 36 題 id 齊全、36 `-check`、36 `-output`、`quiz-reset`／`quiz-progress` 各 1；`num` 24 題／`sel` 12 題，與 §6.3 型別統計相符；12 個 `select` 首項皆 `value=""` |
| 30 | 自我檢核內容與 `app.js QUIZ` 陣列比對 | **通過** | 以 Node 讀 `module.exports.QUIZ`，36 題之 `ans`／`tol`（num）與 `ans`（sel）逐一比對 BUILD-SPEC §6.3 表，不符 **0**；12 個 sel 題的 HTML 選項文字與正確答案亦與 QUIZ 陣列核對一致 |
| 31 | `<option>`／`<label>` 全站掃描（含自我檢核 48 個 select 選項） | **通過** | 併入第 14 項，涵蓋全部 15 個 HTML |
| 32 | `<!doctype html>`／`lang="zh-Hant"`／`meta charset` | **通過** | 15 頁全部齊全 |
| 33 | 頁內 `<style>`／`style=`／額外 `<script>` | **通過** | 全課 0 個 `<style>`、0 個 `style="`、每頁僅 1 個 `<script defer src="app.js">` |
| 34 | class 白名單 | **通過** | 使用的 class 全部落在 BUILD-SPEC §0.2 白名單內，額外 class **0**（`grid` 未被使用，屬允許但未用，非缺陷） |
| 35 | `topbar aria-current` 規則 | **通過** | 抽驗 00／01／字典／自我檢核：僅 00、字典（字典項）、自我檢核（自我檢核項）標記 `aria-current="page"`，01–11 不標記，符合規格 |
| 36 | 每章一句「軟體那一端」 | **通過** | 12 章各恰好 1 處 |
| 37 | 每章來源標註格式 | **通過** | 12 章皆有 `來源：14-通訊系統-上／下 §…`，一律用筆記章號而非本課章號 |
| 38 | `<label for>` 覆蓋率 | **通過** | 全站 90 個表單控制（含 quiz `select`）全部有對應 `<label for>` 或被 `<label>` 包住，缺 **0** |
| 39 | `output aria-live` | **通過** | 全站 48 個 `-output` div 全部含 `aria-live="polite"` |
| 40 | 數值交叉驗算（獨立 Python，`math` only） | **通過** | 12 組 ＋ 09 章 5 種星座 ＋ erfc 參考表全部到六位小數／六位有效數字相符，見 §3 |
| 41 | 規格抽驗（05／07／09 章） | **通過（未抓到算術錯）** | 三章獨立重算與 BUILD-SPEC／PROJECT-BRIEF 錨定值逐一相符，見 §3；本次**未發現規格算術錯** |
| 42 | app.js 之 `erfc`／`Q`／`KT0_DBM_HZ` 對照 Python | **通過** | `node -e` 呼叫 `module.exports`，7 個 erfc 參考值與 `Q(√20)`、`KT0_DBM_HZ` 均與 Python `math.erfc` 及手算相符至 6 位有效數字 |
| 43 | 假 DOM 邊界測試（12 widget × 預設／min／max／option） | **通過** | 123 輪系統性測試 ＋ 9 組手動組合邊界（superhet 影像＝0、LO 為負、amenv μ＝0／1、isi α＝0∧τ＝0、cohdem dsbsc φ＝90、friis G₁＝0＋交換、link 最差裕度、shannon 超容量）；`NaN`／`Infinity`／`undefined`／`null`／`e-` 科學記號畫面輸出 **0**、空白 output **0** |
| 44 | headless Chrome 15 頁 × 2 視埠（CDP 直連，非 dump-dom） | **通過** | 30 次量測，見 §5；`document.title` 含「通訊系統」30/30；橫向溢出（`scrollWidth > clientWidth+1`）**0/30**；console error／page exception **0/30** |
| 45 | 功能互動測試（真實瀏覽器，非假 DOM） | **通過** | dbcalc 即時反應（p1→500 得 26.989700 dB，人工核對 10log10(500)=26.989700 相符）；quiz 對錯回饋、空白不計入、reset 歸零；字典搜尋 "qam" 從 60 過濾到 8 張，見 §5 |
| 46 | 外部請求（真實瀏覽器 Network） | **通過（1 項環境雜訊，非產品缺陷）** | 每頁 4 筆請求＝頁面＋`styles.css`＋`app.js`＋`chrome-extension://…/page-script.js`；後者為本機 headless Chrome 自動載入之擴充套件注入（30 頁一致出現、與頁面原始碼無關，頁面原始碼掃描已確認 0 個非本站外部 `src`／`href`），判定為環境雜訊 |
| 47 | HTML 結構驗證（html-validate 等） | **待人工** | 環境未安裝 `html-validate`，依規則不得安裝新套件；未執行 |
| 48 | 字元級錯字掃描（高頻術語形近變體） | **通過** | 25 個高頻術語全部出現且次數合理；策展形近變體清單掃描 **0** 命中（唯一命中「滾降係」為「滾降係數」子字串誤判，非真變體） |
| 49 | 英文縮寫首次出現繁中展開（抽驗 10 個） | **部分通過，2 項待裁決** | 抽驗 SNR／IF/FM／AM／QAM／NF／BPSK／RF／LO／ISI：8 個在正文首次出現處已有「繁中（English, 縮寫）」展開；**AM、FM 在 00 章正文（第 11、35 行）以口語「AM 電台」「FM 廣播」先出現，正式展開分別在 01、03 章**，見 §6 D1；**RF 全課皆以裸縮寫或 `f_RF` 下標使用，從未出現「射頻（radio frequency, RF）」展開**，見 §6 D2 |
| 50 | 實機觸控／真人視覺／螢幕閱讀器 | **待人工** | 無實體裝置與真人測試者，機械檢查無法涵蓋 |

---

## 2. 章節字數量測

計法：漢字（U+4E00–U+9FFF、U+3400–U+4DBF）＋全形標點（U+3000–U+303F、U+FF00–U+FFEF），取 `<main>` 內容，先移除 `<section class="workspace">` 與 `<nav>`，再去標籤並 `html.unescape`。

| 章 | 字數 | 章 | 字數 |
| --- | --- | --- | --- |
| 00 通訊世界觀與分貝帳本 | 1825 | 06 取樣、量化與 PCM | 1944 |
| 01 AM 包絡與調變指數 | 1819 | 07 基頻傳輸、ISI 與升餘弦 | 1894 |
| 02 抑制載波、單邊帶與同步解調 | 1813 | 08 匹配濾波器與位元錯誤率 | 1889 |
| 03 FM 與 Carson 頻寬 | 1908 | 09 數位調變與星座圖 | 1874 |
| 04 超外差接收機與影像頻率 | 1912 | 10 頻譜效率與 Shannon 極限 | 1865 |
| 05 雜訊、訊雜比與雜訊指數 | 1876 | 11 鏈路預算 | 1908 |

全部落在 1800–2600，無需援引 2 % 寬限。檔案行數：`index.html` 36、章節頁 53–81、`名詞與概念字典.html` 516、`自我檢核.html` 266、`app.js` 403、`styles.css` 14。

---

## 3. 數值交叉驗算

以獨立 Python（僅用 `math` 模組，與 `app.js` 無共用程式碼）重算 BUILD-SPEC §8.4 指定之 12 組，並抽驗第 05、07、09 章之規格算術。

| # | 章 | 重算內容 | 獨立 Python 結果 | BUILD-SPEC／PROJECT-BRIEF 錨定值 | 相符 |
| --- | --- | --- | --- | --- | --- |
| 1 | 00 | 100 mW→dBm；100 MHz→λ/4 | 20.000000 dBm；0.749481 m | 20.000000 dBm；0.749481 m | 是 |
| 2 | 01 | μ=0.5,fm=1kHz→RC上限；RC=100µs 漣波 | 275.664448 µs；0.995017 % | 275.664448 µs；0.995017 % | 是 |
| 3 | 02 | φ=30°→dB 損失；SSB 25 路 | −1.249387 dB；25 路 | −1.249387 dB；25 路 | 是 |
| 4 | 03 | 75/15→BT；3β² dB；CNR(30dB) | 180.000000 kHz；18.750613 dB；19.208188 dB | 180 kHz；18.750613 dB；19.208188 dB | 是 |
| 5 | 04 | 1000/455 高側→fLO,影像;Q=50→IRR dB | 1455;1910 kHz；36.818324 dB | 1455;1910 kHz；36.818324 dB | 是 |
| 6 | 05 | 三級 Friis 預設／對調 NF | 1.908120 dB／9.051478 dB | 1.908120 dB／9.051478 dB | 是（抽驗章，見下） |
| 7 | 06 | n=8→SQNR；CD 位元率 | 49.925712 dB；1411.200000 kbit/s | 49.925712 dB；1411.2 kbit/s | 是 |
| 8 | 07 | α=0.35,τ10%,K20→眼開度；α=0 同條件 | 0.716817（72.958020 %）；0.273479（27.802939 %） | 0.716817；0.273479 | 是（抽驗章，見下） |
| 9 | 08 | Eb/N0=10dB→BPSK/ON-OFF/同調FSK Pe；erfc 表 | 3.872108e-6／1.267366e-2／7.827011e-4；erfc(0.5…7) 全符 | 同左 | 是 |
| 10 | 09 | Eb/N0=12dB→五星座 Pb | BPSK/QPSK 9.006010e-9；8PSK 6.337879e-5；16QAM 1.386587e-4；64QAM 9.723985e-3 | BPSK/QPSK 9.006010e-9；16QAM 1.386587e-4；64QAM 9.723985e-3 | 是（抽驗章，見下） |
| 11 | 10 | B=1MHz,SNR=20dB→C；最低 Eb/N0（η=6）；Shannon 極限 | 6.658211 Mbit/s；10.211893 dB；−1.591745 dB | 6.658211 Mbit/s；12.218487 dB（64-QAM 需求值，非「此 η 最低值」）；−1.591745 dB | 見下說明 |
| 12 | 11 | 2400MHz,100m→FSPL；預設鏈路裕度；最遠距離 | 80.052008 dB；28.912879 dB；2790.255411 m | 80.052008 dB；28.912879 dB；2790.255411 m | 是 |

**關於第 11 組（10 章）的一個文字說明，非數值錯誤**：BUILD-SPEC §7 錨定表寫「64-QAM 6 Mbit/s 需 E<sub>b</sub>/N<sub>0</sub> ＝ 12.218487 dB」，這是「以 6 Mbit/s 反推所需 E<sub>b</sub>/N<sub>0</sub>」（`app.js` 用同一個 `constellationResult` 公式算出，非 `min_ebn0` 那條 Shannon 下界公式）；本欄位的 10.211893 dB 則是獨立重算 `min_ebn0=(2^6−1)/6` 之 dB 值（app.js `shannon()` 内部「此 η 最低 E<sub>b</sub>/N<sub>0</sub>」一列）。兩者是不同量、都各自與程式碼相符，**非規格衝突**，已將二值都列出避免混淆。

**規格抽驗（05、07、09 章，逐一親手重算）**：三章之全部教學數值（Friis 預設與對調 NF、升餘弦眼開度與相對開度、五種星座在 12 dB 下之 Pb）均以獨立 Python 重算並與 BUILD-SPEC／PROJECT-BRIEF 錨定值逐一核對，**本次未發現規格算術錯**（與 `微處理機與匯流排` 課驗收時發現的 2 處規格錯誤情形不同，本課規格經查未見類似問題）。

`app.js` 之 `module.exports`（`erfc`、`Q`、`sinc`、`rcPulse`、`num6`、`sci`、`CONSTEL`、`KT0_DBM_HZ`、`QUIZ`）以 `node -e` 呼叫並與上列 Python 值逐一核對，六位有效數字內完全相符。

---

## 4. 假 DOM 邊界測試

以純 JS 物件模擬 `document.getElementById`／`addEventListener`／`innerHTML`（先設 `global.document` 再 `require("app.js")`，觸發第 7 節「`typeof document!=="undefined"`」自動註冊）。

- **系統性掃描**：12 個 widget，每個對其全部控制做「預設 → 該控制推到 min → 推到 max（或每個 `option`，或 checkbox 兩態）→ 還原預設」，共 **123 輪**檢查，每輪檢查 12 個章節 output 的 `innerHTML`。
- **結果**：`NaN`／`Infinity`／`undefined`／`null`／`e[+-]` 科學記號畫面輸出 **0 次**；空白 output **0 次**。
- **手動組合邊界（9 組，因需同時調整兩個以上控制，系統性單控制掃描無法涵蓋）**：
  1. `superhet`：低側注入、`frf=910`、`fif=455` → 影像頻率＝0，走文字路徑，無報錯。
  2. `superhet`：低側注入、`frf=530`、`fif=1000` → LO 低端為負，走「頻段低端 LO 為負，低側注入不可行」文字路徑。
  3. `amenv`：`mu=1`（效率上限、RC 上限＝0）、`mu=0`（無調變）均正常。
  4. `isi`：`alpha=0` 且 `tau=0` 同時邊界，正常。
  5. `cohdem`：`dsbsc` 且 `phi=90` → 正交零點「完全消失」文字路徑，非除以 0。
  6. `friis`：`nf-g1=0` 且勾選「交換」→ 正常計算，無除以 0（`G` 皆為 `undb()` 恆正）。
  7. `link`：`pt` 最小、`d` 最大、`f` 最大同時 → 最差裕度情境，仍為有限數字。
  8. `shannon`：`snr=-10`、`scheme=qam64` → 超容量分支（`over`）觸發，無報錯。
  9. 以上全數 `innerHTML` 檢查通過（無 bad pattern）。

---

## 5. headless Chrome（CDP 直連量測）

環境未見 Playwright，改用 Chrome 自帶的 DevTools Protocol，經 Node 24 內建 `WebSocket`／`fetch`（未安裝任何 npm 套件）直接對 `google-chrome-stable --headless=new --remote-debugging-port=9333` 送 `Page.navigate`／`Runtime.evaluate`／`Emulation.setDeviceMetricsOverride` 等指令，量測方式等價於 Playwright，且更貼近規格「起本機伺服器＋量測」的字面要求。

- 伺服器：`python3 -m http.server 8800 --bind 127.0.0.1`（用畢已 `pkill`，Chrome debug port 9333 亦已關閉）。
- 15 頁 × 2 視埠（`1440×900` 桌面／`390×844` 手機）＝ **30 次量測**，**每次先讀 `document.title` 斷言含「通訊系統」才記錄其餘欄位**，30/30 通過。

| 視埠 | 頁數 | title 斷言 | 橫向溢出 | console error | page exception | widget output 留白 |
| --- | --- | --- | --- | --- | --- | --- |
| 桌面 1440×900 | 15 | 15/15 | 0/15 | 0 | 0 | 0（12 章節頁）|
| 手機 390×844 | 15 | 15/15 | 0/15 | 0 | 0 | 0（12 章節頁）|

**自我檢核頁的 36 個 quiz `-output`（含兩視埠共 72 筆）在載入時為空屬預期行為**（規格 §7.6：僅按下「對答案」才寫入），不計入「留白」缺陷。

**外部請求**：每頁固定 4 筆——頁面本身、`styles.css`、`app.js`、`chrome-extension://cimiefiiaegbelhefglklhhakcgmhkai/page-script.js`。後者在全部 30 次量測中一致出現、與課程頁面原始碼（已用靜態掃描確認 0 個非本站外部連結）無關，判定為**本機 headless Chrome 自動載入的擴充套件注入，屬環境雜訊，非產品缺陷**（同類判例見 `微處理機與匯流排` 課驗收紀錄之 favicon.ico 一次性 404）。

**功能互動測試**（真實瀏覽器，透過 `Runtime.evaluate` 派發真實 DOM 事件，非假 DOM）：

| 測項 | 結果 |
| --- | --- |
| `dbcalc` 即時反應：`db-p1` 改 500 後 output 立即含 `500.000000`／`26.989700 dB` | 通過（人工核對 10 log₁₀500 ＝ 26.989700，相符） |
| 鍵盤可聚焦：`db-p1.focus()` 後 `document.activeElement.id` ＝ `db-p1` | 通過 |
| 自我檢核答對：`q00-1`＝20 → 「答對」＋解釋＋回去看連結；`quiz-progress`→1/36 | 通過 |
| 自我檢核答錯：`q00-2`＝999 → 「再看一次」＋正確答案＋錯因 | 通過 |
| 自我檢核空白：`q00-3`＝"" → 「先選一個選項」，`quiz-progress` 不遞增 | 通過 |
| `quiz-reset`：清空 value／output，`quiz-progress`→0/36 | 通過 |
| 字典搜尋：`term-search`＝"qam" → `顯示 8 / 60 張卡`，`.term-card:not([hidden])` 亦為 8 | 通過 |

---

## 6. 缺陷清單

| # | 檔名 | 行號 | 症狀 | 建議修法 | 嚴重度 | 狀態 |
| --- | --- | --- | --- | --- | --- | --- |
| D1 | `00-通訊世界觀與分貝帳本.html` | 11（「痛點」段）、35（「數值實例」邊界對照段） | 「FM 廣播」「AM 電台」在 00 章正文以口語提前出現，正式的「繁中（English, 縮寫）」展開分別在 01 章（`振幅調變（amplitude modulation, AM）`）與 03 章（`頻率調變（frequency modulation, FM）`）才出現，嚴格對照 PROJECT-BRIEF §2.8／零基礎加驗「術語首次出現有繁中、英文全名與必要縮寫」為未展開先用 | 若 Lead 認定 AM／FM 廣播屬讀者已知的生活常識、用於建立直覺的例子而非引入新技術詞，可維持現狀並在 §2 用語說明補一句「AM／FM 於此僅作生活例子，正式定義見 01／03 章」；若要嚴格合規，可在 00 章首次出現處加簡短括號展開 | 低（教學上為刻意的生活化鋪陳，非缺乏解釋） | **已解除**（Lead 修正，見第 9 節） |
| D2 | `04-超外差接收機與影像頻率.html` 全篇；`名詞與概念字典.html`「超外差接收機」等卡 | 全篇 44 處 | 縮寫 `RF` 全課從未以「射頻（radio frequency, RF）」形式展開，僅以裸詞或 `f_RF` 下標使用（`f_RF`、`f_IF`、`f_LO`、`f_image` 屬 BUILD-SPEC §3.2 固定符號，但裸字「RF」本身未展開） | 於 04 章首次出現「RF」處或字典「超外差接收機」卡補一次「射頻（radio frequency, RF）」 | 低（上下文緊鄰已展開的 IF／LO，語意可由對稱推得，且 BUILD-SPEC 本身也未將 RF 列入需建卡之詞表） | **已解除**（Lead 修正，見第 9 節） |
| D3 | 全課 | — | `html-validate`（或等價 HTML 結構驗證器）環境未安裝，依規則不得安裝新套件，本輪**未執行**結構驗證（如 `no-redundant-for`／`doctype-style` 等規則） | 由後續驗收（若環境有該工具）補跑 | 待人工，非缺陷 | 未變 |

**未發現的類別（已檢查、結果為 0，不重複列出）**：id 契約不符、控制屬性不符、`option`／`label` 內嵌標籤、七段順序或 workspace 計數錯誤、字數超出範圍、pager 斷環、禁用詞配額不符、簡體字、符號誤用、邊界鐵律違反、數值計算錯誤（含規格本身）、假 DOM 執行期錯誤、頁面 title 錯誤、橫向溢出、console error。

---

## 7. 是否允許交付

**允許交付。** BUILD-SPEC §8 全部關卡（8.1–8.5）與 QUALITY-GATES 六段片段複驗結果為通過；數值交叉驗算 12 組＋規格抽驗（05／07／09 章）全部相符、**未發現規格或程式的算術錯誤**；假 DOM 與真實 headless Chrome（CDP 直連）測試均無 `NaN`／`Infinity`／`undefined`／`null`／橫向溢出／console error。D1、D2 已由 Lead 修正並經第 9 節複驗通過；D3（`html-validate` 未安裝）仍為待人工，不構成阻擋交付的理由。**本課現況：無未結案缺陷，僅剩第 8、9 節列出之待人工項目。**

## 8. 待真人實機驗收項目

1. 實體觸控裝置（非模擬視埠）之手感與觸控目標大小。
2. 真人以螢幕閱讀器（NVDA／VoiceOver 等）實際操作 12 個自由實驗與 36 題自我檢核之可用性（本輪僅驗證 `label for`／`aria-live`／keyboard focus 之存在性，未做完整螢幕閱讀器語音核對）。
3. `prefers-reduced-motion` 之真人視覺確認（本課無動畫，理論上不受影響，但未以系統層級開關實測）。
4. ~~D1／D2 之語感裁決（AM／FM 生活化提前出現、RF 未展開）是否需要修文。~~ 已由 Lead 修正並於第 9 節複驗通過，移除待辦。
5. `html-validate` 等 HTML 結構驗證器之後續補跑（工具未安裝）。

---

## 9. Lead 修正後複驗

**修正內容**（Lead 純字串修正，本次僅複驗、未再自行改動任何檔案）：

1. `00-通訊世界觀與分貝帳本.html` 第 11 行：加入「振幅調變（amplitude modulation, AM）」「頻率調變（frequency modulation, FM）」展開，對應 D1。
2. `04-超外差接收機與影像頻率.html` 第 12 行：「兩個不同 RF」改為「兩個不同的射頻（radio frequency, RF）輸入」，對應 D2。

**複驗環境**：沿用第一輪環境（Node v24.14.1、Google Chrome 153.0.8010.47、Python 3.14.7）；本機伺服器改用新埠 `python3 -m http.server 8801 --bind 127.0.0.1`、Chrome debug 埠改用 `9334`（避開第一輪已釋放但仍需區隔的埠），用畢已 `kill`／確認埠不再佔用。

| # | 項目 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| 1 | 00 章字數（1800–2600） | **通過** | 修正後 **1837** 字（原 1825，+12），仍在範圍內 |
| 2 | 04 章字數（1800–2600） | **通過** | 修正後 **1920** 字（原 1912，+8），仍在範圍內 |
| 3 | `node --check app.js` | **通過** | app.js 本身未被觸碰，語法仍無誤（重跑確認） |
| 4 | 禁用詞配額（先 `html.unescape`，僅這兩檔） | **通過** | 18 個零配額詞、簡體形清單、「誤碼率」「摺疊」在這兩檔皆為 **0**（未新增違規） |
| 5 | 簡體字策展清單掃描（僅這兩檔） | **通過** | 0 命中 |
| 6 | 編碼（UTF-8／BOM／U+FFFD／U+0080–009F／U+E000–F8FF） | **通過** | 兩檔皆 `iconv` 過關，特殊碼點掃描 0 |
| 7 | LaTeX 分隔符 | **通過** | 兩檔 0 命中 |
| 8 | 相對連結存在性 | **通過** | 兩檔 `href="*.html"` 目標全部存在，0 broken |
| 9 | headless Chrome：00 章，1440×900 | **通過** | title 含「通訊系統」；`scrollWidth`(1425)＝`clientWidth`(1425)，無溢出；console error 0、page exception 0 |
| 10 | headless Chrome：00 章，390×844 | **通過** | title 含「通訊系統」；`scrollWidth`(390)＝`clientWidth`(390)；console error 0、page exception 0 |
| 11 | headless Chrome：04 章，1440×900 | **通過** | title 含「通訊系統」；`scrollWidth`(1425)＝`clientWidth`(1425)；console error 0、page exception 0 |
| 12 | headless Chrome：04 章，390×844 | **通過** | title 含「通訊系統」；`scrollWidth`(390)＝`clientWidth`(390)；console error 0、page exception 0 |
| 13 | D1 是否解除 | **通過（解除）** | AM／FM 首次出現處已有「繁中（English, 縮寫）」展開，早於／同於原本 01／03 章之正式定義 |
| 14 | D2 是否解除 | **通過（解除）** | RF 首次出現處已有「射頻（radio frequency, RF）」展開 |

**結論**：14/14 通過，**0 失敗**。D1、D2 兩項缺陷經 Lead 修正後複驗解除，本課已無未結案缺陷（僅餘 D3 與第 8 節之待人工項目）。第 7 節交付判定維持**允許交付**，且移除原本「建議 Lead 裁決是否補展開」之待辦。
