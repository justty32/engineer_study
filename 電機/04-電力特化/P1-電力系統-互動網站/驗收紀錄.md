# 電力系統互動網站驗收紀錄

← [網站說明](README.md)｜[建置規格](BUILD-SPEC.md)｜[品質關卡](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)

## 驗收資訊

- 日期：2026-08-09
- 執行環境：Linux、Node.js、本機 Python 靜態 HTTP server、Firefox 153 WebDriver BiDi
- 執行期檔案：`index.html`、`styles.css`、`app.js`
- 網路限制：未下載套件、未載入外部資產、未進行外部研究

## 通過項目

- `node --check app.js` 與 `xmllint --html --noout index.html` 通過。
- HTML 共 124 個唯一 ID；JavaScript 的直接讀寫 ID 與 57 個輸入 ID 全數存在。
- 8 個模組、8 個導覽按鈕、各模組完成與重設控制皆存在。
- 最小 DOM 事件測試通過：模組導航、功角／頻率切換、完成進度、總重設、單模組重設。
- 本機 HTTP 的 HTML、CSS、JavaScript 均回應 `200`，MIME type 正確。
- 執行期不含外部 URL、CDN、遠端字型、`fetch`、XHR 或 WebSocket。
- 預設高壓案例：`836.7 A`／`167.3 A`、`4.201 MW`／`0.168 MW`、線損比 `25.0`。
- 預設標么案例：`358.6 A`、`259.21 Ω`、`0.9565 p.u.`、`0.0463 p.u.`、`0.2000 p.u.`。
- 預設功因案例：`67.53 A` 降至 `53.31 A`，補償 `553.23 kvar`，線損下降 `37.67%`。
- 三母線基準潮流與三種 N-1 皆交叉計算；跳脫 1–3 時 2–3 線路為 `100 MW`，超過 `70 MW` 限額。
- 三相、SLG、LL 故障電流及三序使用狀態通過；SLG 預設為 `3.333 p.u.`。
- 保護預設為 `0.030 s`／`0.300 s`、裕度 `0.270 s`；未拾取路徑通過。
- 穩定度預設為 `49.01°`、`Aacc=0.0827`、`Adec=0.1688` 且穩定；清除時間 `0.2 s` 判為不穩定，超過 5 秒會拒絕計算。
- 頻率預設為 `-0.600 Hz/s` 與 `59.714 Hz`。
- 調度預設為 G1 `66.67 MW`、G2 `83.33 MW`、成本 `1516.67`；surplus、shortage、備轉不足與上下限路徑通過。
- 唯讀 verifier 回報的狀態符號固定尺寸問題已改為 `data-state` CSS 符號；無未解決 findings。
- Pages 組裝模擬只新增 `power-systems/` 下三個執行期檔案，不發布筆記與規劃 Markdown。
- GitHub Pages workflow `31305571809` 的 build 與 deploy 均成功；發布 commit 為 `d03db76`。
- 線上入口、電力系統 HTML、CSS 與 JavaScript 均回應 HTTP `200`：<https://justty32.github.io/engineer_study/power-systems/>。
- 後續跨站補驗：Firefox 153 在 `390×844` 與 `1440×900` 實際載入總入口與全部 7 個網站，共 16 組 page／viewport；本站無頁面級水平溢位、外部執行期資源或 console error，8 模組與可見控制均存在。

## 未驗證與限制

- 實體手機觸控仍需使用者現場確認；瀏覽器 390px 的版面與水平溢位技術驗收已完成。
- 所有工程模型均為 P1 筆記的教學近似，適用邊界見 [README](README.md)。

## 交付判定

已發布並通過桌面與手機 viewport 的瀏覽器技術驗收。實體手機觸控仍保留為待使用者項。
