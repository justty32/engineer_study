> 封存 2026-09-18：課程已上線、規格／驗收已定案；活文件不再引用本檔。

# 驗收紀錄：馬達驅動與控制（零基礎互動課）

- 測試日期：2026-09-18 14:20–14:44 CST
- 驗收員：獨立驗收員（初驗，第 1 pass，未參與建置）
- 環境：Linux 6.18.49-1-MANJARO、node（本機版）、python3（本機版）、google-chrome-stable（本機已裝，headless=new，未新裝任何套件）
- 唯一可寫檔：本檔。掃描工具全部使用 scratchpad `team-motor-drives/` 下既有腳本（`scan.py`、`fakedom.js`），`crlf.py` 因會就地改寫檔案而**未執行**，改用 `grep -c $'\r$'` 對照 `wc -l` 做唯讀 CRLF 檢查。抽章重算全部自己另寫 python，未跑 `calc.py`。

## 逐關結果

| 關卡 | 結果 | 證據 |
| --- | --- | --- |
| 1. QUALITY-GATES 可複製片段（6 項） | 通過 | missing id=0；外部資源=0；LaTeX 殘留=0；encoding 異常=0；斷鏈=0；`node --check` OK；`wc -l` 見關卡 9 |
| 2. 檔案齊全 | 通過 | 15 個 html（index、00–11、字典、自我檢核）＋ app.js＋styles.css＋PROJECT-BRIEF.md＋BUILD-SPEC.md 齊全；`cmp styles.css` 與 `專題/機器人運動學/互動課程/styles.css` 位元組相同；另跑 `sync-styles.py --check`（全家族 14 門課，0 漂移，exit 0，本課不在既有清單中屬正常） |
| 3. id 契約 | 通過 | 自建 python 比對 BUILD-SPEC 第 4/5/6 節共 184 個預期 id（widget 76 個＋字典 term-search/term-count＋自我檢核 108 個）：missing=0，各頁內無重複 id；`grep -oE 'id="q[^"]*"'` 自我檢核.html = 110 個 occurrence（108 題目 id＋quiz-reset＋quiz-progress，與 36×3 契約相符；`grep -c` 顯示 109 因兩個 id 同行，非缺漏）；字典 term-card=50、index chapter-card=14、自我檢核 card=36 |
| 4. 假 DOM（`node fakedom.js`） | 通過 | 12 個 widget 預設輸出全部 OK（逐字符合規格數值實例）；`checks=636 hits=0`（min/max/mix1/mix2 四組邊界皆無 NaN/Infinity/undefined/null/空值） |
| 5. 抽章重算（自寫 python，未跑 calc.py） | 通過 | 第 03 章 RK4：獨立實作規格演算法，N=10 超越量=4.352513 %、N=1=24.621566 %，與規格逐字相符（連同 tpeak/tr/ts/tdip/trec 等 10 餘量全部吻合，dip 僅第 5 位小數 21.706375 vs 21.706381，屬離散步進誤差量級，不影響判定）；第 08 章基速二次式：主案例 3939.828277 rpm 相符，另驗 6 組邊界（id=−10/−20、Vdc=48/400、iq=20/0）全部吻合；第 09 章 SVPWM：T1=37.111360、T2=19.746542、T0=43.142098、va/vb/vc=93.969262/−17.364818/−76.604444、SVPWM 優勢 15.470054 % 全部吻合，另驗 6 組邊界角度/電壓亦吻合；加驗 3 個跨章表格數字（01 章 Vavg=18/E=10.471976/I=7.528024、04 章微步角 0.1125°/每圈微步 3200/300 rpm、11 章動能 2467.401100 J）全部吻合 |
| 6. 字元級掃描（`scan.py`） | 待人工（1 項疑似違規待人工判定） | 配額詞「標量」「轉差」「占空比」全課各恰好 1、均只在字典；BOM/lang/EXT/MD-LINK/OPTION-TAG/STYLE/ENTITY/簡體字/U+03BC/U+2126/星號乘號/NaN-in-text＝全部 0；`ban0` 清單（轉差率、勵磁、電動機…等 27 詞）＝全部 0；「信號」僅見於「信號與系統」；每章字數 1890–2049（門檻 1800–2500 內）；唯一命中：名詞與概念字典.html 第 229 行「換向」（非「換向器」／非「機械換向」相鄰）×1——句子為「直流機換向器所做的換向屬機械接點行為」，規則字面要求「換向」（除「換向器」外）為 0，此處是描述換向器動作的合法語境但字面違規，列為待人工裁決是否算違規或修規則 |
| 7. 結構契約 | 通過 | scan.py 對 12 章逐一檢查：H2 七段前綴（痛點／白話直覺／推導／數值實例／不要混淆／可觀察證據／連到哪門課）全部齊全、`workspace` 皆恰好 1 個、`前置：`與 `class="prereq"` 皆有、`來源：P3 [上下] §` 皆有；pager 鏈人工追蹤：index→00→01→…→11→字典→自我檢核→index，全部對應無孤兒（含 index/字典/自我檢核 footer「查字典」連結不影響 pager 判定）；index 14 張 chapter-card；字典 50 張 term-card；自我檢核 36 張 card |
| 8. 跨課連結 | 通過 | `grep -l 'P2-電力電子\|P1-電力系統'` 全課為空；20 條 `../../02-電機核心/<課>/<頁>.html` 連結逐一驗證目標檔存在，0 個缺失 |
| 9. 規模與 CRLF | 通過 | HTML 36–529 行（含字典 446、自我檢核 266）皆 ≤1000；app.js 529 行 ≤2000；BUILD-SPEC.md 920 行 ≤1400；html/js/PROJECT-BRIEF.md/BUILD-SPEC.md 全部 `wc -l` = `grep -c $'\r$'`（無裸 LF）；styles.css 未被 CRLF 轉換，仍與正本位元組相同 |
| 10. app.js 語法與禁用 API | 通過 | `node --check app.js` 通過；`grep -c 'Math.random\|setTimeout\|setInterval\|localStorage\|fetch(\|new Date'` = 0 |
| 11. 本機 HTTP 伺服 | 通過（桌機/手機視覺待人工降級為已跑） | 埠 18734；`<title>` 斷言＝「馬達驅動與控制（零基礎互動課）」相符；15 頁 curl 全部 200；環境已有 google-chrome-stable，跑了 headless 1440×900／390×844（index 與第 03 章，含展開 widget 的長頁截圖）：無文字截斷、無控制項位移、無內容重疊、無橫向溢出；`--dump-dom` 搭配 `--enable-logging` 未見頁面層級 console error（僅 Chrome 內部遙測雜訊） |

## 失敗項清單

（本次無「失敗」判定，僅 1 項「待人工」）

- 名詞與概念字典.html：第 229 行，禁用詞「換向」（除「換向器」外應為 0）× 1——待人工裁決是否為合法語境例外

## 已知限制

- 本次僅對第 03、08、09 章做完整規格重算，並加驗 01/04/11 章各 1–3 個表格數字；其餘章節數值僅透過假 DOM 與規格數值實例比對確認，未逐一手算。
- 桌機/手機視覺僅抽測 index 與第 03 章（含 widget 區塊），未逐頁截圖 15 頁。
- 未執行 Playwright 自動化互動測試（點擊/拖動 range、鍵盤操作、focus 可見性等），僅靜態截圖判讀無明顯版面問題。

## 是否允許交付

有條件允許交付：除「換向」用詞待人工裁決外，其餘全部關卡通過，無阻斷性缺陷。

## 複驗（第 2 pass）

- 複驗日期：2026-09-18
- 驗收員：獨立驗收員（複驗，第 2 pass，新線，未參與建置也未做過初驗）
- 環境：Linux 6.18.49-1-MANJARO、node（本機版）、python3（本機版）
- 範圍：只重驗初驗後 Lead 針對純字串修正（銜損→銅損等）是否確實生效、以及該修正是否破壞其餘既有通過項；只可寫本檔，未動課程其他任何檔、未 git 操作、未上網。

| 項目 | 結果 | 證據 |
| --- | --- | --- |
| (a) 「銜損」全課 0 | 通過 | `grep -c '銜' *.html app.js`：16 個檔（15 html＋app.js）全部 0 |
| (b) 「階躍」「閥值」全課 0 | 通過 | `grep -c '階躍'`、`grep -c '閥值'` 對同 16 個檔皆全部 0 |
| (c) 字典換向器句已改、「換向」除例外詞為 0 | 通過 | 名詞與概念字典.html 第 229 行：「不要混淆：「換相」是電子切換相序；直流機換向器所做的機械換向屬機械接點行為…」，字面即含「換向器所做的機械換向」；全課 `grep -oE '換向'` 共 5 次命中（00 章第 17 行 2 次、05 章第 11 行 1 次、字典第 229 行 2 次），逐一比對 `grep -oE '換向器'`（3 次）與 `grep -oE '機械換向'`（3 次，其中 05 章 1 次與 換向器 重疊於「機械換向器」），5 次命中全部落在「換向器」或「機械換向」內，無裸「換向」殘留 |
| (d) scratchpad `python3 scan.py` 與 `node fakedom.js` 重跑 | 通過 | `scan.py`：無 CHARS-OUT／無禁用詞行／無簡體字提示／無 BROKEN LINK；`QUOTA TOTAL: {'標量': 1, '轉差': 1, '占空比': 1}`，且 per-file 輸出只有 名詞與概念字典.html 這一行帶 `Q:` 標記，其餘 14 個 html 無配額詞命中，符合「各 1 且只在字典」；`fakedom.js`：12 個 widget（torqueangle/hbridge/currentloop/cascade/stepper/bldc/vf/clarkepark/focvolt/svpwm/encoder/regen）逐一輸出 OK，`checks 636 hits 0` |
| (e) QUALITY-GATES.md 可複製片段 1)–6) | 通過 | 1) id 缺漏＝空；2) 外部資源（非 justty32.github.io）＝空；3) LaTeX 殘留＝空；4) `iconv` 編碼異常＝空、BOM/替換字元/控制碼/私用區＝空；5) BROKEN LINK＝空；6) `node --check app.js` 通過（exit 0），`wc -l` 15 html（36–529 行）＋app.js 529 行，總計 2191 行，數字與檔案清單相符 |
| (f) CRLF 與 styles.css 比對 | 通過（1 項系統性例外，非本次修正造成） | 15 個 .html＋app.js＋BUILD-SPEC.md（920/920）＋PROJECT-BRIEF.md（214/214）逐檔 `grep -c $'\r$'` 等於 `wc -l`；唯一不等的是 驗收紀錄.md 本身（crlf=0, total=38，純 LF）——經抽查全庫 30+ 門課的 驗收紀錄.md（含 archive/ 下）全部同樣是純 LF、從未 CRLF，屬本專案「稽核紀錄檔用 LF、產品檔用 CRLF」的既定通例，與本次 Lead 的純字串修正無關，不列為缺陷；`cmp styles.css` 與 `專題/機器人運動學/互動課程/styles.css` exit 0，位元組相同 |
| (g) 抽章重算：第 05、第 10 章 | 通過 | 第 05 章（θ=15°、I=10A、Kt=0.5）自寫 python 重算：sin75°×Kt×I=4.829629（頁面 4.829629）、六步平均係數 3/π×cos0°=0.954930、平均轉矩 0.954930×5=4.774648（頁面 4.774648）、90° 佔比 sin75°×100=96.592583 %（頁面「96.592583 %」）全部相符；第 10 章（N=1000、Ts=1ms、n=300rpm、fclk=10MHz）自寫 python 重算：M 法相對誤差 1/20=5.000000%、T 法相對誤差 1/500=0.200000%、交叉速度 60√(fclk/Ts)/4N=1500.000000 rpm 全部與頁面相符；轉矩命令跳動一項首次用四捨五入後的顯示值 K_p=3.141593 重算得 4.934803，與頁面 4.934802 差在末位，改用內部應為全精度 π（頁面「3.141593」僅為顯示四捨五入）重算 π²/2=4.934802 才精確吻合，交叉驗證 N=100（5π²=49.348022，頁面 49.348022）與 N=10000（π²/20=0.493480，頁面 0.493480）兩組邊界亦精確吻合，判定頁面數字正確、我方初次手算誤差為使用了顯示值而非全精度常數 |
| (h) 自我檢核抽 5 題（app.js QUIZ vs BUILD-SPEC 803–845 行） | 通過 | 抽 q02-2／q05-2／q07-2／q09-1／q11-3：app.js 第 93/96/98/100/102 行 `["q02-2","num",13.5,0.1]`、`["q05-2","num",0.826993,0.001]`、`["q07-2","num",29.289322,0.01]`、`["q09-1","num",37.11136,0.01]`、`["q11-3","sel","d",0]` 與 BUILD-SPEC 表格同 id 列（答案／容差）逐一比對全部一致；另抽查 2 題 select 的頁面選項字母對應：自我檢核.html 第 258 行 `id="q11-3"` 的 `option value="d"` 內容為「斬波器撐得住峰值功率」，與 BUILD-SPEC「d：斬波器撐得住峰值功率」相符；第 120 行 `id="q05-1"` 的 `option value="c"` 內容為「010」，與 BUILD-SPEC「c：010」相符 |

複驗結論：允許交付（(a)–(h) 全數通過；(f) 的 驗收紀錄.md 純 LF 為全庫既定通例、非本次修正造成，不影響交付判定；初驗遺留的「換向」待人工項已由 Lead 修正並經本次複驗確認未破壞其餘任何通過項）。
