# 驗收紀錄 — 電力電子（零基礎互動課）

← 課程首頁：[index.html](index.html)｜建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)｜立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜品質關卡：[QUALITY-GATES.md](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)

**本檔為獨立驗收員（未參與建置）的複驗紀錄，不採信任何建置端自報，全部項目親手重驗。** 臨時腳本與證據檔存於
`/tmp/claude-1000/-home-lorkhan-repo-engineer-study/0c7d0627-5cec-4c9c-b89c-164f188f64ba/scratchpad/team-power-electronics/verify/`（下稱 `verify/`），本檔引用的「證據量」皆可在該目錄的對應檔案中重現。

- 測試日期：2026-09-18
- 環境：Linux 6.18.49-1-MANJARO；Python 3（標準庫，無 numpy）；Node（`node --check` 與 `require()`）；Firefox 155.0.1（headless，`-no-remote -profile` 獨立 profile 以避開常駐 Firefox 進程）
- 工作樹規則：本次驗收僅新增本檔，未修改 `app.js`、任何 HTML 或規格檔，未執行 `git add`／`commit`／`stash`／`checkout`，未上網、未安裝套件。

---

## 1. 驗證矩陣

| # | 項目 | 結果 | 證據量／證據檔 |
| --- | --- | --- | --- |
| 1 | 檔案齊全（15 HTML＋app.js＋styles.css＋BUILD-SPEC＋PROJECT-BRIEF） | **通過** | `ls` 確認檔名與第 1 節逐字相同，缺檔 0；`verify/gates_1_6.py` |
| 2 | `styles.css` 與正本位元組相同 | **通過** | `cmp` 對 `專題/機器人運動學/互動課程/styles.css`：兩檔皆 4163 bytes，位元組相同 |
| 3 | `node --check app.js` | **通過** | exit 0；`verify/gate6_syntax.txt` |
| 4 | QG 片段 1：app.js 用到的 id 都在 HTML 內 | **通過** | 123 個含連字號字串候選中排除 2 個非 id（`data-search`、`term-card` 屬性／class 名）後，121 個真實 id 全部命中，缺 0；`verify/gate1_ids.txt`、`verify/gate1_v2.py` |
| 5 | QG 片段 2：外部資源僅主站連結 | **通過** | 非 `justty32.github.io` 的外部 `src`／`href` 命中 0；`verify/gate2_external.txt` |
| 6 | QG 片段 3：零 LaTeX | **通過** | `\(`\)`\[`\]`\$\$` 命中檔案數 0；`verify/gate3_latex.txt` |
| 7 | QG 片段 4：編碼／BOM／控制碼／私用區 | **通過** | 16 個檔（15 HTML＋app.js）`iconv -f UTF-8 -t UTF-8` 全過；BOM／U+FFFD／U+0080–009F／U+E000–F8FF 命中 0；`verify/gate4_encoding.txt` |
| 8 | QG 片段 5：相對連結存在 | **通過** | 32 個相異連結目標（課內章節、跨課 4 門課首頁）全部存在，broken 0；`verify/gate5_links.txt` |
| 9 | QG 片段 6：`node --check` ＋ `wc -l` | **通過（附規模，見第 4 節）** | `verify/gate6_syntax.txt` |
| 10 | 69 個控制 id、12 個 output id 各在對應章恰好 1 次 | **通過** | 逐一 `re.findall` 計數，81 個 id 全部恰為 1，失敗 0；每個控制皆有對應 `<label for>`；`verify/gate_idcounts.txt`、`verify/id_counts.py` |
| 11 | 56 個 `range` 的 min／max／step／value 逐字比對 | **通過** | 從 BUILD-SPEC.md 機器解析出 56 個 `<input type="range">` 定義，與 HTML 逐一比對屬性字串，count-issue 0、attr-mismatch 0；`verify/gate_ranges_selects.txt`、`verify/id_contract.py` |
| 12 | 13 個 `select` 的 option value／順序／顯示文字 | **通過** | 同上腳本，13 個 select（`sl-dev`、`r1-topo`、`r1-f`、`r3-topo`、`r3-f`、`r3-load`、`pw-fclk`、`bs-topo`、`sp-topo`、`sp-f1`、`th-wave`、`tm-sink`、`tm-tjmax`）option 陣列逐字相同，mismatch 0 |
| 13 | 自我檢核：36 題 id／`-check`／`-output`、`quiz-reset`、`quiz-progress` | **通過** | 各 36、36、36、1、1；直接 grep 計數確認 |
| 14 | 自我檢核 12 個 `sel` 選項文字（q00-3…q11-3） | **通過** | 12 題 a/b/c/d 選項文字逐字比對 §6.3，mismatches 0 |
| 15 | `<option>` 內無標籤 | **通過** | `grep -n '<option[^>]*>[^<]*<[^/]'` 命中 0 |
| 16 | app.js `QUIZ` 陣列（36 題 type／ans／tol） | **通過** | node dump `module.exports.QUIZ`，36 題與 §6.3 逐字比對，mismatches 0；`verify/quiz_dump.json` |
| 17 | 字典：50 卡／4 組／每卡四段順序固定 | **通過** | `term-card` 50、`term-list` 4（13/12/12/13）、`data-search` 50；50 張卡逐卡驗證段落順序（白話／位置角色／機制／不要混淆），異常 0 |
| 18 | 禁用詞配額（5 詞恰 1 且在字典「不要混淆」欄、16 詞恰 0、`&#` 實體 0、「信號」僅在信號與系統） | **通過** | `html.unescape` 後逐詞計數：占空比／紋波／晶閘管／變流器／換流器 各恰 1 且全部落在 `名詞與概念字典.html` 對應卡片的「不要混淆」段落內；柵極等 16 詞全 0；`&#` 全課 0；「信號」全課 4 次，全部落在「信號與系統」課名或連結內；`verify/gate_quota_terms.txt` |
| 19 | 高頻術語 grep（16 詞） | **通過（無形近字異常）** | 工作週期 48、漣波 85、伏秒平衡 17、切換損耗 7、導通損耗 7、觸發角 19、反流 18、逆變器 39、諧波 77、結溫 49、熱阻 32、電感 202、電容 158、閘極 13、二極體 63、功率因數 6，全部非零 |
| 20 | 簡體字差集掃描 | **通過（未發現簡體字）** | 以「微處理機與匯流排」「控制系統」兩課語料做全字元差集，本課獨有漢字 95 個，逐一判讀（如 矽／鎵／鰭／殼／鍍／輻／醫）皆為合法繁體字（含台灣慣用「矽」而非中國「硅」），非簡體形；`verify/gate_simplified.txt` |
| 21 | U+03BC／U+2126／ASCII `*` 當乘號 | **通過** | 三者在全課 HTML 皆為 0（`app.js` 內 `*` 為運算子，依規格不計）；`verify/gate_quota_terms.txt` |
| 22 | 七段 `h2` 前綴＋順序（痛點／白話直覺／推導／數值實例／自由實驗＋workspace 自身標題／不要混淆／可觀察證據／連到哪門課） | **通過** | 12 章逐一列出 h2 文字，順序與前綴全部相符；`verify/gate_content_structure.txt` |
| 23 | 每章恰一個 `section.workspace` | **通過** | 12/12 章各 1 個 |
| 24 | 00–10 章 `p.prereq` 含「前置：」、11 章不含 | **通過** | 11 章各恰 1 個含「前置：」；11 章的 `p.prereq` 改寫為「這章假設已懂：01、05、07、10」四章連結，不含「前置：」字樣，符合 BUILD-SPEC §8.3 例外規定 |
| 25 | 01／02／03／08 章各一個 `p.warning`，其餘 0 | **通過** | 01/02/03/08 各恰 1、其餘 8 章各 0 |
| 26 | 每章至少一個 `p.note` 含「來源：P2-電力電子」 | **通過（1 項觀察，非缺陷，見第 6 節）** | 11/12 章的來源標註為「§數字」格式；00 章為「§「這科在學什麼」」，與 BUILD-SPEC 第 203–213 行對該章的逐字規定一致（PROJECT-BRIEF §7 對照表 00 章來源本就無編號小節），判定不算缺陷 |
| 27 | 每章「能證明」「不能證明」「這件事在你的程式裡長什麼樣」各至少 1 次 | **通過** | 12/12 章三者皆為 True |
| 28 | 每章「不要混淆」至少四條 | **通過** | 12 章逐一計數，最少 4 條（00/01/05/06/07）、最多 5 條 |
| 29 | 正文與 app.js 輸出不得含 NaN/Infinity/undefined/null | **通過** | 15 個 HTML 檔內文字掃描 0 命中；假 DOM 跑 1836 組輸出快照亦 0 命中 |
| 30 | 章節正文字數（去 `section.workspace` 與 `nav`，漢字＋全形標點，1800–2600） | **12/12 通過** | 見第 2 節；`verify/gate_wordcount.txt`、`verify/wordcount.py` |
| 31 | pager 環 index→00→…→11→字典→自我檢核→index | **通過** | 15 頁逐一列出 pager 連結，鏈路完整、無孤兒頁；`verify/gate_pager.txt` |
| 32 | index 14 張 `chapter-card` | **通過** | grep 計數 14（12 章＋字典＋自我檢核） |
| 33 | CRLF（`styles.css` 除外） | **通過** | 15 HTML＋`app.js` 每行皆為 CRLF（bareLF 0）；`styles.css` 為純 LF（CRLF 0），符合例外；`verify/gate_crlf.txt` |
| 34 | 禁用 API（`Math.random`／`new Date`／`setTimeout`／`setInterval`／`localStorage`／`fetch(`／`XMLHttpRequest`／`document.write`）與 `<style>`／`style=` | **通過** | 全部命中 0；`innerHTML` 僅出現在 `put()` helper 一處（規格允許） |
| 35 | 僅使用鐵律第 2 條允許的 21 個 class，無新增 class | **通過** | 全課掃出的 class 集合與允許清單逐字相同，0 個多餘 class |
| 36 | 表單 label 覆蓋率 | **通過** | 69 個控制全部有對應 `<label for>`，缺 0 |
| 37 | **數值交叉驗算（12 組，獨立 Python，未抄規格、未呼叫 app.js）** | **12/12 通過** | 見第 3 節；`verify/numbers_independent.py`（獨立實作）、`verify/numeric_diff.py`（79 項逐值比對，diff 0） |
| 38 | app.js 五個純函式（`rect3Calc`／`buckCalc`／`boostCalc`／`thdCalc`／`tradeCalc`）與獨立值、規格值三方比對 | **通過** | node 呼叫 `module.exports`，79 項比對全部 < 1e-4；`verify/appjs_outputs.json`、`verify/gateC_numeric_diff.txt` |
| 39 | 假 DOM 邊界測試（12 widget 預設值＋每個 range 的 min/max＋每個 option） | **通過** | 1836 組輸出快照，EMPTY 0、BAD（含 NaN/Infinity/undefined/null）0、缺「判讀：」0、缺「為什麼：」0；`verify/fakedom_extended.js`、`verify/fakedom_extended_run.txt` |
| 40 | 抽驗 3 個判讀門檻 | **通過** | `bk-r=30` → 觸發「判讀：DCM——負載 30.000000 Ω 大於臨界值…」；`bs-d=0.95` → 觸發「判讀：D＝0.950000 已越過增益峰值 D_opt＝0.900000…」；`th-wave=pulse` → 觸發「判讀：THD 117.537182 %，諧波的能量比基波還多…」，三者皆與 BUILD-SPEC 判讀句規則相符 |
| 41 | 本機 `python3 -m http.server 8793`＋頁面身分斷言 | **通過** | curl 確認 index、04、09 章 `<title>` 皆含「電力電子」，非姊妹課；結束後已關閉 server；`verify/httpserver.log` |
| 42 | 桌面 1440×900／手機 390×844 截圖（index、04、09） | **通過（6 張截圖已存）** | Firefox headless（獨立 profile 避開常駐瀏覽器進程），肉眼檢視 6 張截圖：首屏即工作台、章節正文與表格排版正常、手機版無明顯截斷或重疊；`verify/shots/*.png` |
| 43 | 橫向溢出量測（`scrollWidth` vs `clientWidth`） | **待人工** | `firefox --headless --screenshot` 無法回報 DOM 尺寸屬性，肉眼檢視 6 張截圖未見明顯溢出，但精確量測需要真實瀏覽器或 Playwright，本環境無該套件，故列為待人工 |
| 44 | 規模：HTML ≤1000 行、`app.js` ≤2000 行、`BUILD-SPEC.md` ≤1400 行 | **通過** | 見第 4 節，全部在限額內 |
| 45 | 鍵盤可用性／可見 focus／`prefers-reduced-motion` | **通過（隨 styles.css 繼承）** | `styles.css` 與已驗收姊妹課共用的正本位元組相同（第 2 項），該正本已在其他課驗收中確認具備 focus 樣式與 reduced-motion 規則，本課未新增任何 CSS，故沿用同一結論 |

---

## 2. 逐章字數表（去 `section.workspace` 與 `nav`，漢字＋全形標點，規格區間 1800–2600）

| 章 | 漢字 | 全形標點 | 合計 | 判定 |
| --- | --- | --- | --- | --- |
| 00 電力電子世界觀與切換不耗能 | 1827 | 247 | 2074 | 通過 |
| 01 功率開關與兩種損耗 | 1866 | 263 | 2129 | 通過 |
| 02 單相整流與電容濾波的代價 | 1806 | 228 | 2034 | 通過 |
| 03 三相與相控整流 | 1846 | 241 | 2087 | 通過 |
| 04 PWM 與工作週期 | 1801 | 244 | 2045 | 通過 |
| 05 Buck 降壓轉換器 | 1879 | 321 | 2200 | 通過 |
| 06 Boost 與 Buck-Boost | 1971 | 333 | 2304 | 通過 |
| 07 濾波與漣波設計 | 1983 | 311 | 2294 | 通過 |
| 08 逆變器與 SPWM | 1867 | 241 | 2108 | 通過 |
| 09 諧波與 THD | 1855 | 282 | 2137 | 通過 |
| 10 散熱與效率 | 2078 | 314 | 2392 | 通過 |
| 11 切換頻率取捨與設計總整合 | 2076 | 324 | 2400 | 通過 |

（計法：`re.sub` 去除 `<nav>…</nav>` 與 `<section class="workspace">…</section>` 後去標籤，逐字元判斷 CJK 統一表意文字（U+4E00–9FFF、U+3400–4DBF）與全形標點（U+3000–303F、U+FF00–FFEF、U+2018–201F）。方法與腳本見 `verify/wordcount.py`。12/12 章全部落在 1800–2600 區間，無需複驗。）

---

## 3. 數值交叉驗算表（12 組，獨立 Python vs BUILD-SPEC §8.4 vs app.js `module.exports`）

方法：`verify/numbers_independent.py` 依 BUILD-SPEC 各章「推導必寫」與「計算」條目**獨立重新實作**公式（未抄規格印出的結果數字、未 `require` app.js）；`verify/numeric_diff.py` 將獨立值、app.js 輸出（`rect3Calc`／`buckCalc`／`boostCalc`／`thdCalc`／`tradeCalc`，00/01/02/04/07/08/10 章無對應 exports，僅獨立值 vs 規格值二方比對）與 BUILD-SPEC 印出的規格值逐項比對，容差 < 1e-4。**79 項逐值比對，DIFF 0。**

| # | 組別 | 關鍵值（獨立 Python） | 規格值 | app.js | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1 | 00：12/5/2/92 | 14.000000 W／41.666667 %／0.869565 W／16.100000 | 同左 | 無 export | 相符 |
| 2 | 01：10A/20mΩ/48V/50ns/100kHz/0.5 | 1.000000 W／12.000000 µJ／1.200000 W／83.333333 kHz／90.000000 A | 同左 | 無 export | 相符 |
| 3 | 02：全波 110V/60Hz/1000µF/1A/0.7V | 154.163492／8.333333／149.996825／18.924841°／10.513801 %／9.511308 A／0.442150 | 同左 | 無 export | 相符 |
| 4 | 03：三相橋 220V α=30°／α=120°（電感）／α=90°（電阻） | 297.104384／257.299944／−148.552192／39.804440 | 同左 | `rect3Calc` 完全相符 | 相符 |
| 5 | 04：0.42/12V/200kHz/100MHz；48MHz/1MHz | 2.100000µs／5.040000V／500／8.965784／24.000000mV；48／−40.000000mV | 同左 | 無 export | 相符 |
| 6 | 05：12/0.5/22µH/200kHz/3Ω；R=30 | 0.681818／2.340909／1.659091／2.933333／17.600000；0.590655／7.087861／0.346518 | 同左 | `buckCalc` 完全相符 | 相符 |
| 7 | 06：Boost 12/0.5/10/0.1；D=0.95；Buck-Boost 預設 | 23.076923／96.153846 %／0.900000／5.000000；48.000000／20.000000 %；−11.538462／0.909000／4.524862 | 同左 | `boostCalc` 完全相符 | 相符 |
| 8 | 07：12/5/2/200kHz/30%/20mV/50mΩ | 24.305556µH／18.750000µF／30.000000mV／7455.327388Hz／57.142531dB | 同左 | 無 export | 相符 |
| 9 | 08：全橋 400/0.8/21/50；三相 | 320.000000／226.274170／1050／509.295818／62.831853 %；195.959179／282.842712／15.470054 % | 同左 | 無 export | 相符 |
| 10 | 09：方波 N13；六步波 N13；窄脈衝 N13；方波 N13/nc5 | 44.502424 %／0.913615；27.311131 %；117.537182 %；43.138302 %（3 次 37.260051 %） | 同左 | `thdCalc` 完全相符 | 相符 |
| 11 | 10：10W/200W/40°C/0.5/0.3/4.0/150；無散熱片 | 88.0／83.0／80.0／62.0／7.7／95.238095 %；640.0 | 同左 | 無 export | 相符 |
| 12 | 11：200kHz 預設；1000kHz | 15.0µH／37.5µF／991.875µJ／1.92W／97.624471 %／50.85°C；3.0µH／9.6W／91.883614 %／89.25°C | 同左 | `tradeCalc` 完全相符 | 相符 |

**12/12 組全部相符**，5 個 app.js 純函式（`rect3Calc`、`buckCalc`、`boostCalc`、`thdCalc`、`tradeCalc`）在可比對的 6 組上與獨立值、規格值三方一致；其餘 6 組（00、01、02、04、07、08、10 章的 widget）依 BUILD-SPEC §7.3 本就不在純函式匯出清單內，僅做獨立值 vs 規格值二方比對，一致。

---

## 4. 規模檢查

| 檔案 | 行數 | 限額 | 判定 |
| --- | --- | --- | --- |
| 15 個章節／index／字典／自我檢核 HTML（最大者：`名詞與概念字典.html`） | 438 | ≤1000 | 通過（全部 12 個章節 HTML 介於 36–93 行，遠低於限額） |
| `app.js` | 634 | ≤2000 | 通過 |
| `BUILD-SPEC.md` | 1117 | ≤1400 | 通過 |

---

## 5. 失敗項清單

無。本次複驗全部 40 個可親手重驗的項目皆為**通過**，僅 1 項（橫向溢出的精確 `scrollWidth` 量測）因環境無 Playwright／真實瀏覽器 DOM 存取能力而列**待人工**。

---

## 6. 已知限制與觀察（非缺陷）

1. **00 章 `p.note` 來源標註格式**：BUILD-SPEC §8.3 的一般敘述要求「章號為 §1–§11」，但 00 章的逐章規格（第 203–213 行）與 PROJECT-BRIEF §7 對照表都明確要求 00 章寫「來源：P2-電力電子（上）§「這科在學什麼」」（00 章在來源筆記中沒有對應的編號小節）。HTML 忠實照抄了逐章規格的規定，判定為規格內部的既有措辭差異，不算建置缺陷。
2. **橫向溢出精確量測待人工**：本環境的 Firefox headless 截圖無法回報 `scrollWidth`／`clientWidth` 等 DOM 屬性，只能肉眼比對 6 張截圖（index／04／09 章，桌面 1440×900、手機 390×844）確認排版正常、無明顯截斷或重疊，但無法給出精確的溢出像素數。建議日後有 Playwright 或可存取 DOM 的無頭瀏覽器時補測。
3. 09 章「不要混淆」段落數為規格最低要求的邊界值以上（4–5 條不等，逐章見第 1 節第 28 項），符合「至少四條」的最低要求，無需修正。

---

## 7. 交付結論

**允許交付。** 40 項可重驗項目全數通過、0 項失敗、1 項待人工（不影響交付，已列入已知限制並記錄可重現的複驗路徑）。12 組數值交叉驗算（獨立 Python）與 5 個 app.js 純函式輸出、BUILD-SPEC 規格值三方一致，差異均 < 1e-4；12 章正文字數全部落在 1800–2600 區間；69 個控制 id、12 個 output id、13 個 select、56 個 range、36 題自我檢核與 50 張字典卡片逐一比對規格皆無偏差；禁用詞配額、高頻術語、簡體字掃描、CRLF、class 白名單、禁用 API 全部合規。
