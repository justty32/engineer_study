# 驗收紀錄 — 普通物理（零基礎互動課）

← [BUILD-SPEC.md](BUILD-SPEC.md)｜[PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜工作流：[build-with-codex](../../../wf/workflows/interactive-study-site/BUILD-WITH-CODEX.md)｜[品質關卡](../../../wf/workflows/interactive-study-site/QUALITY-GATES.md)

- **驗收日期**：2026-08-27（首輪）／2026-08-27（複驗，見第五節）
- **驗收對象**：`共通基礎/物理/互動課程/`（19 HTML ＋ `app.js` ＋ `styles.css`）
- **驗收員**：獨立驗收 agent（未參與建置；依 BUILD-WITH-CODEX 流程第 3 步）
- **立場**：builder 自報全過**不予採信**，全部項目獨立重驗。無證據者一律不標通過。
- **寫入範圍**：本檔為驗收員唯一寫入之檔案，產品檔全程唯讀，發現問題只記錄不修。
- **最終判定**：**允許交付**（首輪為「有條件允許交付」，A／B／D／E 處置後於複驗全數關閉——見第五節）。

## 環境

| 項目 | 內容 |
| --- | --- |
| OS | Windows 11 Pro 10.0.26200 |
| Shell | Git Bash ＋ PowerShell 7 |
| Python | 3.13.0 |
| Node | `C:\Users\WG-Guanyu\AppData\Local\ms-playwright-go\1.57.0\node.exe`（v24.11.1） |
| Playwright | `ms-playwright-go/1.57.0/package`（既有，未下載新工具），`channel:'chrome'` |
| Chrome | `C:\Program Files\Google\Chrome\Application\chrome.exe` |
| 本機站台 | `python -m http.server 8731 --bind 127.0.0.1`（驗畢已關閉） |

## 結論

**首輪（2026-08-27）：有條件允許交付。** 功能面、數值面、編碼面、版面面全數通過且有證據；未發現任何杜撰、公式走樣或計算錯誤。建議 Lead 先處理問題 A、B 再 commit，兩者皆為字串級修正，不動邏輯。

**複驗（2026-08-27）：允許交付。** A（Lead 裁決計法）、B（08 章補英文全名）、D（doppler 邊界說明句）、E（掛站接線）全部處置完成並經獨立重驗關閉；無回歸。詳見**第五節**。

---

## 一、逐項結果

| # | 檢查項 | 結果 | 證據摘要 |
| --- | --- | --- | --- |
| 1 | 檔案存在與行數 | **通過** | 19 HTML ＋ `app.js` ＋ `styles.css` ＝ 21 檔全在；行數與位元組見第三節 |
| 2 | id 契約（控制／select／output／題目） | **通過** | 詳下 1.2 |
| 3 | LaTeX 分隔符、HTML `$` | **通過** | `\(`、`\)`、`\[`、`\]`、`\frac`、`\mathbf`、`\begin` 全 0；HTML `$` ＝ 0 |
| 4 | 外部資源 | **通過** | 全庫唯一外部 URL 為 `https://justty32.github.io/engineer_study/`；無 `fetch`、`XMLHttpRequest`、CDN、MathJax、`@import`、WebSocket |
| 5 | 版面約束 | **通過** | `<style>` 0、inline `style=` 0、未知 class 0（僅用 spec §0.2 白名單） |
| 6 | 編碼與換行 | **通過** | 21 檔 UTF-8 無 BOM；U+FFFD、U+0080–009F、U+E000–F8FF 全 0；HTML 與 JS 全 CRLF，`styles.css` 為 LF（正本原樣，工作流踩雷條明訂不得轉） |
| 7 | pager 相連 | **通過** | 19 頁環狀鏈 `index→00→…→15→字典→自我檢核→index` 全數與 spec §2.2 表相符 |
| 8 | 相對連結存在 | **通過** | 全部 `href` 解析後檔案皆存在（缺檔 0）；14 條跨課 `../../數學/互動課程/…` 逐一驗證存在 |
| 9 | `styles.css` 正本閘門 | **通過** | 詳下 1.3 |
| 10 | 內容正確性抽章（≥5 章） | **通過** | 詳下 1.4 |
| 11 | spec 自身數值抽驗 | **通過** | 詳下 1.5 |
| 12 | widget 邊界抽測（NaN 不得洩漏） | **通過** | 詳下 1.6 |
| 13 | 教學品質（QUALITY-GATES 1、2 節＋硬體加驗） | **部分通過** | 詳下 1.7，問題 B |
| 14 | 字元級錯字掃描 | **通過** | 詳下 1.8 |
| 15 | 拆檔量測 | **通過（1 項超標，已裁決）** | 詳下第三節 |
| 16 | 瀏覽器實測（桌面＋手機全 19 頁） | **通過** | 詳下 1.9 |
| 17 | 正文字數硬門檻 §0.9 | **待 Lead 裁決** | 詳下 1.10，問題 A |

---

### 1.1 檔案清單

19 HTML ＝ 章節頁 16（`00`–`15`）＋ `index.html` ＋ `名詞與概念字典.html` ＋ `自我檢核.html`，與 spec §1 表一致。

### 1.2 id 契約逐一比對

以 python 從 `BUILD-SPEC.md` 抽出契約、從 HTML 抽出實作，逐屬性比對：

| 對象 | spec | HTML | 差異 |
| --- | ---: | ---: | --- |
| widget `<input>` | 81（另 1 筆為 §2.3 樣板 `控制id`） | 81 | **屬性不符 0 筆**（`type`、`min`、`max`、`step`、`value` 全比對） |
| widget `<select>` | 7 | 7 | `topic-pick`(6)、`rot-shape`(5)、`th-mode`(3)、`oh-mat`(4)、`if-mode`(3)、`op-mode`(3)、`qm-mode`(3)；**`option value` 內容與順序全同** |
| widget `<div class="output">` | 16 | 16 | 全數 `aria-live="polite"`，無重複 id |
| 自我檢核題目 | 44 | 44 | 13 單選（`select`）＋ 31 數值（`input type=number step=any`），與已裁決事項相符 |
| 題目三件套 | — | 44／44／44 | `q##-#`、`q##-#-check`、`q##-#-output` 三組 id 集合**完全一致**；`<button>` 非 `type="button"` 者 0 |
| `<label for>` 覆蓋 | — | — | 未被 label 包住的控制 **0 個** |

- 全站 `select` 共 20 個（7 widget ＋ 13 單選題），與 spec §8.2 第 5 條相符。
- 全站 `output` 共 60 個（16 widget ＋ 44 題），無重複。
- 已裁決事項確認：`interfaceWave` 函式改名不影響 id 契約，`if-mode` 與 `interface-output` 均照原契約實作。

### 1.3 `styles.css` 正本閘門

```
cmp 共通基礎/物理/互動課程/styles.css 專題/機器人運動學/互動課程/styles.css  → 無差異
md5 三方相同：cb9d27ba5eda812ae95a18d6aabd5c5d
  共通基礎/物理/互動課程/styles.css
  專題/機器人運動學/互動課程/styles.css   （正本）
  共通基礎/數學/互動課程/styles.css       （spec §0.2 所指的來源）

python wf/workflows/interactive-study-site/tools/sync-styles.py --check
  正本 sha256：894f24a1…f3c2a　位元組數：4141
  共 4 門下游課程：4 門相同，0 門漂移，0 門缺檔
  [通過] 全家族與正本位元組相同。      exit=0
```

**結果：位元組相同，`--check` exit 0。** 本課尚未登記進 `sync-styles.py` 的 `FAMILY` 清單（故列表中未見「共通基礎/物理/互動課程」），依交辦屬正常，**僅記載**：Lead 收尾時可一併登記。

### 1.4 內容正確性抽章（親自重算，5 章）

對照來源筆記 `01-普通物理-上／中／下.md` 與 spec 逐章契約，**公式無走樣、推導必寫全數出現、無杜撰**；所有數值親手重算：

| 章 | 重算項目 | 結果 |
| --- | --- | --- |
| 01 運動學 | `v0x=v0y=14.142136`、`T=2.886150`、`R=40.816327`、`H=10.204082`、`t_peak=1.443075`、`t=1s` 的 `x=14.142136`／`y=9.242136`／`v_y=4.342136`／速率 `14.793720`、能量自檢 `109.427071+90.572929=200.000000` | **12/12 相符**。特別確認 `v_y² = 18.854142` 係以未捨入的 `v_y=4.3421356…` 算出（若誤用已捨入的 `4.342136` 會得 `18.854145`）——**原文取值正確** |
| 04 動量與碰撞 | `p=3.000000`、`v_cm=0.600000`、`v1′=−1.800000`、`v2′=2.200000`、碰後動量 `3.000000`、K 前後皆 `10.500000`、`e=0` 共同速度 `0.600000` 與損失 `9.600000`、`μ=1.2`、`e=0.5` 損失 `7.200000` | **14/14 相符**；約化質量式 `ΔK=(1/2)μ(1−e²)(v1−v2)²` 交叉驗證一致 |
| 08 靜電學 | `F=1.497925×10⁻⁶ N`、`E1=1997.233731`、`E2=1198.340238`、`Ex=3195.573969`、`V=299.585060−179.751036=119.834024`、零場點二次式 `2x²−3x+0.45=0` 得 `x=(3+√5.4)/4=1.330948`、`C=88.541878 pF`、`U=6.375015×10⁻⁹ J` | **10/10 相符**；零場點二次式係數獨立推導一致，且落在絕對值較小電荷外側的判讀正確 |
| 10 磁場與電磁感應 | `ε=0.300000 V`、`I=0.150000 A`、`F=0.015000 N`、`P機=P電=I²R=0.045000 W`、`I線=2πrB/μ0=124999.999932 A`、`L=μ0N²A/ℓ=12.566371 µH`、`τ=L/R=6.283185 µs` | **9/9 相符**；`P機=B²L²v²cos²θ/R` 之推導代數獨立展開一致 |
| 14 幾何光學與波動光學 | `s_i=15.000000 cm` 與 `M=−0.500000`、`s_o=20` 得 `s_i=20, M=−1`、`s_o=5` 得 `s_i=−10, M=+2`、`sinθ2=0.750000` 與 `θ2=48.590378°`、`θc=arcsin(1/1.5)=41.810315°`、`Δy=5.000000 mm`、`θ1=λ/d=0.005 rad=0.286479°`、`d` 加倍得 `2.5 mm`、`D` 加倍得 `10 mm`、`θmin=5.000000×10⁻⁶ rad` | **12/12 相符**；「調到 45° 則全反射」與 `θc=41.81°` 一致 |

補充：五章的模型假設與適用範圍均明寫（質點與忽略空氣阻力、一維正碰、點電荷、均勻磁場忽略自感與鐵損、薄透鏡與小角近似），符合 QUALITY-GATES §1「不把教學近似說成工程保證」。

### 1.5 spec 自身數值抽驗（防止 builder 忠實照抄規格的算術錯）

**（a）自我檢核 31 題數值答案全部獨立重算**（非抽樣，全覆蓋）。以 python 由物理原式重算並與 spec §6.3 表比對：

- **31/31 全部相符**，最大絕對差 `4.7×10⁻⁷`，全部遠小於各題容差的一半。
- `app.js` 的答案與容差與 spec §6.3 表逐筆比對：**差異 0 筆**（含 13 題單選的 `ans` 字串）。

**（b）spec §8.4 的 8 組交叉驗算全部以假 DOM 實跑 `app.js` 覆核**：

| 組 | 驗算 | 結果 |
| --- | --- | --- |
| 20 `projectile` | `T=2.886150`、`R=40.816327`、`H=10.204082`、速率 `14.793720`、`v²/2+gy=200.000000` | 相符 |
| 21 `incline` | `N=16.974098`、`fsMax=6.789639`、「會滑動」、`a=2.353885`、`θc=21.801409°` | 相符 |
| 22 `collide` | `v1′=−1.800000`、`v2′=2.200000`、`p=3.000000`、K 前後 `10.500000`；`e=0` 得 `0.600000` 與損失 `9.600000` | 相符 |
| 23 `shm` | `ω0=10`、`ζ=0.100000`、`X=0.050000`、放大率 `5.000000`、相位 `90.000000°`、等效 `L=1 H`／`R=2 Ω`／`C=0.01 F` | 相符（且 `1/√(LC)=10` 自檢一致） |
| 24 `thermal` | noise：`vrms=4.070355e−7 V`、`kTB=4.141947e−17 W`、`−133.827955 dBm`；carnot：`50.000000 %` | 相符 |
| 25 `coulomb` | `F=1.497925e−6 N`（吸引）、`Ex=3195.573969 V/m`、`V=119.834024 V`、零場點 `1.330948 m` | 相符 |
| 26 `induction` | `ε=0.300000`、`I=0.150000`、`F=0.015000`、`P機=P電=I²R=0.045000`、`I線=124999.999932 A` | 相符 |
| 27 `optics` | lens `si=15.000000 cm` 與 `M=−0.500000`；snell `θ2=48.590378°` 與 `θc=41.810315°`；slit `Δy=5.000000 mm` | 相符 |
| 28 額外 | `emwave` `λ=0.299792 m`／`η=376.730314 Ω`／`S=1.327209e−3 W/m²`；`standing` `v=316.227766 m/s`／`f1=158.113883 Hz`；`photon` `E=3.099605 eV`／`Kmax=0.799605 eV`／`λcut=539.061732 nm` | 相符 |

另覆核 `rotation`（`I=0.25`、`L=2.5`、`K=12.5`、`α=4`、`I′=0.0625`、`ω′=40`、`K′=50`、`ΔK=37.5`）與 `ohmwire`（`R線=0.168 Ω`、`I=1.180173 A`、效率 `98.347758 %`）亦與 spec 相符。

**（c）物理常數一致性自檢**（spec §3.4 指定）：

```
1/√(MU0·EPS0) vs C_LIGHT   相對誤差 2.17e−14   通過(<1e−9)
√(MU0/EPS0)   vs ETA0      相對誤差 3.02e−12   通過
1/(4π·EPS0)   vs K_E       相對誤差 5.44e−10   通過
h·c/e·1e9     vs HC_EVNM   相對誤差 2.68e−10   通過（額外自加）
```

**（d）`node --check app.js` 語法 OK**；`Math.random` 0、`new Date` 與 `Date.now` 0、`setTimeout` 與 `setInterval` 0（確定性成立）。

**結論：本次未在 spec 中發現任何算術錯誤。**（試點案曾有兩處筆誤被照抄，本案無此情形。）

### 1.6 widget 邊界抽測（NaN 不得洩漏）

以 node 假 DOM 直接驅動 `app.js`，涵蓋交辦的 4 個 widget 及 spec §8.5 全部 6 條：

| 邊界 | 輸出判準句 | NaN／Infinity／undefined |
| --- | --- | --- |
| `coulomb` `es-x` 為 0（落在 q1 上） | 「觀察點正好在點電荷上：r = 0 時場與電位發散——這是點電荷模型的極限…」 | **無洩漏** |
| `coulomb` `es-x` 等於 `es-d`（落在 q2 上） | 同上 | **無洩漏** |
| `coulomb` `q1=q2=0` | 「兩個電荷都是 0，全空間電場都是 0。」 | **無洩漏** |
| `interfaceWave` doppler `v源 ≥ v波` | 「已達到或超過波速…這個公式失效（進入音障與震波的範圍，波前疊在一起形成馬赫錐）。把波源速度調小再看。」 | **無洩漏** |
| `interfaceWave` impedance `Z1=Z2` | 「完全匹配…VSWR = 1.000000，回波損耗無限大（沒有回波可量）。」（以文字取代發散值） | **無洩漏** |
| `optics` lens `so = f` | 「物在焦點上…出射是平行光，像在無窮遠，沒有有限的像距可印。這正是準直器的工作點。」 | **無洩漏** |
| `optics` lens `f = 0` | 「焦距為零沒有物理意義（1/f 會發散），請把 f 調離 0。」 | **無洩漏** |
| `optics` snell `θ=45`（n1=1.5 到 n2=1） | 「全反射：沒有折射光，能量全部反射回介質 1。」 | **無洩漏** |
| `photon` photo `hf < W`（λ=1600 nm、W=6 eV） | 「光子能量不足（E = 0.774901 eV 小於 W = 6.00 eV）：無論光強多大都打不出電子——這正是光的粒子性證據…」 | **無洩漏** |
| `shm` `c=0` 且 `drive=10` | 「無阻尼共振：理論上振幅無限成長，真實系統會被非線性或結構破壞限制住，所以這個數字不存在上限值可印。」 | **無洩漏** |
| `energy` `h > h0`（v0=0、loss=0） | 「以目前的能量到不了 h = 10.000000 m…最高只能到 hmax = 5.000000 m。」 | **無洩漏** |
| `ohmwire` `rload=0` | 「導線比負載還耗電…這條線太細或太長，功率大半燒在路上。」 | **無洩漏** |

- **全部邊界輸出皆含判準句與因果句，未出現裸數字。**
- 防護機制經原始碼確認：`fmt`、`fmtExp`、`fmtSigned`、`auto` 對非有限值一律回傳破折號 `—`；`safeDiv` 分母為零回傳 `null` 由呼叫端走文字分支。實測 `fmt(NaN)`、`fmt(Infinity)`、`fmtExp(NaN)`、`auto(NaN)` 均得 `—`。
- **非缺陷記載**：`rotation` 在 `k=0` 時會輸出「檢查角動量：L = 2.5、L′ = 0——兩者相同」之不實陳述；但 `rot-shrink` 的 `min="0.2"`，UI 不可達該值，**不列為缺陷**，僅供日後若放寬下限時參考。

### 1.7 教學品質（QUALITY-GATES §1、§2 ＋ 硬體原理加驗）

機械面 16 章全查（非抽樣）：

| 檢查 | 結果 |
| --- | --- |
| 七段骨架（痛點→白話直覺→推導→數值實例→不要混淆→可觀察證據→連到哪門課） | **16/16 齊全，順序固定** |
| `section.workspace` 位置（在「數值實例」後、「不要混淆」前） | **16/16 正確**；每章恰 1 個 workspace |
| 先教後操作（不可先考後教） | **通過**——workspace 一律在推導與數值實例之後 |
| 比喻收回 | **16/16**；01–15 章各有 1 次「比喻到此為止」後接精確定義、符號與單位；00 章以「比喻必須立刻收回精確定義…」明寫同一原則 |
| 「不要混淆」條數（spec 要求 ≥4） | **16/16 通過**，實際 5–7 條 |
| 「可觀察證據」三件事（用什麼量／能證明／不能證明） | **16/16 皆 3/3 齊備** |
| 每章來源標註 | **16/16** 各含「來源：…」 |
| 骨架元素（`lang="zh-Hant"`、`charset`、viewport、`main-site-link`、`topbar`、`site-footer`） | 19 頁全齊 |
| 無計分、進度百分比、解鎖、強制答對 | 通過；`quiz-progress` 僅顯示「已作答 N / 44 題（僅供參考，不影響瀏覽）」，`quiz-reset` 可無限重做 |
| 互動回饋說明「為什麼」 | 通過；抽測 12 組輸出全部含因果句 |

深度抽查 2 章（08 靜電學、10 磁場與電磁感應）：

- **量測儀器「能證明／不能證明」齊備且有實質邊界**。例：08「扭秤資料能檢查力隨電荷乘積增大、隨距離平方下降」對「不能把平方反比外推到原子尺度…也不能直接證明畫在紙上的電場線是真實物件」；10「機械輸入與電輸出在摩擦、銅損等效率折扣內相符」對「不能證明自然界絕對沒有磁單極，只能繼續支持目前未觀察到它的結論」。
- **易混拆解到位**。例：08「電場為零的位置不一定電位為零；電位為零的位置也不一定電場為零」、「電容 C 由幾何與材料決定，不等於已儲存的電荷 Q」；10「磁通密度 B 單位 T，磁通量 Φ 單位 Wb」、「自感 L 是物理量，電感器是元件」、「有很大的靜態磁通不保證有感應電壓」。
- **每個公式都附符號與 SI 單位**，抽查 08、10 全段落無漏；量綱標註正確（如 `∮ E·dA` 兩側同為 V·m、`∇·B` 左側 T/m）。

**發現問題 B（見第二節）：`08-靜電學.html` 全章 0 個術語英文全名。**

字典與自我檢核契約：

| 對象 | spec | 實作 | 結果 |
| --- | --- | --- | --- |
| `名詞與概念字典.html` 卡片數 | 57（20／18／19） | 57（**20／18／19**） | 通過 |
| 每卡四段（白話／位置或角色／機制／不要混淆） | 固定順序 | 57 張全齊，缺段 0 | 通過 |
| `term-search`、`term-count`、3 個 `.term-list`、`aria-current` | — | 全部具備；57 張皆有 `data-search` | 通過 |
| `自我檢核.html` | 44 題＋`quiz-reset`＋`quiz-progress`＋16 個章節 `h2` | 全數具備 | 通過 |
| `index.html` | 18 張 `chapter-card`、9 個 `flow` span、邊界表 ≥5 列、四份來源檔名純文字 | 18、9、8 列、4 份齊全；`.md` 連結 0 | 通過 |

### 1.8 字元級錯字掃描

方法：opencc `s2tw` 詞組感知轉換後逐字元比對 ＋ 三百餘個高信度簡體字全庫 grep ＋ 高頻物理術語形近錯字 grep ＋ 罕見字（出現 ≤2 次者共 274 字）逐一人工核對上下文。

| 類別 | 結果 |
| --- | --- |
| 簡體字 | **0 筆**（兩種方法交叉驗證；opencc 提示的「布、只、污、扣、系、祕、面、干、峰、范」經上下文核對皆為台灣標準用字，屬誤報） |
| 物理術語形近錯字 | **0 筆**。39 個指定術語正確寫法共出現 796 次且用字一致（「都卜勒」32 次、無「都普勒」混用；「冷次定律」「白努利方程」「司乃耳定律」逐一核對正確）。高風險組合（斂／旂、貫性、澆射、共震、駐坡、能際、彈牲、光電效鷹…）全部 0 命中 |
| 繁體用字與異體字 | **0 筆**。「線圈」5 對「綫圈」0；「裡」31 對「裏」0；「著」8 對「着」0；「台」3 處皆為量詞正確用法；康熙部首 U+2F00–2FDF 與 CJK 相容表意 U+F900–FAFF 全庫 0 命中 |
| 標點全半形混用 | **0 筆**（半形 `,.;:!?` 緊鄰 CJK 之模式全庫 0 命中） |

### 1.9 瀏覽器實測

`python -m http.server 8731` ＋ Playwright（既有版本，`channel:'chrome'`，未下載任何工具），全 19 頁 × 2 視窗：

| 視窗 | console error | 外部請求 | 橫向溢位頁 |
| --- | ---: | ---: | ---: |
| 桌面 1440×900 | **0**（見下註） | **0** | **0**（19 頁 `scrollWidth` 皆等於 `clientWidth` 1440） |
| 手機 390×844 | **0** | **0** | **0**（19 頁皆 390 對 390） |

> 註：桌面首頁曾記到 1 筆 404 console error。追查 `server.log` 與逐請求日誌確認為 **Chrome 自動索取 `/favicon.ico`**（頁面自身僅發出 `index.html`、`styles.css`、`app.js` 三個請求），非頁面宣告的資源，**不列為缺陷**。

`aria-live="polite"` 已在 60 個 output 上齊備；`<label for>` 覆蓋率 100%，鍵盤可及性具備基礎條件。

### 1.10 正文字數（spec §0.9 硬門檻 2000–3000 中文字）

以 `<main>` 內、扣除 `section.workspace`、去除 HTML 標籤後計數：

| 章 | 僅漢字 (U+4E00–9FFF) | 漢字＋全形標點 | 章 | 僅漢字 | 漢字＋全形標點 |
| --- | ---: | ---: | --- | ---: | ---: |
| 00 | 2094 | 2351 | 08 | 2032 | 2332 |
| **01** | **1992** | 2287 | **09** | **1972** | 2248 |
| 02 | 2092 | 2359 | 10 | 2040 | 2331 |
| 03 | 2070 | 2386 | 11 | 2020 | 2320 |
| **04** | **1990** | 2219 | 12 | 2016 | 2269 |
| **05** | **1986** | 2241 | **13** | **1963** | 2221 |
| **06** | **1972** | 2251 | **14** | **1985** | 2244 |
| **07** | **1993** | 2243 | **15** | **1968** | 2207 |

- **嚴格計法（僅漢字）**：9 章低於 2000，分別短少 7–37 字（**0.4 %–1.9 %**）。
- **寬鬆計法（漢字＋全形標點，中文「字數」慣例）**：16 章全數 ≥ 2207，**全部通過**。
- spec §0.9 未定義計數方式，故**列為問題 A，交 Lead 裁決**。

---

## 二、問題清單

| 編號 | 嚴重度 | 位置 | 問題 | 建議 |
| --- | --- | --- | --- | --- |
| **A** | 中 | 01、04、05、06、07、09、13、14、15 共 9 章 | spec §0.9「每章正文 2000–3000 中文字」為硬門檻，但**未定義計數方式**。以嚴格漢字計法，這 9 章為 1963–1993，短少 0.4 %–1.9 %；以「漢字＋全形標點」計法則全數通過（≥2207）。 | 由 Lead 裁決計數定義。若採嚴格計法，9 章各補 1–3 句即可達標（純新增，不動 widget 區塊）；若採寬鬆計法，建議把定義寫回 spec §0.9 以免下次量產再爭議。 |
| **B** | 中 | `08-靜電學.html` | **全章 0 個術語英文全名**，違反 spec §2.1「術語首次出現寫 `繁中（English, 縮寫）`」與 QUALITY-GATES §2 零基礎加驗「術語首次出現具繁中、英文全名與必要縮寫」。該章首現的 庫倫定律、高斯定律、電場、電位、電容、介電質、點電荷、電通量 均無英文。**其餘 15 章皆有**（每章 1 個以上，13 章多達 5 個以上）。 | 字串級補寫，`名詞與概念字典.html` 已備妥標準譯名可直接取用：庫倫定律（Coulomb's law）、高斯定律（Gauss's law）、電場（electric field）、電位（electric potential）、電容（capacitance）、介電質（dielectric）。Lead 可用 sed 級修正。 |
| **C** | 低 | `BUILD-SPEC.md` | 1217 行，微超「spec 類 md 1200 行」參考線 17 行（1.4 %）。 | **已知且已裁決**（Lead 裁定維持單檔），僅記載。 |
| **D** | 低 | `app.js` `interfaceWave` doppler 分支 | 觀察者以波速遠離（`if-vobs` 等於負的 `if-vwave`）時輸出 `f′ = 0.000000 Hz`，數學正確但**缺少判準說明句**（「觀察者以波速遠離，波追不上，收不到任何波前」）。同函式的 `v源 ≥ v波` 分支則有完整警語，兩個邊界處理密度不一致。 | 建議補一句因果，與 `v源 ≥ v波` 分支對齊。非阻擋項。 |
| **E** | 資訊 | `.github/workflows/pages.yml`、主站入口 | **部署尚未接線**：`pages.yml` 課程表無 `共通基礎/物理/互動課程|general-physics`，亦無 `general-physics` 的 `replacements` 規則（本課含 `../../數學/互動課程/` 跨課連結，**需要**該規則）；主站入口與來源 README 亦未加。 | PROJECT-BRIEF 第 10 節已明列此為 **Lead 收尾工作，不由 builder 執行**，故**不計為 builder 缺陷**，僅提醒交付前完成。另建議一併把本課登記進 `sync-styles.py` 的 `FAMILY` 清單。 |

**未執行項目：無。** 交辦的 8 大檢查項全部實際執行並留存證據。

---

## 三、拆檔量測（固定項）

| 檔案 | 行數 | 位元組 | 參考線 | 判定 |
| --- | ---: | ---: | --- | --- |
| `00-物理地圖.html` | 68 | 9,908 | HTML 1000 行 | 未超標 |
| `01-運動學.html` | 68 | 11,535 | HTML 1000 行 | 未超標 |
| `02-牛頓運動定律.html` | 67 | 11,058 | HTML 1000 行 | 未超標 |
| `03-功與能量.html` | 70 | 11,768 | HTML 1000 行 | 未超標 |
| `04-動量與碰撞.html` | 71 | 11,736 | HTML 1000 行 | 未超標 |
| `05-轉動與角動量.html` | 72 | 11,557 | HTML 1000 行 | 未超標 |
| `06-簡諧運動與共振.html` | 73 | 11,807 | HTML 1000 行 | 未超標 |
| `07-熱學與流體.html` | 72 | 11,917 | HTML 1000 行 | 未超標 |
| `08-靜電學.html` | 70 | 11,993 | HTML 1000 行 | 未超標 |
| `09-電流與電路基礎.html` | 70 | 11,384 | HTML 1000 行 | 未超標 |
| `10-磁場與電磁感應.html` | 72 | 12,188 | HTML 1000 行 | 未超標 |
| `11-馬克士威與電磁波.html` | 72 | 12,858 | HTML 1000 行 | 未超標 |
| `12-波動與駐波.html` | 69 | 11,142 | HTML 1000 行 | 未超標 |
| `13-反射折射與都卜勒.html` | 73 | 12,193 | HTML 1000 行 | 未超標 |
| `14-幾何光學與波動光學.html` | 75 | 12,394 | HTML 1000 行 | 未超標 |
| `15-量子與半導體.html` | 69 | 11,436 | HTML 1000 行 | 未超標 |
| `index.html` | 47 | 7,325 | HTML 1000 行 | 未超標 |
| `名詞與概念字典.html` | 82 | 43,516 | HTML 1000 行 | 未超標 |
| `自我檢核.html` | 106 | 19,727 | HTML 1000 行 | 未超標 |
| `app.js` | 754 | 58,061 | app.js 2000 行 | 未超標 |
| `styles.css` | 14 | 4,141 | 正本複本，不得改 | — |
| `BUILD-SPEC.md` | **1217** | 167,695 | spec 類 md 1200 行 | **超標 17 行**（問題 C，已裁決維持單檔） |
| `PROJECT-BRIEF.md` | 295 | 26,908 | spec 類 md 1200 行 | 未超標 |

> HTML 皆為每行一段的長行格式，故行數遠低於參考線；位元組量最大者為 `名詞與概念字典.html`（43.5 KB，57 張卡）。以位元組看無任何檔案接近需拆檔的規模。

---

## 四、允許交付與否（首輪判定）

**首輪結論：有條件允許交付。**

- **可交付的依據**：21 檔齊全；id 契約零偏差；44 題答案與 31 組數值答案獨立重算全數正確；spec §8.4 的 8 組交叉驗算全數相符；`styles.css` 與正本位元組相同且 `sync-styles.py --check` exit 0；LaTeX、外部資源、編碼、未知 class、`Math.random` 全為 0；pager 與相對連結完整；桌面與手機 19 頁 console error 0、外部請求 0、橫向溢位 0；字元級錯字 0；5 章內容親手重算無誤、無杜撰、無公式走樣；12 組 widget 邊界無 NaN、Infinity、undefined 洩漏且皆含判準與因果句。
- **交付前建議處理**：問題 **B**（08 章術語英文全名，字串級修正）與問題 **A**（§0.9 字數計法裁決）。兩者皆不影響功能與正確性。
- **交付前必須完成（Lead 收尾）**：問題 **E** 的部署接線——`pages.yml` 課程表加 `共通基礎/物理/互動課程|general-physics`，並**務必**為 `general-physics` 加上 `"../../數學/互動課程/": "../engineering-math/"` 的 `replacements` 規則（本課有 14 條跨課相對連結，缺此規則線上會全部連壞）。
- 問題 **C**（spec 1217 行）與 **D**（doppler 邊界說明句）為低優先，可延後。

---

## 五、複驗（2026-08-27）

**觸發**：Lead 裁決 A、建置指揮官以 focused codex pass 修復 B 與 D、另一線完成 E 掛站。本節為**獨立增量複驗**，builder 自稱（含「workspace 區塊位元組級不變」）**一律不予採信**，全部以驗收員自己的指令重驗。

### 5.0 變動面確認（先界定複驗範圍）

以檔案大小與 mtime 逐檔比對首輪紀錄，**僅 2 個產品檔變動**：

| 檔案 | 首輪 | 複驗 | 差 |
| --- | ---: | ---: | ---: |
| `08-靜電學.html` | 11,993 B／70 行 | 12,212 B／70 行 | **+219 B** |
| `app.js` | 58,061 B／754 行 | 58,456 B／755 行 | **+395 B／+1 行** |
| 其餘 19 個產品檔 | — | — | **位元組與 mtime 完全未變**（皆停在 14:22／14:35／13:18／13:21） |

無夾帶修改、無波及其他章節。

### 5.1 問題 B 複驗 — `08-靜電學.html` 補術語英文全名

| 檢查 | 結果 |
| --- | --- |
| **章內術語英文全名數** | 首輪 **0** → 複驗 **10**：`electric field`、`electric potential`、`capacitance`、`Coulomb's law`、`superposition principle`、`Gauss's law`、`electric flux`、`relative permittivity`、`electric potential energy`、`dielectric`（拼寫逐字核對正確，含所有格撇號） |
| **首輪點名的 8 個首現術語** | 庫倫定律（Coulomb's law）、高斯定律（Gauss's law）、電場（electric field）、電位（electric potential）、電容（capacitance）、介電質（dielectric）、電通量（electric flux） **7/8 已補**；另主動多補 3 個（疊加原理、相對介電常數、電位能） |
| **殘留（不阻擋）** | 「點電荷」仍無英文（point charge）。惟該詞**不在 spec §5.2 的 57 詞字典清單內**，非指定術語，且該章其餘所有字典詞皆已具英文 → **視為已關閉**，僅記載 |
| **workspace 區塊位元組級比對** | **確認未被動到**。以首輪 Read 逐字留存的原始區塊（11 行）與現檔擷取比對：兩者 **1,002 B（LF 正規化後）、sha256 `0ed8410daa7cc560b594a5b79d1d3d93…` 完全相同**，`diff` 無任何差異。builder 的宣稱**經獨立驗證屬實** |
| **id 契約複掃** | 5 個 `<input>`（`es-q1`／`es-q2`／`es-d`／`es-x`／`es-er`）屬性逐一比對 **與 spec 完全相同**；`coulomb-output` 具 `class="output" aria-live="polite"`；`<label for>` 全覆蓋 |
| **編碼複掃** | UTF-8 無 BOM、CRLF、U+FFFD 0、C1 0、PUA 0 |
| **LaTeX／`$`** | `\(`、`\)`、`\[`、`\]`、`$`、`\frac`、`\mathbf` **全 0** |
| **外部資源** | 唯一 URL 為 `https://justty32.github.io/engineer_study/`；`<style>` 0、inline `style=` 0、未知 class 0；`script`／`link` 僅 `app.js`／`styles.css` |
| **教學結構未回歸** | 七段骨架齊全且順序正確；workspace 序位（數值實例後、不要混淆前）正確且僅 1 個；「不要混淆」6 條；證據三段 3/3；「比喻到此為止」在；來源標註 1 處；`prereq` 在；pager 與跨課連結 `../../數學/互動課程/11-PDE與向量分析.html` 未變 |
| **數值未被動到** | 首輪已親手重算的 10 個數值（`1.497925`、`1997.233731`、`1198.340238`、`3195.573969`、`299.585060`、`179.751036`、`119.834024`、`1.330948`、`88.541878`、`6.375015`）與零場點二次式 `2x² − 3x + 0.45` **全部原樣仍在** |
| **錯字複掃（opencc s2tw 逐字元）** | **差異 0 筆**，新增文字未引入簡體字或異體字；半形標點夾中文 0 筆 |

**問題 B：關閉。**

### 5.2 問題 D 複驗 — `interfaceWave` doppler `f′ = 0` 分支

修復實作為在 doppler 分支加上 `if(vwave+vobs<=0)` 守衛並附因果句。以 node 假 DOM 實測**觸發**與**不誤觸**兩側：

| 情形 | `vwave+vobs` | 是否出現「波前永遠追不上他」句 | 判定 |
| --- | ---: | --- | --- |
| `vobs=−100, vwave=100`（UI 可達的邊界） | 0 | **出現**，且明說「這不是公式失效，是『跑得和波一樣快就看不到波在振動』的極限情形——與 v源 ≥ v 那種波前疊出震波的失效機制正好相反」 | 正確觸發 |
| `vobs=−90, vwave=100` | 10 | 未出現（f′ = 100.000000 Hz） | **不誤觸** |
| `vobs=−100, vwave=110` | 10 | 未出現（f′ = 90.909091 Hz） | **不誤觸** |
| `vobs=0, vwave=100` | 100 | 未出現，改出現「兩邊都靜止，沒有頻移」 | **不誤觸** |
| 兩邊靜止 `vwave=340` | 340 | 未出現，改出現「兩邊都靜止，沒有頻移」 | **不誤觸** |

- **控制範圍分析**：`if-vwave ∈ [100,1500]`、`if-vobs ∈ [−100,100]`，故 `vwave+vobs ≥ 0` 恆成立，**永遠不會出現負頻率**；`<=0` 守衛在 UI 內恰好只在等號成立時觸發，邏輯與範圍相容。
- **舊分支未回歸**：`vsrc ≥ vwave` 仍正確顯示「這個公式失效…馬赫錐」警語。
- **spec 預設未回歸**：`vsrc=30, vobs=0, vwave=340, f0=1000` → **1096.774194 Hz**，與 spec §4／q13-3 一致。
- 上述 7 組輸出 **NaN／Infinity／undefined 洩漏皆為 0**。

`node --check app.js` **語法 OK**。`app.js` 全域複掃：UTF-8 無 BOM、CRLF、U+FFFD／C1／PUA 全 0；`Math.random` 0、`new Date`／`Date.now` 0、`setTimeout`／`setInterval` 0；`fetch(`／`XMLHttpRequest` 0；外部 URL 0；18 個函式與 16 個常數宣告齊全。opencc s2tw 差異 3 筆（「干擾」「這台熱機」「電荷分布」）經上下文核對**皆為台灣標準用字之誤報**，且位於未變動的既有文字。

**問題 D：關閉。**

### 5.3 無回歸抽測（§8.4）

以假 DOM 重跑 3 組並與 spec 逐字比對：

| 組 | 驗算 | 結果 |
| --- | --- | --- |
| §8.4-25 `coulomb` 預設 | `F=1.497925e−6 N`（吸引）、`Ex=3195.573969 V/m`、`V=119.834024 V`、零場點 `1.330948 m` | **相符** |
| §8.4-20 `projectile` 預設 | `T=2.886150`、`R=40.816327`、`H=10.204082`、`v_y=4.342136`、速率 `14.793720`、`v²/2+gy=200.000000`、落地速率 `20.000000` | **相符** |
| §8.4-27 `optics` lens 預設 | `si=15.000000 cm`、`M=−0.500000`、判讀「so > 2f：倒立縮小實像」 | **相符** |

全站複掃亦無回歸：題庫 44 題（31 num ＋ 13 sel）與 spec 差異 **0 筆**、31 題數值答案獨立重算**全數相符**；id 契約 **屬性不符 0 筆**、`label for` 違規 0；pager 鏈完整；相對連結缺檔 0；LaTeX／外部資源／未知 class／inline style 全 0；`styles.css` 與正本 `cmp` 無差異且 `sync-styles.py --check` **exit 0**。

### 5.4 問題 A 複驗 — 字數（Lead 裁定採「漢字＋全形標點」）

依裁定計法重算 16 章：**全部落在 2207–2386，16/16 通過，FAIL 0 章**（含補字後的 08 章 2352）。無需加厚。

> 記載：嚴格漢字計法下的數字仍保留於 §1.10 供追溯；建議 Lead 把「漢字＋全形標點」的定義補回 spec §0.9，避免下次量產再起爭議。

### 5.5 問題 E 複驗 — 掛站接線（唯讀確認）

| 項目 | 結果 |
| --- | --- |
| `pages.yml` 課程表列 | **已加**：第 119 行 `共通基礎/物理/互動課程|general-physics` |
| `general-physics` replacements 規則 | **已加**且映射正確：`"../../數學/互動課程/": "../engineering-math/"` ——本課 14 條跨課連結線上可正確解析 |
| 主站入口 | **已加**：`互動學習網站/index.html` 第 160 行 `<a class="topic-row topic-row-active" href="./general-physics/">` |
| 來源 README | **已加**：`共通基礎/README.md` 第 28 行含互動課程連結與線上版 URL |

**問題 E：關閉。**

### 5.6 瀏覽器實測複驗

`python -m http.server 8732` ＋ 既有 Playwright（`channel:'chrome'`），全 19 頁 × 2 視窗重跑：

| 視窗 | console error | 外部請求 | 橫向溢位頁 |
| --- | ---: | ---: | ---: |
| 桌面 1440×900 | **0**（唯一 1 筆 404 仍為 Chrome 自動索取 `/favicon.ico`，非頁面資源，同首輪認定） | **0** | **0** |
| 手機 390×844 | **0** | **0** | **0** |

變動最大的 `08-靜電學.html` 在兩個視窗皆 `err=0 ext=0`、`scrollWidth` 等於 `clientWidth`（1440／390），**無版面回歸**。

### 5.7 複驗後問題狀態

| 編號 | 首輪狀態 | 複驗後 |
| --- | --- | --- |
| **A** 字數門檻 | 待 Lead 裁決 | **關閉**（裁定採漢字＋全形標點，16/16 通過） |
| **B** 08 章英文全名 | 中 | **關閉**（補 10 個；workspace 經 sha256 證實未動；殘留「點電荷」非指定術語，不阻擋） |
| **C** spec 1217 行 | 低（已裁決） | **維持已裁決**，不影響交付 |
| **D** doppler 邊界句 | 低 | **關閉**（觸發與 5 組不誤觸情形全部驗證） |
| **E** 掛站接線 | 資訊 | **關閉**（課程表＋replacements＋主站入口＋README 齊備） |

**複驗新發現：無新缺陷。** 唯一殘留為「點電荷」未附英文，屬非指定術語，記載不阻擋。

---

## 六、最終判定

**允許交付。**

首輪開立的 A、B、D、E 全部經獨立重驗關閉，C 為已裁決之低優先項；複驗未發現任何新缺陷，亦未發現任何回歸。修復僅動 2 個檔案（`08-靜電學.html` +219 B、`app.js` +395 B），其餘 19 個產品檔位元組未變；`08-靜電學.html` 的 `workspace` 互動區塊經 **sha256 位元組級比對證實完全未被動到**，首輪已重算的 10 個數值原樣保留。

全課現況：21 檔齊全、id 契約零偏差、44 題答案與 31 組數值獨立重算全對、spec §8.4 八組交叉驗算全中、`styles.css` 與正本位元組相同且家族閘門 exit 0、LaTeX／外部資源／編碼／未知 class／`Math.random` 全 0、pager 與相對連結完整、16 章字數全數達標、字元級錯字 0、桌面與手機 19 頁 console error 0／外部請求 0／橫向溢位 0、widget 邊界無非有限值洩漏且皆含判準與因果句、掛站接線齊備。
