# 驗收紀錄

測試日期：2026-08-14
環境：Windows PowerShell；靜態檔案檢查，不啟動伺服器。

## 已通過

- `node --check app.js`：通過，無 JavaScript 語法錯誤。
- 靜態掃描 10 個 HTML：所有 `href`／`src` 指向存在的本機檔案，且每頁都有上一頁／下一頁導覽。
- 全目錄 16 個檔案以嚴格 UTF-8 解碼：通過。
- 外部資產掃描：未發現 `http://`、`https://` 或協定相對資源。
- 人工內容檢查：具有 00 起點、8 站主線、可搜尋字典、即時無計分互動、前置連結、失敗邊界與證據說明。

## 未執行項目與限制

- 未啟動本機伺服器。
- Lead 於 2026-08-14 再次執行 JavaScript、嚴格 UTF-8、`zh-Hant`、相對連結與外部資產整合掃描，結果通過。
- 本機瀏覽器控制介面因環境層連線失敗，未驗證 1440×900 與 390×844 版面、鍵盤操作、焦點順序或主控台錯誤。
- 互動是協定教學模型，不會送出真實封包，也不取代正式標準或封包分析工具。

是否允許交付：允許；靜態關卡已通過，視覺與實際瀏覽器互動列為已知待驗限制。

## GitHub Pages 正式發布

- 正式網址：<https://justty32.github.io/engineer_study/network-protocols/>
- 發布版本：`2f3787a6a2b8f3b46bd94c4031515b30c6783b16`。
- GitHub Actions 工作流程 `31773408571`：執行完成且結果為成功。
- 線上首頁及其實際引用的 CSS、JavaScript：HTTP 200。
- 手機與桌面實機視覺、鍵盤焦點及瀏覽器主控台仍未人工驗收。

## 2026-08-17 修補驗收

- HTTP 資源路徑改以 `textContent` 與文字節點輸出，不再把使用者輸入解析成 HTML。
- 以 `<img src=x onerror=alert(1)>` 作為回歸輸入，確認輸出保留完整字面文字且不建立 HTML 元素；`node --check app.js` 通過。
