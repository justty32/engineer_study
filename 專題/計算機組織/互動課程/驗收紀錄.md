# 驗收紀錄

## 已通過的靜態驗收

- 共 16 個 HTML：首頁、14 章與可搜尋字典；頁尾依賴鏈可完整前後走訪。
- `node --check app.js` 通過。
- 22 個課程檔案以嚴格 UTF-8 解碼通過；所有 HTML 具 `zh-Hant` 與 UTF-8 宣告。
- 所有相對 `href`／`src` 目標存在；無遠端資產、圖片、內嵌框架或表單提交。
- 需要即時計算的互動控制與輸出 ID 均存在；另外以原生 `details` 提供不需 JavaScript 的手工追蹤。
- 首頁與章節明確標示理想邏輯、教學指令、示意延遲、示意 ABI 與真實平台之模型邊界。
- x86-64、AArch64 與 RISC-V 各自獨立成章，沒有共用錯誤的暫存器、語法或呼叫慣例。
- 獨立技術稽核確認 CMOS NAND、時序邏輯、三套指令集、ABI、符號、重定位與連結核心因果正確；並已修正 AArch64 旗標、映像載入／動態連結／執行期責任、理想 CMOS 漏電措辭與整合式組譯器邊界。

## 尚待人工瀏覽器驗收

- 桌面 `1440×900` 與手機 `390×844` 視覺、鍵盤焦點、瀏覽器主控台及真實互動。
- 本次瀏覽器控制介面無法建立連線，已把人工驗證項目寫入 `wf/WAIT_USER.md`。

交付判定：靜態內容與連結允許交付；視覺驗收待使用者確認。

## GitHub Pages 正式發布

- 正式網址：<https://justty32.github.io/engineer_study/computer-organization/>
- 發布版本：`2f3787a6a2b8f3b46bd94c4031515b30c6783b16`。
- GitHub Actions 工作流程 `31773408571`：執行完成且結果為成功。
- 線上首頁及其實際引用的 CSS、JavaScript：HTTP 200。
- 手機與桌面實機視覺、鍵盤焦點及瀏覽器主控台仍未人工驗收。
