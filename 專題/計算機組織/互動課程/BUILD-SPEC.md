# 建置規格

- 所有頁面相對載入 `styles.css`；需要即時計算的頁面再載入 `app.js`，其餘以原生 `details` 提供無腳本手工追蹤。
- 每頁具首頁、00 起點、字典、上一頁與下一頁；13 接到逆向工程。
- 所有控制即時更新、無提交、分數、倒數或答對解鎖。
- 互動：邏輯合成、加法、時序狀態、指令週期、三套組合語言追蹤、位元組序、記憶體階層、呼叫框架、編譯管線、符號／重定位、載入映像與跨層故障定位。
- 字典依繁中、英文、縮寫與白話即時篩選。
- 教學數值與模型必須標示簡化範圍，不把示意週期或位址當成真實平台保證。
