# 驗收紀錄 — 機器人數學基礎（互動課程）

- **測試日期**：2026-08-25
- **環境**：本機靜態伺服器 `python -m http.server 8099`；Playwright 1.62.1 驅動系統 Chrome（`channel: chrome`，未下載瀏覽器）；Node v24。

## 通過項

- **檔案完整**：index、00–12、名詞與概念字典、app.js、styles.css 皆存在；UTF-8。
- **語法**：`node --check app.js` 通過。
- **離線純淨**：全資料夾無 LaTeX 分隔符（`\( \) \[ \] $`）、無 MathJax/KaTeX、無任何 http(s) 外部資產（僅回主站連結）、app.js 無 `Math.random`／`Date.now`。
- **互動實測（13/13 通過）**：以 Playwright 逐頁載入，`.output` 初始即有內容，改變第一個控制項後 output 內容即時改變，且無 console error（頁 00 先前出現的一次 404 經查為瀏覽器對整個 origin 自動抓取 `/favicon.ico`，與內容無關，既有課程行為相同；以獨立 context 逐頁重測後 4xx 為 0）。
- **DOM 契約**：BUILD-SPEC 指定的每個 widget 控制 id 與 output id 都存在於對應頁，且在 app.js 有對應守衛函式與檔尾註冊。
- **版面**：桌面 1440×900 與手機 390×844 截圖檢視 index 與第 05 章，無文字截斷、無橫向溢出、無重疊；公式以純文字 Unicode 正常顯示。

## 已知限制

- 尚未把本課加入主站入口 `互動學習網站/index.html` 與部署對照（待與擁有者確認）。
- 四門機器人課尚未回頭連到本課對應章節（規劃於「3 倍擴充」階段一併加入）。

## 交付結論

核心內容、互動、離線性、版面均通過，允許交付作為機器人四門課的數學先修。
