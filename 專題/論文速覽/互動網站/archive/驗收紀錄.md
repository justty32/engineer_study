> 封存 2026-09-18：課程已上線、規格／驗收已定案；活文件不再引用本檔。

# 驗收紀錄

## 2026-08-28 假資料版（deck.json 尚未產出，內容為依 SCHEMA 造的 2 lanes × 2 卡 × all_papers 各 5 列）

測試環境：Linux（Manjaro）、Node v24.14.1、Python 3、系統既有 Google Chrome 152.0.7977.64（`--headless=new` + CDP，node 內建 WebSocket 驅動，未下載任何東西）、`python3 -m http.server 8765`。
驗證腳本（不在 repo 內）：paper_readings 側 scratchpad `verify/cdp_check.js`（主流程：三種 viewport 探針、互動、持久化、截圖）、`verify/extra_shots.js`（淺色模式、名詞白話、表格、index.html 截圖、reduced-motion 探針）、`verify/focus_probe.js`（真實 Tab 鍵焦點）。

### 已通過（皆有腳本輸出或截圖為證）

- `node --check app.js`：通過。
- 靜態掃描：兩個 HTML 與 `styles.css` 內無外部 `<script src>`／`<link>`／`@import`／`url()`；唯一的外站 URL 是 `index.html` 的主站連結與 JSON 資料裡的 arXiv／GitHub 連結（純 `href`，不載入資源）。`<link rel="icon" href="data:,">` 抑制 favicon 404。
- 執行期網路請求（CDP `Network.requestWillBeSent`，整段測試流程）：只有 `127.0.0.1:8765` 的 html/css/js；另有一筆 `chrome-extension://…/page-script.js` 來自本機 Chrome 的政策安裝擴充套件，非頁面發出（HTML 中無任何對應引用）。
- Console：`Runtime.exceptionThrown`／`console.error`／`Log.error` 全程 0 筆（含載入、互動、重載、三種 viewport）。
- 橫向溢出：320×640、390×844、1440×900 三種寬度 `documentElement.scrollWidth === clientWidth`；逐元素檢查超出視口右緣的元素為 0（排除刻意移出畫面的 `.skip-link` 與 `.table-wrap` 內部）；`.card` 內部 `scrollWidth ≤ clientWidth`。展開全部 `details.lane-all` 後頁面寬仍為 390，表格由 `.table-wrap{overflow-x:auto}` 收住（`wrapScrollable: true`）。
- 字級與行高：根字級 390px＝18px、1440px＝19px；`body` line-height 30.6px（1.7）。
- 渲染數量（假資料）：2 lanes、2 gist 卡、4 必讀卡、6 個 nav 卡、2 個計畫條項、3 條跨線索、10 列 all_papers（含 1 列 `summary_file: null` → 只連 arXiv）、3 個 `.card-plain`、1 個 `.lane-glossary`（4 條）、7 個行內名詞按鈕、12 個連結按鈕；所有 `target=_blank` 都有 `rel=noopener`（缺 noopener 為 0）。
- 拇指區：`.link-btn`／`.deep-toggle`／`.nav-btn`／`.plan-item`／`summary`／`.timer-btn`／`#theme-toggle` 高度 < 44px 的元素為 0；底列 `position: fixed`，翻頁鈕高 52px。
- 已讀：勾選 → 卡片 `.is-read`、文字「已讀 ✓」、進度「已讀 1/4」、剩餘由 15 → 12 分鐘、`localStorage['pb:symbolic-arc-automata:read']=["1911.01547"]`；重載後 checkbox 仍勾選、狀態仍在。
- 深讀：按下 → `.is-deep`、`aria-pressed=true`、文字「已標記深讀 ★」、「回家想深讀」清單出現 1 筆且空清單提示 `hidden`；重載後仍在；「清空清單」後清單 0 筆、提示恢復、卡片 `.is-deep` 全移除。
- 上一張／下一張：從 `gist-arc` → `card-1911.01547`，焦點移到該卡；鍵盤 `j` 前進、`ArrowLeft` 後退皆正確。
- 行內名詞：點 `button.term` → 之後插入 `span.term-pop`（含完整名詞與白話），`aria-expanded=true`；再點收回並移除。
- 計時器：按「開始 30 分鐘」→ 文字「停止計時」、鍵寫入；1.6 秒後顯示「已用 00:01／30:00」；再按 → 文字回「開始 30 分鐘」、顯示清空、鍵移除。
- 主題：自動模式下按鈕顯示目前狀態；強制深色後 `data-theme=dark`、`aria-pressed=true`、底色 rgb(16,25,28)／文字 rgb(230,240,240)；`prefers-color-scheme: light` 模擬下底色 rgb(244,247,246)／文字 rgb(23,50,56)；`pb:theme` 持久、重載後仍套用。
- 可及性：真實 Tab 鍵逐一停在 skip-link → deck 總覽 → 主題鈕 → 計畫條 → 計時鈕，每個都 `:focus-visible` 命中且 outline 為 3px 實線；`prefers-reduced-motion: reduce` 模擬下 `scroll-behavior: auto`、transition 0.01ms。
- `index.html`：390 與 1440 無溢出、1 列 deck 連到 `./symbolic-arc-automata.html`。
- 截圖（scratchpad `verify/run-fake/`）：`shot-390.png`、`shot-390-dark.png`、`shot-390-light.png`、`shot-390-light-term.png`、`shot-390-light-glossary.png`、`shot-390-light-table.png`、`shot-390-full.png`、`shot-1440.png`、`shot-index-390.png`、`shot-index-1440.png`，皆人工看過。

### 已知限制／待真資料再驗

- 目前頁面內嵌的是假資料；每張真卡是否都渲染、字數是否溢出，待 `deck.json` 灌入後重跑同一套腳本。
- 假資料的 `gist.open` 自帶「還沒解決：」，與契約強制的「還沒解決」標籤重複；真資料的 `open` 欄位不應再帶這四個字（灌入前檢查）。
- 未在實機手機驗證（僅 headless Chrome 模擬 390×844 mobile viewport）；`IntersectionObserver` 的手感與 iOS safe-area 待實機。
- `.term` 行內標記依 glossary `term` 做字串比對，只標每段第一次出現；同義詞或英文縮寫不會被標。

### GitHub Pages 登記（尚未部署）

- `.github/workflows/pages.yml`：`on.push.paths` 加三行、課程表加 `專題/論文速覽/互動網站|paper-briefs`。
- `互動學習網站/index.html`：第 30 列「論文速覽」→ `./paper-briefs/`；主題數 29 → 30。
- `README.md`：互動課程段加「專題 — 論文速覽」一行（本地＋線上 slug）。
- 尚未 commit／push，線上 URL 與 workflow run 待部署後補記。

是否允許交付：假資料版允許（靜態關卡與 headless 互動關卡全過）；正式交付待真資料灌入後重驗。

## 2026-08-28 真資料版（`briefs/deck.json`：4 lanes × 4 卡、all_papers 150 列、每線 glossary 7～8 條、每卡 plain）

環境同上（Google Chrome 152 headless + CDP，`python3 -m http.server 8765`）。腳本：`verify/real_probe.js`（四種 viewport／主題 × 基礎探針＋「全部 details 展開、全部名詞點開」探針＋進度數學）、`verify/spot2.js`（定點截圖）、`verify/termkey.js`／`spacekey.js`（名詞鍵盤）。

### 灌入
- `python3 inline_deck.py briefs/deck.json`：lanes=4、highlights=16、all_papers=150；以 Python 重新解析頁面內嵌 JSON，與 `deck.json` 逐位相等。
- 資料層檢查：四線 `gist.open` 皆不帶「還沒解決」前綴；lane 分鐘 7/7/8/7（合計 29，`total_minutes` 30 只用於計時器）；`summary_file: null` 的列 0 筆（150 列全連 summarize）；每卡三個連結俱全且 `links.arxiv` 與 `arxiv_id` 一致。欄位最長：title_zh 18、title_en 104、one_liner 50、plain 79、core 110、why 53、number.value 14、glossary.term 30。

### 已通過（皆有 `run-real/real-report.json` 或截圖為證）
- 16 張必讀卡全部渲染（每線 4 張，序號 1/4～4/4；`主線 i／4`；分鐘／篇數與資料一致；`深讀綜述` href = `deep_doc`）；nav 卡 20 張（4 全景 + 16 必讀）；計畫條 4 項；跨線索 3 條、引用膠囊 15 個。
- 16 張卡的 arXiv／摘要／翻譯三個 href 與 `links` 逐字相等；150 列摺疊清單（25/33/65/27）每列 href 依規則（`summary_file` → GitHub summarize；null → arXiv）逐列比對無誤。
- 名詞白話：四線 8/7/8/8 條；行內名詞 55 個；全部點開後 55 個 `.term-pop`、`aria-expanded=true` 55；點開狀態下 320／390／1440 頁面仍無橫向溢出（`scrollWidth === clientWidth`），pop 內無溢出（320px 有 1 個 pop 內部寬 2px 差，肉眼不可見、頁面不溢出）。
- 全部 `details`（4 個表格＋4 個名詞白話）展開後：頁面寬度不變，四個 `.table-wrap` 皆可橫向捲動。
- 標題／chip：`.card-title-zh`、`.card-title-en`、`.chip-*`、`.tag-role`、`.card-seq`、`.card-year` 無一被 `text-overflow`／`line-clamp` 截斷、無 scrollWidth 超出；最長題名「讓 LLM 寫 ASP，求解器改作業」「單層 SSM 模擬任意有限自動機」在 390px 折成兩行完整顯示（截圖 `real-card-*.png`）。
- 底列數學：全未讀「已讀 0/16・預估剩餘 29 分鐘」；ARC 四張全勾 → 「已讀 4/16・22 分鐘」（=7+8+7）、計畫條 ARC `4/4` 且 `.is-done`；再勾 SR 一張 → 「5/16・21 分鐘」（=⌈7×3/4⌉+8+7）；16 張全勾 → 「16/16・全部讀完 ✓」、四個計畫條皆 `.is-done`。
- console error／exception：0；執行期外部請求：0（同前，唯一的 `chrome-extension://…` 為本機 Chrome 擴充套件注入，非頁面發出）。
- 可點元素 < 40px 高：0（`.thread-ref` 已由 32px 提到 44px）。
- 行內名詞改為 `span[role=button][tabindex=0]`（`<button>` 在 Chrome 是 inline-block，長名詞會整塊掉行留缺口，截圖對照 `real-card-2604.27960.png`）：Tab 可停、`:focus-visible` 3px 外框、Enter 展開、Space 收回且不捲動頁面（body 上 Space 仍正常捲 728px）。
- 截圖（`verify/run-real/`，五張主控用的副本在 scratchpad `screenshots/`）：`real-390-light.png`、`real-390-dark.png`、`real-390-glossary.png`、`real-card-2604.27960.png`（行內名詞跨行）、`real-390-term.png`（名詞展開）、`real-390-table.png`（nesy 65 篇表格）、`real-390-threads.png`、`real-390-footer.png`、`real-390-dark-card.png`、`real-390-full.png`（全頁 22101px）。

### 本輪修正
- `styles.css`：`.thread-ref` min-height 32→44px；`.term` 底線改用 `text-decoration`（原 border 在 inline-block 上偏低）。
- `app.js`：`.term` 由 `<button>` 改為 `span[role=button][tabindex=0]`，`#lanes` 委派加 Enter／Space。README DOM 契約同步。

### 已知限制
- 未在實機手機驗證；`IntersectionObserver` 手感、iOS safe-area 待實機。
- 320px 下一個 `.term-pop` 內部有 2px 寬度差（NCA 條目），不影響頁面。
- 尚未 commit／push；線上 URL、workflow run 待部署後補記。

是否允許交付：允許（真資料全部渲染、無溢出、無 console error、無外部請求、狀態可保存、登記完成）。

## 2026-09-18 第二輪：同步到最新索引＋逐 lane 品質過一遍（627 條）

範圍：paper_readings 入庫 batch12／13 後索引 597 → 627（論文 613 ＋ 訪談 14；batch14 未開，索引到此為止）。五個 deck 全部同步；18 條 lane 各派一條 codex gpt-sol 品質線（每卡 one_liner／plain／core／why／number／role／year、全景卡 gist、glossary、all_papers 逐列對 index），5 個 deck 各派一條 deck 層線（subtitle／intro／threads／outro 對齊改後 lanes）；5 條 gpt-sol 唯讀 QA 線抽核；headless Chrome 探針由 sonnet 子 agent 重做。工作區與各線交接書、CHANGES、QA.md、探針腳本與截圖都在 scratch（`teams/B/r2/`），不進 repo。

### 同步（機械，領導腳本）
- 差集：index 全部 ID（含 video.md）− 五 deck `all_papers` 聯集 = 30 篇（batch12／13），依類別塞進 10 條 lane：sr +1（C）、open-ended +1（D）、gp-library +4（G）、skill-library +2（01z）、constrained-decoding +4（03）、self-improve-loop +7（04）、workflow-calibration +2（06）、nesy-binary +1（07）、architecture-memory +6（09）、world-models +2（10）。
- 同步後：`python3 data/validate_deck.py data/*.json` 五 deck errors=0、warnings=0；全站 all_papers 聯集 627 ＝ index 627，重複 0、缺 0（`diff.py` 以 index 各檔正則逐列解析後比對）。
- 各 deck 篇數：symbolic-arc-automata 151、evolution-program-synthesis 101、agent-skills-cost-decoding 128、agent-loop-workflow-memory 193、nesy-binary-worldmodel-ndea 54。`index.html`（總數與五列篇數）、README 頁面地圖與不變式、SCHEMA 不變式、根 README、`互動學習網站/index.html` 那列的數字同步改為 627。

### 換卡（每張理由記在該 lane 的 CHANGES.md）
- gp-library：2211.16605 → 2609.05093（Discovery Loop：可執行程式演化＋獨立驗證）。
- open-ended：2512.16406 → 2608.30983（Language-Driven Quality-Diversity）。
- constrained-decoding：2403.01632 SynCode → 2609.01354（Where the Verifier Fails：驗證器本身會誤判）。
- architecture-memory：2308.08155 AutoGen → 2609.00243（Invalidation Contracts：跨回合記憶失效治理）。
- nesy-binary：2502.01680 → 2609.05369（神經符號程序推理 for 長時程 VLA）。
- world-models：2502.04728 → 2609.10540（Programmable World Model）。
- 其餘 12 條 lane 判斷新篇未比現有 4 張更該先讀（理由在各 CHANGES.md），不換。

### 品質線改動量（HEAD 對比，領導腳本統計）
- symbolic-arc-automata：既有 16 卡全數改寫（number 改 12）、4 線 gist 全改、glossary 31 條（改寫 10）。
- evolution-program-synthesis：換 2、改 10／14（number 9）、gist 4／4、glossary 32→31（改 5）。
- agent-skills-cost-decoding：換 1、改 6／11（number 4）、gist 3／3、glossary 20→24（改 5）。
- agent-loop-workflow-memory：換 1、改 12／15（number 7）、gist 4／4、glossary 28→32（改 6）。
- nesy-binary-worldmodel-ndea：換 2、改 8／10（number 5）、gist 3／3、glossary 21 條（改 6）。
- 五個 deck 的 subtitle／intro／threads／outro 皆重寫；threads 引用的 paper_ids 全部是本 deck 必讀卡（原 symbolic-arc-automata 7 個失效引用已清）。
- 訪談卡 year：repo 內無發佈日期，改由 agy cli 上網查 YouTube 頁面 `publishDate`：ndea-02 Constrained Adaptive Rejection Sampling `https://www.youtube.com/watch?v=BwcuNBfHj2w` 2026-07-01；ndea-03 DreamCoder's Wake-Sleep Library Learning `https://www.youtube.com/watch?v=H5FKqIDq5cM` 2026-04-07；ndea-11 Symbolic World Models `https://www.youtube.com/watch?v=fCaLTOCiOsk` 2026-01-26；ndea-12 The Surprising Performance Drivers of HRM `https://www.youtube.com/watch?v=VJ8tekpMqBM` 2025-08-18。填入 `YYYY-MM`，SCHEMA 的 year 說明同步。

### QA 唯讀線（gpt-sol × 5，每 lane ≥2 張、多數全核；QA.md 在 scratch）
- 抽核 69 張卡：number 與 core／one_liner 的數字全部在對應 summarize 找到；year 全對；role 無同詞 >2 次；連結對應本機檔 100% 存在；all_papers `title_zh` 與 index 逐字相同。
- 標為「不通過」的 3 張（ToolLLM 2307.16789、ToolAlpaca 2306.05301、DSPy 2310.03714）：`links.summary` 指到的 `摘要.md` 是目錄頁，數字在其子檔（`摘要/01_….md`）；連結正確、數字有據，不改。
- 未採納的 QA 建議：（a）「gist.open 缺『還沒解決』前綴」——契約規定不帶，頁面加標籤；（b）「all_papers 缺 url 欄」——url 為可選；（c）glossary 未涵蓋 all_papers 索引題名內的縮寫——契約只要求卡片正文；（d）`4,715 題`、`(x, y)` 半形——前者是 index 原題照抄，後者是數學式。留給下輪的建議：arc 線可加一條「ARC／ARC-AGI」glossary。

### headless Chrome 探針（sonnet × 1；Google Chrome 153 `--headless=new` + CDP、`python3 -m http.server 8765`；report.json、REPORT.md、6 張截圖在 scratch）
- Console：五 deck 頁＋index，載入／6 種 viewport×主題／全部 details 展開＋全部 `.term` 點開／持久化互動＋4 次 reload，exception／console.error／Log error 合計 0。
- 執行期網路：deck 頁 76 筆、index 3 筆，全部 `127.0.0.1:8765` 的 html/css/js；外部請求 0。
- 橫向溢出：320×640、390×844、1440×900 × 淺／深色皆 `scrollWidth === clientWidth`；展開全部 details＋點開全部 `.term` 後 320／390 仍無溢出（首輪 320 出現 +16px 假象，確認為 headless 首次 viewport override 未 settle，暖機後消失並重驗）。
- 渲染數量 = JSON：lanes／`.card-paper`／`.plan-item`／`.thread`／all_papers 列／glossary 條／`.card-year` 非空數逐頁相等（151／101／128／193／54 列）。
- 連結逐列：卡片 `links.*`、627 列 all_papers href、5 條 `.lane-deep`、`rel=noopener`，不符 0。
- 持久化（symbolic-arc-automata，390）：已讀／深讀／清空／計時器／主題／上一張下一張／`j`／`k` 皆符合契約且 reload 後仍在。
- index.html：390／1440 無溢出、5 列 href 正確、每列篇數 = 該 deck JSON 合計。
- 其他：`node --check app.js` 通過；engineer_study `wf/tools/lint.sh`：wf 嚴格層 PASS，整庫 3 個 BROKEN 全在另一 session 未追蹤的 `電機/…互動課程/` 目錄，與本站無關。

是否允許交付：允許（同步完整、契約全過、QA 數字全有據、探針全過）。未在實機手機驗證（同前）。
