# 機器人路徑與運動規劃互動課程驗收紀錄

初次發布測試日期：2026-08-14
本次深度返工測試日期：2026-08-14
環境：Windows PowerShell、Node.js；靜態整合檢查。

## 已通過

- 共 10 個 HTML；16 個互動課程目錄檔案皆以 UTF-8 解碼。
- `node --check app.js` 通過。
- 本機 HTTP 伺服器實測首頁、代表章節、CSS 與 JavaScript：HTTP 200。
- 所有 HTML 具 `zh-Hant`、UTF-8、頁尾前後導覽；相對 `href`／`src` 全數存在。
- 無圖片、內嵌框架、表單提交或外部執行期資產；JavaScript 引用的 DOM ID 均存在。
- 間隙、取樣命中率與速度下界公式以獨立計算交叉檢查；A* 展開數是明確標示的教學指標，不冒充實測。
- 內容涵蓋路徑／軌跡邊界、組態空間、連續碰撞、圖搜尋、取樣規劃、時間參數化、七軸冗餘、重規劃與 C++ 證據。
- 新增 `00A-搜尋與圖論先修.html`，從節點、邊、成本、路線與連續空間離散化開始；已接入首頁、章節 pager，無孤兒頁。
- 深度返工後，`00`、`00A`、`01` 至 `06` 各頁去除標籤與空白後純文字皆達約 1100 字元；字典超過 5000 字元，且 25 個條目逐條具有白話、位置／角色、如何生效、具體例子、不要混淆五部分。
- 各主線章節均在互動之前提供白話問題、最小前置、逐步因果、數值例；並補上可觀察證據、常見誤解與適用邊界。

## 尚待人工瀏覽器驗收

- 桌面 `1440×900`、手機 `390×844`、鍵盤焦點、瀏覽器主控台與真實互動。
- 內建瀏覽器控制介面因環境層缺少必要設定而無法建立連線；不得視為已通過。

交付判定：靜態內容、連結、互動契約與公式允許發布；視覺驗收待使用者確認。

## GitHub Pages 正式發布

- 正式網址：<https://justty32.github.io/engineer_study/robot-motion-planning/>
- 發布版本：`8c610aca43f10a16b0994b9361e8fd249183d4ef`。
- GitHub Actions 工作流程 `31774916075`：完成且結果為成功。
- 線上 9 個 HTML、CSS 與 JavaScript 共 11 檔：全數 HTTP 200；跨課程連結改寫通過。
- 手機與桌面實機視覺、鍵盤焦點及瀏覽器主控台仍未人工驗收。

上述網址與工作流程紀錄對應返工前版本。2026-08-14 的深度返工內容尚待整體提交與 GitHub Pages 重新部署；不得以舊工作流程成功結果宣稱新內容已在線上驗收。

## 深度返工本機發布模擬

- 與運動學、視覺、學習三門課共同組裝成 GitHub Pages 目錄結構；跨課程連結已依正式網址規則改寫。
- 整批共 44 個 HTML、4 個 CSS、4 個 JavaScript，嚴格 UTF-8、相對連結與目標存在性檢查通過。
- 本機 HTTP 伺服器逐一請求 52 個公開檔案，全部回傳 HTTP 200。
