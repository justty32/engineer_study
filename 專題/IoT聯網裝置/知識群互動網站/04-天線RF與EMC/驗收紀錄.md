# IoT 天線、RF、EMC 與法規邊界互動網站驗收紀錄

← [README](README.md)｜[建置規格](BUILD-SPEC.md)

## 本機驗收

測試日期：2026-08-09。環境：Linux、Node.js、Python 靜態 HTTP server；未下載瀏覽器或套件。Lead 與 Verifier Kierkegaard（`gpt-5.6-luna`）分別執行測試。

| 項目 | 結果 | 證據 |
| --- | --- | --- |
| 語法、8 模組與 DOM 契約 | 通過 | `node --check`；106 個唯一 ID；17 個 button 均有 type；8 組 module／nav／complete／reset 綁定完整 |
| 預設公式與決策 | 通過 | 波長、Friis、失配、天線選型、失諧與 edge-rate 預設值皆符合規格 |
| 反射／margin／gate 錯誤路徑 | 通過 | 完美／完全反射只以 null 作顯示哨兵；低 margin、layout 三情境、compliance 四情境與 near-body 順序通過 |
| 導覽、進度、重設與錯誤清空 | 通過 | Node DOM stub 驗證導覽、即時更新、`localStorage`、全域／單元重設、提交攔截與錯誤回饋 |
| 無外部資源與 UTF-8 | 通過 | 無外部字型、圖片、SVG、Canvas、CDN、API 或分析碼；runtime 為 UTF-8 |
| 本機靜態服務 | 通過 | `http://127.0.0.1:4178/` 的 HTML、CSS、JS 均回應 HTTP `200` |
| 1440px／390px 實際渲染 | 待驗 | CSS 已含 700px／390px 單欄、橫向導覽、換行、focus 與 reduced-motion；本機無瀏覽器 |

獨立驗收沒有阻擋問題。殘餘風險為尚未取得實際瀏覽器 console、觸控與畫面證據。

## 發布驗收

| 項目 | 結果 | 證據 |
| --- | --- | --- |
| Pages | 待發布 | <https://justty32.github.io/engineer_study/iot-rf-antenna/> |
| workflow | 待發布 | — |
| 線上 runtime | 待發布 | — |
| 手機實機 | 待驗 | 發布後由使用者確認 |

- 教學近似不取代 VNA、暗室、OTA、EMC／RF 測試、模組整合條件或法規判定。
- 未下載瀏覽器、套件或執行期資產。
