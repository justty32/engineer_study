# 驗收紀錄

測試日期：2026-08-14

## 已通過

- `course.js` 通過 `node --check`。
- 九份 HTML 的 ID 分別為 14／20／4／8／21／4／6／2／3 個，均無重複；共用程式引用的 ID 在對應課程頁皆存在。
- 九頁的 CSS、JavaScript、章節與返回連結皆存在。
- 全部 runtime 與規格檔以嚴格 UTF-8 解碼通過，無圖片、CDN 或外部執行期依賴。
- Timer 預設 fixture：72 MHz／(71+1)／(999+1) = 1 kHz，週期 1 ms。
- ADC 預設 fixture：12 bit、3.3 V，理想 LSB 約 0.8057 mV；1.65 V 得 code 2048。
- Memory fixture：Flash 56+14=70/128 KB；已列 RAM 6+22+12=40/64 KB。
- CFSR 簡化 fixture：`0x00008200` 對應 PRECISERR + BFARVALID。
- 零基礎 fixture：48 MHz 的週期為 20.833 ns；十進位 165 = 二進位 `1010 0101` = 十六進位 `0xA5`。
- 字典包含 33 個可搜尋詞條及 8 組易混概念；每個詞條都有白話功能與「不要混淆」。
- 原理關卡：`volatile` 不描述成 atomic；ISR 不做阻塞工作；DMA 明示 buffer ownership；PWM 不描述成真正類比電壓；watchdog 不描述成 bug 修復。

## 待實機瀏覽器

- 目前內建瀏覽器環境缺少必要的 sandbox policy metadata，尚未取得桌面與手機實際渲染、互動與 console 證據；零基礎頁與搜尋字典也包含在待驗範圍，不得把靜態響應式規則當成實際渲染已通過。
