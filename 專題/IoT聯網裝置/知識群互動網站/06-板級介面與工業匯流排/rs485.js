(function(){"use strict";function q(s){return document.querySelector(s)}function noise(){var n=+q("#noise").value,a=2+n,b=1+n,d=a-b;q("#noise-out").textContent=n.toFixed(1)+" V";q("#va").textContent=a.toFixed(1)+" V";q("#vb").textContent=b.toFixed(1)+" V";q("#vdiff").textContent=d.toFixed(1)+" V";q("#rlogic").textContent=d>0?"1":"0";q("#noise-explain").textContent="A、B 同時"+(n>=0?"增加 ":"減少 ")+Math.abs(n).toFixed(1)+" V，但相減後仍為 "+d.toFixed(1)+" V。真實限制是兩腳相對本機地不可超出 common-mode range。"}q("#noise").addEventListener("input",noise);
var de={buffer:["太早：shift register 還沒送完","軟體 TX buffer 空不代表 UART 腳位送完。若立刻關 DE，最後 byte／stop bit 會被截斷。"],complete:["正確：最後一位真的離開腳位後再放手","等待硬體 Transmission Complete flag，再關 DE 並切回接收。"],late:["太晚：你會擋住遠端回答","本端 driver 持續驅動 A/B，遠端 turnaround 後開始回答時可能發生碰撞。"]};function deRender(){var x=de[q("#de-timing").value];q("#de-title").textContent=x[0];q("#de-text").textContent=x[1]}q("#de-timing").addEventListener("change",deRender);
function term(){var z=120,r=+q("#rs-load").value,g=(r-z)/(r+z),m=Math.abs(g);q("#rs-load-out").textContent=r+" Ω";q("#rs-gamma").textContent=(g>=0?"+":"")+g.toFixed(2);q("#rs-reflect").textContent=Math.round(m*100)+"%";q("#rs-reflect-bar").style.width=Math.round(m*100)+"%";q("#rs-reflect-bar").className=g<0?"negative":"";q("#rs-term-text").textContent=m<.03?"120 Ω 負載匹配這條教學電纜，能量進入電阻成熱，不需反射波修正。":g>0?"負載偏大，產生同相反射；越大越接近開路。":"負載偏小，產生反相反射；越小越接近短路。"}q("#rs-load").addEventListener("input",term);noise();deRender();term();}());
