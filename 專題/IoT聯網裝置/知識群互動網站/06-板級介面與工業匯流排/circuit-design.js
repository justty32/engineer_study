(function(){"use strict";function q(s){return document.querySelector(s)}function qa(s){return Array.from(document.querySelectorAll(s))}
function calc(){var vin=+q("#design-vin").value,i=+q("#design-current").value,pulse=+q("#design-pulse").value,time=+q("#design-time").value,droop=+q("#design-droop").value,loss=(vin-3.3)*i/1000,cap=pulse/1000*time/1000000/droop*1000000,rated=Math.ceil((i+pulse)*1.3/10)*10;q("#design-vin-out").textContent=vin.toFixed(1)+" V";q("#design-current-out").textContent=i+" mA";q("#design-pulse-out").textContent=pulse+" mA";q("#design-time-out").textContent=time+" µs";q("#design-droop-out").textContent=droop.toFixed(2)+" V";q("#ldo-loss").textContent=loss.toFixed(2)+" W 熱";q("#bulk-cap").textContent=cap.toFixed(0)+" µF";q("#rated-current").textContent=rated+" mA 以上";q("#design-explain").textContent=(loss>.5?"大壓差下 LDO 的熱損失已相當明顯；這個案例通常先評估 buck。":"這組 LDO 熱損失較小，但仍要查封裝熱阻與環境溫度。")+" C≈IΔt/ΔV 只算理想儲能下限，還要考慮 ESR、走線、穩壓控制迴路、容差與負載波形。"}qa(".requirement-grid input").forEach(function(x){x.addEventListener("input",calc)});
var blocks={input:["輸入邊界","先讓錯誤能量在板子入口被處理","接頭之後依風險放保險／eFuse、反接保護與 TVS。保護元件要靠近入口，讓異常電流不要先穿過整塊板。","限流電源首次上電；逐步升壓並看輸入電流是否異常。"],power:["電源責任","穩壓器要同時滿足電壓、峰值、熱與穩定性","依輸入範圍和負載決定 buck、LDO 或兩級電源；照 datasheet 選電感、輸入／輸出電容與回授值，不能任意換。","空載與最大負載量 rail；再用負載階躍看下陷、過衝、紋波與溫升。"],mcu:["MCU 最小系統","讓晶片在韌體介入前就處於合法狀態","接齊所有 VDD／GND／類比電源 pin、去耦、RESET、BOOT、時鐘與 SWD。確認 reset 期間 GPIO 不會誤開負載。","先不接周邊：量所有 rail、reset 與 clock，再用 SWD 讀 ID、燒最小 blink。"],io:["介面與外線","把 MCU 腳位能力轉成現場可生存的連線","I²C 需上拉；SPI 需正確 CS 與電壓；RS-485 需 transceiver、DE、可配置終端／偏壓，以及接頭旁保護。","用示波器／邏輯分析儀分別驗證電氣邊緣、交易與錯誤恢復。"]};qa("[data-design-block]").forEach(function(b){b.addEventListener("click",function(){var x=blocks[b.dataset.designBlock];q("#design-block-tag").textContent=x[0];q("#design-block-title").textContent=x[1];q("#design-block-text").textContent=x[2];q("#design-block-proof").innerHTML="<b>驗證：</b>"+x[3]})});
var risks={decoupling:"移除去耦：MCU 切換時瞬態電流要繞更遠，VDD 可能下陷或振鈴。",reset:"移除 reset／boot 預設：上電斜坡與漏電可能讓晶片進入錯誤模式。",debug:"移除 SWD／測試點：板子不動時，無法便宜地區分供電、reset、clock 與韌體。",pullup:"移除 I²C 上拉：開漏裝置全部放手後，SDA／SCL 沒有機制回到高電位。",protection:"移除外線保護：ESD／突波能量可能直接進 transceiver 或 MCU。",termination:"移除可配置終端：長線末端可能反射；但每台都硬裝也會讓總線負載過重。"};function review(){var off=qa("[data-review]").filter(function(x){return!x.checked});q("#review-count").textContent=(6-off.length)+" 個設計責任已顧到";q("#review-risks").innerHTML=off.length?off.map(function(x){return"<li>"+risks[x.dataset.review]+"</li>"}).join(""):"<li>目前未移除任何措施；逐項關閉可觀察對應風險。</li>"}qa("[data-review]").forEach(function(x){x.addEventListener("change",review)});calc();review()}());
