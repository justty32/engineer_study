# 機器人學習與訓練互動課程驗收紀錄

測試日期：2026-08-14
環境：Windows PowerShell、Node.js；靜態內容與整合檢查。

## 本次零基礎返工

- 新增 `00A-機器學習數學先修.html`，從四則運算與 C++ 函式基線教機率、函數、參數、損失、梯度、學習率與最佳化。
- `00` 至 `06` 各頁重寫為白話問題、一句話答案、最小前置、角色、逐步因果、數值例、完整教學後互動、證據、常見誤解與適用邊界。
- 長版字典共 35 條；每條均具有「白話、位置／角色、如何生效、具體例子、不要混淆」五部分。
- 更新首頁、README、PROJECT-BRIEF 與 BUILD-SPEC，使下一位 agent 可只靠儲存庫重建讀者基線和頁面責任。

## 已通過

- 八個教學章去除 HTML 標籤與空白後皆至少 1100 字元：最短 1115 字元；數學先修 1249 字元。
- 字典去除 HTML 標籤與空白後 5042 字元；35 條五欄位結構逐條通過。
- `node --check app.js` 通過。
- 全部相對 `href`／`src` 的目標存在；主線頁面有上一頁與下一頁，新增數學頁已接入首頁與 pager。
- 六項互動所需 DOM ID 全數存在；程式採元素存在才啟動的安全共用契約。
- HTML、CSS 與 JavaScript 不引用外部執行期資產；無圖片、遠端字型、內容傳遞網路或模型下載。
- 全部修改檔以嚴格 UTF-8 解碼通過。

## 證據邊界與待人工驗收

- 誤差累積公式假設步驟獨立；探索暴露與一維模擬支撐明確標示為教學指標，並非安全機率或真機保證。
- 尚待桌面 `1440×900`、手機 `390×844` 的實際視覺、鍵盤焦點、瀏覽器主控台與互動操作驗收；本紀錄不把靜態檢查冒充瀏覽器驗收。
- 本次只返工 `專題/機器人學習/`；未修改 GitHub Pages 工作流程、網站總入口或 `wf/`。

交付判定：內容深度、先修順序、字典結構、靜態連結與互動契約允許合併；實際瀏覽器視覺仍待人工確認。

## 既有正式發布紀錄

- 正式網址：<https://justty32.github.io/engineer_study/robot-learning/>
- 上一發布版本：`8c610aca43f10a16b0994b9361e8fd249183d4ef`。
- 上一 GitHub Actions 工作流程 `31774916075` 成功；本次返工尚未由本 agent 提交或發布。

## 深度返工本機發布模擬

- 與運動學、路徑規劃、視覺三門課共同組裝成 GitHub Pages 目錄結構；跨課程連結已依正式網址規則改寫。
- 整批共 44 個 HTML、4 個 CSS、4 個 JavaScript，嚴格 UTF-8、相對連結與目標存在性檢查通過。
- 本機 HTTP 伺服器逐一請求 52 個公開檔案，全部回傳 HTTP 200。
