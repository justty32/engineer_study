# 驗收紀錄

## 已通過的靜態驗收

- 共 14 個 HTML：首頁、00、00A、01–10 與可搜尋字典；20 個課程檔案皆可嚴格 UTF-8 解碼。
- `node --check app.js` 通過。
- 所有 HTML 具 `zh-Hant`、UTF-8、頁尾前後導覽；相對 `href`／`src` 全數存在。
- 無外部資產、圖片、內嵌框架或表單提交；所有 JavaScript 使用的 38 個 DOM ID 都存在。
- 七軸零位正運動學交叉驗算得到 `[0.72, 0, 0.20]` m，符合三段連桿總長與基座高。
- 旋轉矩陣正交性測試通過。
- 七個關節的解析幾何雅可比線速度欄與有限差分比較，最大絕對誤差 `3.28×10⁻⁷`。
- 預設數值 IK 案例的初始六維加權誤差為 `0.5643`；40 步後位置誤差為 `4.04×10⁻¹⁶` m。此結果只驗證教學預設案例，不構成全域收斂保證。
- 章節明確區分：表示奇異／手臂奇異、關節極限／奇異、位置／位姿任務、解析／數值 IK、精確／阻尼零空間投影。
- 零基礎深度返工：12 個教學頁純文字均達約 1,100 字元以上；新增角度／弧度／三角函數先修頁。
- 字典 35 條均具白話、位置／角色、如何生效、具體例子與不要混淆；字典純文字超過 6,400 字元。

## 尚待人工瀏覽器驗收

- 桌面 `1440×900` 與手機 `390×844` 的視覺、鍵盤焦點、瀏覽器主控台與真實操作。
- 瀏覽器控制介面本次無法建立連線；人工項目已寫入 `wf/WAIT_USER.md`。

交付判定：靜態內容、公式與互動契約允許交付；視覺驗收待使用者確認。

## GitHub Pages 正式發布

- 正式網址：<https://justty32.github.io/engineer_study/robot-arm-kinematics/>
- 發布版本：`2f3787a6a2b8f3b46bd94c4031515b30c6783b16`。
- GitHub Actions 工作流程 `31773408571`：執行完成且結果為成功。
- 線上首頁及其實際引用的 CSS、JavaScript：HTTP 200。
- 手機與桌面實機視覺、鍵盤焦點及瀏覽器主控台仍未人工驗收。

上述發布版本對應深度返工前內容，不作為本次返工已上線的證據。

## 深度返工本機發布模擬

- 與路徑規劃、視覺、學習三門課共同組裝成 GitHub Pages 目錄結構；跨課程連結已依正式網址規則改寫。
- 整批共 44 個 HTML、4 個 CSS、4 個 JavaScript，嚴格 UTF-8、相對連結與目標存在性檢查通過。
- 本機 HTTP 伺服器逐一請求 52 個公開檔案，全部回傳 HTTP 200。

## 深度返工正式發布

- 發布版本：`913b096a33a16e607fdbf176c80f5afb7ab15bf7`。
- GitHub Actions 工作流程 `31776610553` 完成且結果為成功。
- 四門課合計 52 個線上 HTML、CSS 與 JavaScript 全數回傳 HTTP 200。

## 2026-08-15 進階擴展驗收

- 新增 `11-SE3李群扭量與指數映射.html` 與 `12-運動學校正敏感度與可辨識性.html`，課程增為 16 個 HTML；首頁、`10 → 11 → 12 → 字典` pager 與字典回鏈均已接通。
- 新增平面固定扭量指數積分，以及連桿／零點系統偏差對比 `1/√N` 隨機不確定度兩項自由實驗；`node --check app.js` 通過，48 個 JavaScript DOM ID 全數存在。
- 兩個新章純文字長度分別為 1571、1393 字元；字典增為 40 條，補 SE(3)、扭量、Adjoint、運動學校正與可辨識性。
- 獨立數值交叉驗算：`v=0.6 m/s`、`ω=45 deg/s`、`t=1 s` 的平面指數積分為 `(x,y)=(0.54019,0.22375) m`；0.6 m 連桿含 `+0.5 mm` 長度與 `+0.5°` 零點偏差時，末端偏差為 `5.262 mm`。
- Google Chrome headless 以桌面 `1440×900` 與手機 `390×844` 載入兩個新增頁面；初始 JavaScript 輸出皆產生，相關錯誤掃描為 0，首屏截圖未見文字重疊或頁面水平溢位。
- 進階擴展已由版本 `b607a411a7d95c2814952c242286b4076efbb1d5` 發布；GitHub Actions 工作流程 `31861734579` 的 build 與 deploy 均成功。完整鍵盤操作、所有控制值極端案例與實體觸控仍列在 `wf/WAIT_USER.md`。
