# 驗收紀錄

## 已通過的靜態驗收

- 共 13 個 HTML：首頁、00–10 與可搜尋字典；19 個課程檔案皆可嚴格 UTF-8 解碼。
- `node --check app.js` 通過。
- 所有 HTML 具 `zh-Hant`、UTF-8、頁尾前後導覽；相對 `href`／`src` 全數存在。
- 無外部資產、圖片、內嵌框架或表單提交；所有 JavaScript 使用的 38 個 DOM ID 都存在。
- 七軸零位正運動學交叉驗算得到 `[0.72, 0, 0.20]` m，符合三段連桿總長與基座高。
- 旋轉矩陣正交性測試通過。
- 七個關節的解析幾何雅可比線速度欄與有限差分比較，最大絕對誤差 `3.28×10⁻⁷`。
- 預設數值 IK 案例的初始六維加權誤差為 `0.5643`；40 步後位置誤差為 `4.04×10⁻¹⁶` m。此結果只驗證教學預設案例，不構成全域收斂保證。
- 章節明確區分：表示奇異／手臂奇異、關節極限／奇異、位置／位姿任務、解析／數值 IK、精確／阻尼零空間投影。

## 尚待人工瀏覽器驗收

- 桌面 `1440×900` 與手機 `390×844` 的視覺、鍵盤焦點、瀏覽器主控台與真實操作。
- 瀏覽器控制介面本次無法建立連線；人工項目已寫入 `wf/WAIT_USER.md`。

交付判定：靜態內容、公式與互動契約允許交付；視覺驗收待使用者確認。
