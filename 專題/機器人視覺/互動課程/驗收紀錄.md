# 機器人視覺互動課程驗收紀錄

測試日期：2026-08-14
環境：Windows PowerShell、Python HTMLParser、Node.js；靜態內容與整合檢查。

## 本次返工範圍

- 將 `00–06` 全部由短摘要改為 foundations-first 教學；保留原投影、標定誤差、RANSAC、雙目深度、延遲與輸出契約互動。
- 新增 `00A-影像資料先修.html`，補影像陣列、灰階／顏色通道、取樣、量化、雜訊、模糊與 C++ 記憶體契約。
- 字典由一句定義改為 25 條五段式條目；首頁、README、Brief 與建置規格同步更新。

## 已通過

- 8 個教學章純文字長度各為 1220–1768 字元，皆高於約 1100 字元目標；每章包含白話問題、最小前置、逐步因果、手算例、互動前說明、證據、誤解與邊界。
- 字典純文字 5160 字元；25 條皆具「白話、位置／角色、如何生效、具體例子、不要混淆」五部分。
- 全目錄檔案以嚴格 UTF-8 解碼；10 個 HTML 的相對連結目標全數存在，首頁可到達所有頁，pager 串接 `00 → 00A → 01–06 → 字典`。
- `node --check app.js` 通過；六組既有互動所需 DOM ID 與字典搜尋 DOM 全數存在。
- HTML 不含外部 script、stylesheet、圖片或 iframe 執行期請求；未新增圖片、套件與第三方依賴。
- `git diff --check -- 專題/機器人視覺` 通過，僅有 Git 的行尾轉換提示，沒有空白錯誤。
- 投影 `u=fX/Z+c`、RANSAC `1-(1-w^s)^N`、雙目 `Z=fb/d`、深度敏感度 `fb/d²` 與延遲 `vΔt` 以獨立數值交叉驗算。

## 尚待人工瀏覽器驗收

- 桌面 `1440×900`、手機 `390×844` 的文字溢出、觸控、鍵盤焦點、瀏覽器主控台與實際拖曳互動。
- 先前 `8c610ac`／工作流程 `31774916075` 只代表舊版；本次返工的有效發布證據見下方新紀錄。

交付判定：內容深度、結構、UTF-8、相對連結、DOM、JavaScript 語法與公式靜態驗收通過；允許合併進整批返工，線上與真人視覺驗收仍保留。

## 深度返工本機發布模擬

- 與運動學、路徑規劃、學習三門課共同組裝成 GitHub Pages 目錄結構；跨課程連結已依正式網址規則改寫。
- 整批共 44 個 HTML、4 個 CSS、4 個 JavaScript，嚴格 UTF-8、相對連結與目標存在性檢查通過。
- 本機 HTTP 伺服器逐一請求 52 個公開檔案，全部回傳 HTTP 200。

## 深度返工正式發布

- 發布版本：`913b096a33a16e607fdbf176c80f5afb7ab15bf7`。
- GitHub Actions 工作流程 `31776610553` 完成且結果為成功。
- 四門課合計 52 個線上 HTML、CSS 與 JavaScript 全數回傳 HTTP 200。

## 2026-08-15 進階擴展驗收

- 新增 `07-機率估測協方差與感測融合.html` 與 `08-視覺里程計SLAM與閉環校正.html`，課程增為 12 個 HTML；首頁、`06 → 07 → 08 → 字典` pager 與字典回鏈全數可達。
- 新增一維 Kalman 更新與漂移／閉環校正兩項自由實驗；`node --check app.js` 通過，20 個 JavaScript DOM ID 全數存在。
- 兩個新章純文字長度分別為 1457、1558 字元；字典增為 31 條，補狀態估測、Kalman 增益、創新、VO、SLAM 與閉環。
- 獨立數值交叉驗算：先驗 `1.0±0.3 m`、量測 `1.4±0.1 m` 時，`K=0.9`、後驗 `1.36±0.09487 m`；100 m、0.5% 系統漂移與 `8 mm/√m` 隨機尺度的合成漂移為 0.5064 m，80% 示意閉環後為 0.1013 m。
- Google Chrome headless 以桌面 `1440×900` 與手機 `390×844` 載入兩個新增頁面；初始 JavaScript 輸出皆產生，相關錯誤掃描為 0，首屏截圖未見文字重疊或頁面水平溢位。
- 進階擴展已由版本 `b607a411a7d95c2814952c242286b4076efbb1d5` 發布；GitHub Actions 工作流程 `31861734579` 的 build 與 deploy 均成功。完整鍵盤操作、所有控制值極端案例與實體觸控仍列在 `wf/WAIT_USER.md`。
