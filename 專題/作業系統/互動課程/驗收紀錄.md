# 驗收紀錄 — 作業系統（互動課程）

## 建置方式

- 由指揮者（Claude）架 `BUILD-SPEC.md`（11 章 00–10＋字典＋index，11 個確定性互動元件，元素 id 與計算邏輯寫死），codex CLI headless 依規範建置。
- `styles.css` 由 `機器人數學基礎/互動課程` 複製，未修改（4141 bytes 一致）。
- app.js 首輪建置時 codex 有一處樣板字串引號 typo（race 函式），由指揮者手動修正；其餘 12 個互動函式沿用 robot-math 慣例（`$`/`on`＋guard＋末端 `[...].forEach(f=>f())`）。

## 自動驗收（指揮者親跑）

- `node --check app.js`：通過。
- app.js 內 `Math.random`／`Date.now`／`new Date`：0 個（確定性）。
- app.js 依賴的 35 個 id 全部存在於 HTML；缺 id 0。
- select 選項 value 對齊 app.js 邏輯：svc-pick(run/file/net/crash/mem)、race-mode(lock/nolock)、va-pagesize(1024/4096/16384)、fs-block(1/4)、dl-c1–4(on/off) 全對。
- LaTeX 分隔符殘留：0；外部資源：0（僅主站連結）。

## Playwright 實機驗證（390px，系統 Chrome）

12 頁逐一測「載入即有輸出」＋「改主要輸入→輸出變化」：

| 頁 | 主輸入 | 初值 | 反應 | console err | 溢出 |
|----|--------|------|------|-------------|------|
| 00 為什麼需要作業系統 | svc-pick | 有 | 變化 | favicon(無害) | 0 |
| 01 行程是什麼 | mem-code | 有 | 變化 | 0 | 0 |
| 02 行程排程 | sch-b1 | 有 | 變化 | 0 | 0 |
| 03 執行緒與並行 | amd-p | 有 | 變化 | 0 | 0 |
| 04 同步與競態 | race-mode | 有 | 變化 | 0 | 0 |
| 05 記憶體與位址空間 | va-addr | 有 | 變化 | 0 | 0 |
| 06 虛擬記憶體與分頁置換 | pr-frames | 有 | 變化 | 0 | 0 |
| 07 檔案系統 | fs-size | 有 | 變化 | 0 | 0 |
| 08 輸入輸出與中斷 | io-data | 有 | 變化 | 0 | 0 |
| 09 系統呼叫與核心邊界 | sys-chunk | 有 | 變化 | 0 | 0 |
| 10 死結 | dl-c1 | 有 | 變化 | 0 | 0 |
| 名詞與概念字典 | term-search | 有 | 變化 | 0 | 0 |

12/12 互動元件確實掛勾、可反應輸入。數值抽驗：位址 va=9000 / 頁大小 4096 → 頁號 2、偏移 808；Amdahl p=0.9 / N=4 → 3.08 倍。皆正確。

## 部署

- slug `operating-systems`；`.github/workflows/pages.yml` 已加 trigger paths 與 `copy_course '專題/作業系統/互動課程' operating-systems`（獨立課，無跨課相對連結，不需 Python 改寫）。
- 主站入口 `互動學習網站/index.html` 第 14 位（計算機組織之後、逆向工程之前），總數 20。

## 待使用者親驗

- 部署後於手機實機開 `operating-systems/`，確認各章互動、字典搜尋、pager 導覽正常。
