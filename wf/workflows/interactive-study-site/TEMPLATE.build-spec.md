# BUILD-SPEC — <課程名>（零基礎互動課）

[interactive-study-site](README.md)｜[WORKFLOWS](../../WORKFLOWS.md)

> 複製本檔到產物目錄（`<來源主題>/<科目>-互動課程/BUILD-SPEC.md`）改寫，並**把上面那行導覽換成**課程慣用的一行：`← 立案文件：[PROJECT-BRIEF.md](PROJECT-BRIEF.md)｜工作流：[build-with-agents](../../../wf/workflows/interactive-study-site/BUILD-WITH-AGENTS.md)｜[foundations-first](…/FOUNDATIONS-FIRST.md)｜[品質關卡](…/QUALITY-GATES.md)`（相對層數依目錄深度調整）。本檔的存在意義是「照抄即可實作」：冷啟動的章節作者與 verifier 只讀本檔＋PROJECT-BRIEF＋指定來源就能開工；任何留白，下游都會自己發明一個。

目標資料夾：`<來源主題>/<科目>-互動課程/`

## 0. 鐵律（違反即失敗）

<!-- wf-nav -->
1. 純原生 HTML／CSS／JavaScript，完全離線；唯一允許的外部 URL 是每頁頂端 `main-site-link` 的 `https://justty32.github.io/engineer_study/`。
2. `styles.css` 由正本 `專題/機器人運動學/互動課程/styles.css` 原樣 `cp`，一個位元組都不改；只用既有 class，不新增 class、不寫 inline `style`。
3. 公式一律純文字 HTML（Unicode 符號＋`<sub>`／`<sup>`），嚴禁 LaTeX；`<option>` 內只能放純文字。
4. 禁止 `Math.random()`、`setTimeout` 動畫、`localStorage`、`fetch`。
5. UTF-8 無 BOM、`<html lang="zh-Hant">`、行尾 CRLF（`styles.css` 例外，不轉）；不得用 PowerShell 寫檔。
6. 不得修改來源筆記與任何既有檔；HTML 不連 `.md`，來源以文字標註筆記章號。
7. 不寫計分、進度百分比、解鎖、倒數、強制答對。
8. 每章正文 <N1>–<N2> 個中文字（計法＝漢字＋全形標點）。
9. 禁用詞配額與符號固定項照 PROJECT-BRIEF 用語裁決（明碼，不得用 HTML 實體規避）。

## 1. 檔案清單與所有權

多頁離線課程。主線為 <首頁、`00`、`01`–`NN`、`名詞與概念字典.html`、`自我檢核.html`（可選）>；每頁有上一頁／下一頁，無孤兒頁。

| 檔名 | 內容 | 唯一寫入者 |
|------|------|-----------|
| `index.html` | 課程地圖 | 包 A |
| `00-<章名>.html` | 章 00 | 包 A |
| `名詞與概念字典.html` | 字典（<N> 卡） | 包 <X> |
| `app.js` | **單一檔，所有頁面 hook**（建置指揮官獨佔） | 包 <G> |
| `styles.css` | 從正本複製 | Lead（`cp` 而已，無人編輯） |
| `驗收紀錄.md` | 驗收證據 | Verifier |

## 2. 頁面骨架契約

貼一份章節頁與字典頁的完整 HTML 骨架（`<head>`、`main-site-link`、`topbar`、`eyebrow`／`h1`／`lead`／`prereq`、固定的 `h2` 順序、`section.workspace`、`pager`），作者照抄。

## 3. 公式與符號轉寫規約

<本課用到的符號寫法：負號 U+2212、微符號 U+00B5、上下標寫法、二進位／十六進位字面值格式…>

## 4. 逐章契約（教學頁）

每章至少 <N> 個中文字，且依序提供：白話 why → 一句話答案 → 最小前置 → 首次術語展開 → 逐步因果 → 至少一個可手算例 → 互動前的完整說明 → 可觀察證據 → 常見誤解與適用邊界。互動不計分、不鎖頁。

字典頁純文字至少 <N> 字元；每條必備<白話／位置與角色／如何生效／具體例子／不要混淆>。

每章**恰好一個**自由實驗；每個互動元件一列，**欄位不得留空**；計算式含 `|` 時寫成 `&#124;` 或改用清單（未跳脫的 `|` 會把式子攔腰截斷）。

| id | type | min | max | step | value | 計算式 | 判準（怎樣算對）| 數值實例（輸入 → 預期輸出）|
|----|------|-----|-----|------|-------|--------|------------------|---------------------------|
| `<id>` | `range`／`select`／… | | | | | | | |

- `select` 的選項與 value **順序**也要寫死。
- 每個公式標符號、單位、假設、量綱；至少兩個極端案例。
- 每個數值實例都要有人**獨立重算**過（規格的算術錯會被照抄進產品）。

## 5. 字典與自我檢核契約

<字典每卡固定欄位、分組、搜尋 haystack；自我檢核題數、每題結構、不計分不鎖頁>

## 6. `app.js` 契約

<每個 widget 的 hook 函式名、讀哪些 id、寫哪些 id、確定性要求、元素存在檢查>

## 7. 驗收方式清單（給 verifier）

依 [QUALITY-GATES](QUALITY-GATES.md) 全數執行；本課另加：<抽章重算哪一章、字元級掃描哪些高頻術語與禁用詞、規模參考線（HTML / JS / MD 各多少行）>。

## 8. 下游派工切分與逐章行號表（下游只讀自己那段）

| 章 | 本檔行號範圍 | 版型範本頁 |
|----|-------------|-----------|
| `<01>` | `<X–Y>` | `<範本檔>` |

每包最多 3–4 章，一波最多 2 job；`app.js` 在 HTML 包完成、id 凍結後才動工。

## 9. 來源筆記缺口（僅列出，本案不回頭改寫筆記）

<來源沒講、本課補上的內容，逐條列出處與補法>
