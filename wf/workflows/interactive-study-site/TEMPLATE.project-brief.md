# PROJECT-BRIEF — <課程名>（零基礎互動課）

[interactive-study-site](README.md)｜[WORKFLOWS](../../WORKFLOWS.md)

> 複製本檔到產物目錄（`<來源主題>/<科目>-互動課程/PROJECT-BRIEF.md`）改寫，並**把上面那行導覽換成**課程慣用的兩行：`← 工作流：[interactive-study-site](../../../wf/workflows/interactive-study-site/README.md)｜[foundations-first](…)｜[principles-first](…)｜[品質關卡](…)｜[build-with-agents 產線](…)` 與 `← 建置契約：[BUILD-SPEC.md](BUILD-SPEC.md)`（相對層數依目錄深度調整）。段落順序固定，用不到的段留一句「不適用」而不是刪掉：下游會照這幾段找答案。

## 一句話定位

把 `<來源筆記路徑>`（<行數>）做成一門<從什麼開始蓋、每章一個互動實驗>的零基礎互動課。課名（index 的 `h1`）：**<課程名>（零基礎互動課）**；部署 slug：`<slug>`（`.github/workflows/pages.yml` 課程表）。套用 foundations-first（＋principles-first，硬體主題）。

## 用語裁決（Lead 已裁決，全課無例外）

台灣慣用語優先。列成可機械掃描的表：`本課一律寫｜不得寫（配額）｜備註`；「恰好 1 處」的詞只准出現在字典對應卡的「不要混淆」欄且須明碼。其餘固定項：反相／乘積／負號／微符號等符號寫法、術語首次出現格式 `繁中（English, 縮寫）`。譯名與對照表衝突時記 `wf/workflows/decisions.md`。

## 讀者基線與理解債

- 已會什麼（相鄰強項）：<…>
- 不會什麼（真正的空白）：<…>
- 理解債名詞（看過但沒真懂，會擋住後面的章節）：<…>
- 既有版本造成理解債的地方（返工才寫）：<…>

預設讀者＝擁有者：軟體很強、電機／通訊本科但基礎不扎實（`wf/workflows/plain-explain.md` 讀者基線表）。**不得因為讀者某個領域很強，就假設他別的領域也懂。**

## 目標能力

讀者完成後能<解釋／計算／判斷／診斷／設計>什麼——寫成可觀察的動作，不是「理解 X」。

## 概念依賴與頁面責任

```text
<最底層概念> → <…> → <…> → <最終能力>
```

- `00`：<零基礎起點教什麼>
- `01`–`0N`：<各章一句話責任>
- `字典`：<每條固定含哪幾段>

## 語言、互動與證據契約

- 正文與 UI 語言：<…>；術語首次出現的格式：<例：繁中（English, ACR）>。
- 互動：無計分、無答對解鎖；控制值一改，數值與 why 立刻更新。
- 證據：每章指出可觀察的證據來源，並明說它**證明不了**什麼。

## 章節與來源對應表（可追溯）

| 章 | 一句話責任 | 來源筆記章節 | 相鄰課程 |
|----|-----------|-------------|---------|
| `00` | <…> | <筆記 §x> | <…> |

- 本地：<檔案路徑>（HTML 內以文字標註筆記章號，不連 `.md`）
- 外部一手（只供追溯，不是執行期依賴）：<URL 或「無」>

## 與其他課程的邊界

<哪些概念交給既有課（相對連結指到該課 `互動課程/` 頁面，掛站時靠 `replacements` 改寫）、哪些本課自己教>

## 教學與模型邊界

用了哪些理想化／簡化模型、在什麼範圍內成立、超出會怎樣。**簡化不得講成工程保證。**

## 不做事項

- 不做<哪些互動機制>；不寫計分、解鎖、倒數。
- 不引入外部字型／圖片／分析碼／依賴；不用 LaTeX。
- 不修改<哪些來源檔>與名詞對照表。

## 交付物與完成定義

<HTML 頁數、字典卡數、自我檢核題數；Done when 照 interactive-study-site 與 foundations-first 三條；掛站接線屬 Lead 收尾>

## 已知風險與待辦

<來源最薄的章、需要真人驗的項、待 Lead 裁決的譯名>
